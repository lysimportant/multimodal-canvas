# 首页视觉、鼠标反馈与动效 TODO

更新时间：2026-09-06。状态：**首页实现、专项质检和已登录账户联合验收完成。** 主计划见 [后台与账户 TODO](../TODO-ADMIN.md)，通用规范见 [全站 UI/UX 与质检清单](admin-ui-ux-review.md)，最终日志与 Git 交付依据见 [实施检查点](../IMPLEMENTATION-CHECKPOINT.md)。

## 本次实现

首页保留 `Multimodal Canvas` 产品名和工作台入口，使用全幅网格工作流场景、真实参考画面、克制的节点与连线。正文采用连续内容区，提供可播放的本地公开样片、资源条目和现有项目入口。颜色使用中性底色、青绿功能色、珊瑚强调与少量亮绿；支持现有明亮、深色和高对比主题。

首屏文字和场景在 500-700ms 内进入；内容区初次进入视口时轻量显示，始终保留首帧可见内容。连线由节点真实边界计算，缩放与尺寸变化后自动更新。鼠标为直径 264px 圆形窗口的中心，圆内显现平时隐藏的工作流网络、分支节点与公开图像的另一层画面，圈外保持原有内容。原生指针保留；正文可以显影，输入、选区、媒体控件、弹层、滚动和离开窗口时退出。按钮与视频入口保留独立边缘反馈和键盘焦点。

普通文案与隐藏文案复用同一模板及定位，标题、正文、入口和素材说明在圆内外对齐；隐藏层设置 `inert`、`aria-hidden` 和 `pointer-events: none`，不会重复提供读屏标题、可聚焦链接或业务请求。圆环与裁剪使用同一首屏局部坐标，窗口缩放后清除旧位置；十字标记与横纵跟随线已移除。

动效遵循 `prefers-reduced-motion`，可通过首页右上角图标关闭。触屏不启用指针追随；后台标签与离屏首屏停止装饰运动。指针移动用单次 RAF 合并写入，不创建持续 React 渲染循环。低性能设备、节省流量设置或五秒内三次不短于 120ms 的长任务会降级为静态。

首页不会读取私有项目、注销账户、创建项目或运行生成任务。演示视频只预加载元数据，不自动播放；原生播放器支持播放、暂停、进度、静音和全屏，离开视口或进入后台时暂停且不自动恢复。视频加载失败保留 poster 并允许明确重试，图片加载失败保留比例与可访问替代说明。

## 文件与素材来源

| 文件                                                           | 职责                                         |
| -------------------------------------------------------------- | -------------------------------------------- |
| `apps/web/src/pages/HomePage.tsx`                              | 首页结构、工作流、现有工作台/继续项目入口    |
| `apps/web/src/pages/HomeMotion.ts`                             | 动效、真实连线、鼠标、性能退化与生命周期     |
| `apps/web/src/pages/HomeHeroCopy.tsx`                          | 两层共享文案与入口布局，隐藏副本不增加交互   |
| `apps/web/src/pages/HomeRevealField.tsx`                       | 圆内静态工作流网络与公开位图，不读取业务数据 |
| `apps/web/src/pages/HomeDemoMedia.tsx`                         | 原生播放器、离屏/后台暂停与错误重试          |
| `apps/web/src/pages/HomeDemoImage.tsx`                         | 公开图片加载与稳定尺寸的错误替代             |
| `apps/web/src/pages/home-page.css`                             | 首页构图、主题、入场、滚动与 hover 样式      |
| `apps/web/src/pages/HomePage.test.tsx` / `HomeMotion.test.tsx` | 页面内容、媒体错误、指针与资源清理单元测试   |
| `apps/web/e2e/home-page.spec.ts`                               | 桌面/触屏截图、实际 App 路由、媒体与性能专项 |

演示素材来自 [MDN interactive-examples](https://github.com/mdn/interactive-examples)，2026-09-06 核实仓库的 [CC0 1.0 许可](https://github.com/mdn/interactive-examples/blob/main/LICENSE)。没有调用任何付费服务，没有使用用户项目或真实账户内容。详见 [本地素材说明](../apps/web/public/demo/README.md)。

| 素材                     | 尺寸/时长                      | 体积           | SHA-256                                                            |
| ------------------------ | ------------------------------ | -------------- | ------------------------------------------------------------------ |
| `field-study.mp4`        | 960 × 540 / 5.055 秒           | 1,128,375 字节 | `0cd83d944a6ca7822b4a8306cecc60a36e859b041f6702c6a1ad9ead78924451` |
| `field-study-poster.jpg` | 960 × 540，视频第 0.2 秒完整帧 | 69,636 字节    | `e592664e22471ae9da16cdcda662ec2f59b2c749ff8656c842f58d874d6213e6` |

原始视频地址：<https://interactive-examples.mdn.mozilla.net/media/cc0-videos/flower.mp4>。poster 由本地 Chromium 将该视频第 0.2 秒绘入 canvas 后按 JPEG 质量 0.9 导出。页面明确标注「公开素材」「演示项目」，没有伪造模型生成状态、成本、运行结果或音频波形。

## HOME TODO：16 项交付

- [x] **HOME-01 / P1 首屏构图**：三个桌面尺寸均能看到下一内容区线索，文字与参考图不相交。窄屏采用正常文档流，长项目名能够换行，已修正视频节点遮挡图片脚注的问题。
- [x] **HOME-02 / P1 色彩与文字层级**：明亮、深色、高对比主题截图与自动对比度检查通过；正文、辅助文字、功能强调和页尾标题对比度至少 4.5:1，无连续字号缩放或负字距。
- [x] **HOME-03 / P1 真实媒体与来源**：本地公开视频及同源 poster 已落盘，媒体像素抽样超过 120 种颜色，来源与哈希已记录。
- [x] **HOME-04 / P1 首次进入动效**：500-700ms 分组入场，首帧可见，真实 `/` 路由连续三次访问入口可用。
- [x] **HOME-05 / P1 工作流连线动态**：连线按节点端点计算，短线沿输入到输出方向移动，固定节点不漂移。
- [x] **HOME-06 / P1 演示场景局部响应**：圆内揭示独立的隐藏工作流和图像层；标准 Canvas 图片解码比对确认变化局限于圆内，不移动正文或入口，不拦截点击。
- [x] **HOME-07 / P1 鼠标周围圆形窗口**：直径 264px 的细圆环与隐藏层共同跟随鼠标；正文可显影，输入、触摸、选区、媒体、弹层、窗口失焦及卸载时退出。
- [x] **HOME-08 / P1 目标边缘悬停反馈**：实际按钮/链接 160ms 边缘反馈、同等清晰的键盘焦点；hover 前后包围框保持一致。
- [x] **HOME-09 / P1 滚动内容节奏**：IntersectionObserver 首次显示动画，基础内容永不透明隐藏；快速滚动和锚点跳转保留原生行为。
- [x] **HOME-10 / P1 媒体播放闭环**：真实播放、暂停、静音、跳到第 3 秒、离屏暂停、错误与重试均经浏览器验证。
- [x] **HOME-11 / P1 入口和账户流程连续性**：首页工作台、继续项目及匿名路由通过；最终真实 HTTP/Prisma/浏览器完成已登录创建/运行项目、账户菜单、运行中及完成后往返、返回首页和显式注销，误注销 0 次。
- [x] **HOME-12 / P1 减少动态效果**：系统设置在运行期间切换立即生效，关闭追随与装饰运动，保留导航和用户主动播放。
- [x] **HOME-13 / P1 触屏与混合输入**：精细指针媒体查询与实际 pointerType 双重检查；390px 触屏截图无横向溢出或残留指针。
- [x] **HOME-14 / P1 生命周期与性能退化**：单次 RAF、观察器清理、十次卸载重挂、后台状态、四倍 CPU 限速和严重长任务降级均已验证；测量见下文。
- [x] **HOME-15 / P1 视觉与键盘可访问性**：三个桌面尺寸、720 × 450 的 200% 缩放等效布局、长中文项目名、键盘焦点、装饰读屏隐藏和媒体错误语义通过。未进行人工屏幕阅读器朗读验收。
- [x] **HOME-16 / P1 专项验收与交付证据**：圆形显影更新后的首页 10 项单测与 8 项 Playwright 专项通过；包含圆内像素/圈外不变、文字对齐、桌面/触屏、媒体与性能。全仓库与账户联合记录见实施检查点，本轮新增登录侧栏动效记录见下文。

## HOME-QA：12 项专项验收

| 编号       | 结果与证据                                                                                                                 |
| ---------- | -------------------------------------------------------------------------------------------------------------------------- |
| HOME-QA-01 | 1280 × 720、1440 × 900、1920 × 1080 无横向溢出，首屏文字与主图不相交，下一段线索可见；三主题正文与页尾标题自动对比度通过。 |
| HOME-QA-02 | 原生图片尺寸 960 × 540，canvas 采样非空；本地素材可访问，图片与视频错误替代单测通过。                                      |
| HOME-QA-03 | 真实 App `/` 路由三次访问可用；独立页面十次卸载重挂始终只有一个装饰节点。                                                  |
| HOME-QA-04 | 连线使用真实节点边界，ResizeObserver 更新；指针只修改装饰，鼠标位置不改变正文或入口尺寸。                                  |
| HOME-QA-05 | 圆环和裁剪在同一帧使用相同局部坐标，正文保持显影；触屏、失焦、系统减少动态及卸载正确退出。                                 |
| HOME-QA-06 | 主入口 hover 前后边界一致，键盘焦点为可见实线，静态节点没有伪播放或伪运行按钮。                                            |
| HOME-QA-07 | 原生锚点进入演示区并滚动返回，内容持续可见，装饰观察器不更改滚动位置。                                                     |
| HOME-QA-08 | 视频 currentTime 实际增长，宽度 960；暂停、跳到第 3 秒、静音切换、离屏暂停和错误重试通过。                                 |
| HOME-QA-09 | 实际 `/` -> `/workspace` 三次入口回归无生成/项目 POST、无 pageerror；已登录最终联合验收通过，返回首页/工作台仍保留身份。   |
| HOME-QA-10 | reduced-motion 在页面存续期间切换生效；触屏没有追随指针，低核心数设备直接静态降级。                                        |
| HOME-QA-11 | 同机静态/动态帧采样、十次卸载 GC 后堆占用、四倍 CPU 限速入口交互、三次长任务降级均通过。                                   |
| HOME-QA-12 | 最新首页 10 项单测、8 项浏览器专项通过；此前全仓库、后台与账户联合证据保留于实施检查点，未把条件 skip 当作通过。           |

## 验证与性能记录

环境：Windows、本地 pnpm workspace，Node 与 pnpm 版本以主任务 `IMPLEMENTATION-CHECKPOINT.md` 基线为准。首页使用项目既有 React、lucide、Vitest 与 Playwright，没有新增依赖。独立开发服务器为 `http://127.0.0.1:5186/`，未修改生产容器。

- 原始基线：既有首页两项单测通过；原实现没有首页 animation/transition/鼠标监听，没有真实图片或视频文件。性能对照采用同一最终页面的静态与动态模式，不把旧版源码审计当作视觉或帧率测量。
- `pnpm --filter @multimodal-canvas/web exec vitest run src/pages/HomePage.test.tsx src/pages/HomeMotion.test.tsx`：2 个文件，8 项通过。
- `WEB_BASE_URL=http://127.0.0.1:5186 pnpm --filter @multimodal-canvas/web exec playwright test e2e/home-page.spec.ts`：7 项专项通过。前六类隔离验证使用真实首页组件；另有实际 App `/` 路由回归，避免把组件挂载误报为账户系统集成测试。Windows PowerShell 先设置 `$env:WEB_BASE_URL` 再运行 pnpm。
- `pnpm --filter @multimodal-canvas/web typecheck`：通过。
- `pnpm --filter @multimodal-canvas/web build`：通过；Vite 提示主 chunk 大于 500kB。该提示涉及整个应用，未为首页任务调整打包策略或拆分其他模块。
- 首页新增/修改源码、样式和专项测试的 `prettier --check`：通过。Markdown 按仓库 ignore 设置另行人工检查链接与状态，不谎称已被格式工具扫描。

性能预算：首屏 poster 小于 100KB、视频文件小于 1.5MB，浏览器可以为元数据读取部分视频；桌面 RAF p95 小于 60ms；十次卸载重挂后显式 GC 的 JS 堆增量小于 2MB；指针停止后没有持续 RAF 循环。三次不短于 120ms 的页面长任务触发静态退化，不等待所有媒体解码。

同机 1440 × 900、Chromium、每组 90 个帧间隔：静态与动态的中位数均约 16.7ms，p95 约 16.8ms，大于 50ms 的帧均为 0。十次卸载重挂后显式 GC 测得 JS 堆从 7,905,528 增至 8,618,520 字节，增量 712,992 字节；四倍 CPU 限速下指针移动、关闭动效和进入工作台约 702ms。数据用于本机回归，不宣称所有设备达到固定帧率，也不替代长期使用的内存分析。

截图与 JSON 附件写入 `apps/web/test-results/`；测试每次运行可能覆盖该目录。截图仅包含公开素材和合成演示项目。代码验证没有发现首页控制台异常、私有资源请求、重复项目创建或生成任务提交。

## 恢复检查点

本轮圆形显影和登录侧栏的最终记录：

- 首页 10 项单测、8 项 Playwright 专项通过，覆盖真实像素裁剪、两个文字层精确对齐、移动/离开/缩放、系统减少动态、媒体、主题和生命周期。
- 登录侧栏保留 410px 右侧布局，260ms 滑入与 220ms 遮罩淡入，退出 220ms 后卸载；快速重开可反向过渡，关闭期间无重复提交，认证完成后再把焦点交给新建表单。
- 新登录单测 6 项和既有认证 58 项通过；最终构建的登录专项及既有全站冒烟共 28 项通过。桌面/390px 均采到实际中间帧，减少动态效果时约 3.9ms 卸载，不等待退出动画。
- 最终仓库 lint/typecheck/test/build 通过，Web 462 项；新建项目始终需要用户明确提交。没有新依赖、真实账号写入或生产服务更新。
- 圆内效果截图与像素 JSON：`.data/home-reveal-pixels-final/`；首页完整专项：`.data/home-reveal-playwright/`；登录逐帧数据与截图：`.data/home-login-motion-static-artifacts/`。
- 最新本机样本动态 RAF p95 16.8ms，10 次卸载并 GC 后堆增量 738904 字节，4 倍 CPU 限速入口操作约 899ms。构建仍有约 1004KB 主 JS 分块提示，未扩大为打包改造。

最后成功阶段：首页 UI、媒体、输入退化、性能与生命周期及 HOME-11/HOME-16 最终账户集成通过，统一构建与静态冒烟已完成。未验证的人工读屏、长时间堆占用和真实手机复杂画布行为不标记为已完成，也不在首页任务中扩展实现。后续恢复请先核验实施检查点、Git 状态及最终日志。
