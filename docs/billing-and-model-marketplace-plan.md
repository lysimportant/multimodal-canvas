# 平台计费与模型广场执行方案

更新时间：2026-09-19。状态：方案已确认，业务代码、数据库迁移和页面尚未实施。

本文对应 `TODO-CONSOLIDATED.md` 的 P2-03，记录后续开发和验收范围。本次为 P2 文档更新，只修改本方案及对应待办；不实现扣款、不应用迁移、不发起真实 Provider 请求或发布生产服务。

## 基线与证据

- 当前项目按 Provider 凭据保存模型目录，刷新时整批替换该凭据的目录；模型 ID、名称及可选能力、限制、价格由上游响应规范化得到：[`apps/api/src/settings.ts`](../apps/api/src/settings.ts)。这份目录尚不是独立的平台模型库，也没有正式的手动添加与平台定价入口。
- 当前提交路径只把目录价格作为 `estimatedCost`，`Run.cost` 保存的是估算；无价格或无效价格不阻断提交。它们不是用户扣款依据：[`apps/api/src/app.ts`](../apps/api/src/app.ts)、[`apps/api/src/usage-policy.ts`](../apps/api/src/usage-policy.ts)。
- 当前 `UsageLedger` 记录 Provider 明确报告的金额；一个 Run 的多个执行节点可以分别产生费用记录。现有去重机制不能替代用户钱包、冻结和退款账本：[`apps/worker/src/prisma-persistence.ts`](../apps/worker/src/prisma-persistence.ts)、[`prisma/schema.prisma`](../prisma/schema.prisma)。
- 当前移除连接使用软禁用并保留历史凭据；Schema 中硬删除凭据会级联删除对应上游目录。平台模型和历史价格必须独立保存，不沿用这种级联关系。
- Sub2 视频真实验收返回了任务状态、视频内容和时长，但没有 usage 或费用字段：[`docs/live-provider-acceptance.md`](live-provider-acceptance.md)。
- Provider 路由、响应、取消、重试和 usage 边界的已知证据见 [`docs/provider-contract-acceptance.md`](provider-contract-acceptance.md)。
- 管理员、普通用户和配置权限的现有入口见 [`docs/admin-operations.md`](admin-operations.md)；账户基线见 [`TODO-ADMIN.md`](../TODO-ADMIN.md)。
- 当前未完成事项仍记录在 [`TODO-CONSOLIDATED.md`](../TODO-CONSOLIDATED.md)；本方案对应其中的平台计费、模型广场与 Provider 成本对账。

## 目标与边界

系统尚未对客户开放，目标统一为平台服务端余额计费，不长期维护“上游计费 / 平台钱包”两套可选模式。“本地”指平台后端及数据库负责报价、余额和账单；Provider 继续提供模型能力，并向平台收取上游费用。

管理员维护独立的平台模型、API 调用绑定和版本化售价。用户预付余额，提交前冻结允许消费的最高额度，再按实际执行的收费项结算。用户消费和 Provider 成本分别记账，所有付费入口共用现有 API / Worker / PostgreSQL 内的一个账务模块，首期不拆独立计费微服务。

第一阶段采用单一结算币种，由管理员发放内部测试额度并记录流水；具体币种在账务编码前确定。暂不接支付宝、微信、Stripe 等真实支付，不实施多币种钱包或自动换汇。普通生成、批量生成、工作流、提示词优化、图片反推和显式重试均纳入统一计费；自动触发的付费操作必须计入用户确认的费用范围。

现有项目、资产、任务、凭据和 usage 记录继续保留，历史运行不追扣。实施迁移前备份并验证恢复，不因尚未对客就清空数据。生产操作、破坏性迁移和真实付费验收仍需按各自规则执行；本次文档更新不授权这些操作。

## 页面入口

PC Web 优先，入口与目录共用一份平台模型数据：

| 位置               | 页面或入口                    | 用途                                                                |
| ------------------ | ----------------------------- | ------------------------------------------------------------------- |
| 主导航“工作台”旁   | `/models` 模型广场            | 按文本、图片、视频、音频分类，支持搜索、规格、能力和价格详情        |
| 管理后台           | `/admin/models` 模型管理      | 同步导入、手动新建、编辑、API 绑定、定价、审核和上下架              |
| 现有设置           | `/settings` 的“连接与 Key”    | 管理员维护 API 地址、Key 和上游目录刷新，保留节点默认等已有设置分类 |
| 节点编辑器         | 模型选择器                    | 读取同一份已发布平台目录，显示规格价格，不必先离开画布访问广场      |
| 头像菜单与个人中心 | `/account/billing` 余额与账单 | 查看可用余额、冻结余额、消费明细和退款                              |

管理员“模型管理”提供“同步导入”和“手动新建”两个入口。普通用户只能看到已发布模型的展示信息、已验证能力、限制、售价和可用状态；API Key、内部凭据编号、上游成本明细与管理地址不进入普通用户响应。页面隐藏入口不能替代服务端权限校验。

## 产品角色和主流程

### 管理员

1. 在“设置 → 连接与 Key”中维护 Sub2/New API 等连接。
2. 在“模型管理”中同步上游候选目录并选择导入，或直接手动新建平台模型。
3. 配置展示信息、媒体类型、规格，以及连接、真实上游模型 ID 和调用合同的绑定。
4. 验证能力与参数兼容性，设置平台售价、计费单位和价格版本，再审核上架。
5. 发放测试额度，查看用户账单、Provider 成本和待对账项。

### 普通用户

1. 在主导航进入“模型广场”，或直接在节点选择已上架模型与规格。
2. 查看本次任务的报价、最高扣费额度和余额；自动优化、反推等附带付费调用也需展示。
3. 确认后由服务端确定付款账户、冻结余额并使用对应 API 连接执行。
4. 在个人账单查看收费明细、冻结、释放或退款状态，通过任务关联查看结果。

### 任务收费流程

```text
选择平台模型与规格
  → 服务端验证有效调用绑定、价格版本及费用上限
  → 原子保存任务、报价快照、余额冻结和待投递记录
  → 投递队列，按实际子调用保存发送意图和 request_id/task_id
  → 成功且结果可交付：按确认用量或已发布固定价逐项结算，释放多余冻结
  → 明确未执行或失败的收费项：释放对应冻结
  → 请求结果或计费用量未知：待核实，不自动重发
  → 仅上游成本未知：用户账单正常结算，Provider 成本单独对账
```

一个 Run 可以有多条真实子调用收费明细，Run 账单只是汇总。每个收费项使用稳定业务身份；浏览器重复提交、Webhook 重放、队列重投及 Worker 恢复只能命中原记录，不能再次冻结或扣款。用户明确发起新的付费重试时才创建新执行身份和新报价，恢复已有结果不算新消费。

## 模型广场

### 平台模型、调用绑定和 API 连接

| 对象     | 保存内容                                                                               | 变化边界                                                       |
| -------- | -------------------------------------------------------------------------------------- | -------------------------------------------------------------- |
| 平台模型 | 稳定 `platformModelId`、展示名称、介绍、分类、规格、发布状态                           | 由管理员维护，价格版本关联此身份；更换连接不改变模型身份       |
| 调用绑定 | 平台模型、连接身份、精确 `upstreamModelId`、接口合同、已验证能力与参数限制、绑定修订号 | 验证后显式启用，变更只影响新任务；首期每个模型启用一个绑定     |
| API 连接 | 地址、加密 Key、凭据版本、可用状态                                                     | 管理连接及轮换；保留历史任务所需版本，不级联删除平台模型或价格 |

现有 `ModelCatalog` 继续作为连接范围的上游候选目录缓存。平台模型与人工设置独立持久化，不把平台售价或商品身份寄存在刷新时会替换的目录行中。节点和项目默认模型引用平台模型身份；提交时由服务端解析并冻结绑定、真实上游模型 ID、凭据版本、合同和价格版本。

同名不代表同一个模型，不能按展示名称自动合并或替换绑定。管理员可以修改展示名称，但发送给 Provider 的 `model` 必须保留已确认的精确 ID、大小写和后缀。

### 同步导入

同步请求使用管理员指定连接的模型目录接口；OpenAI 兼容连接通常为 `/v1/models`。同步结果先进入候选列表，管理员勾选导入后建立 `draft` 平台模型，不把每次刷新得到的全部模型直接上架。

| 上游数据                 | 保存与使用规则                                                                                 |
| ------------------------ | ---------------------------------------------------------------------------------------------- |
| 模型 ID、名称            | ID 必须存在；缺少名称时显示 ID。它们用于选择及绑定上游模型，不自动覆盖人工展示名               |
| 媒体类型、能力、参数限制 | 仅把明确返回的字段作为来源证据；缺失标记待确认。上架前按实际合同验证，不依据名称推断就承诺能力 |
| `price` / `pricing`      | 保存为 `provider_declared` 成本参考，记录单位和来源，不自动成为用户售价                        |
| 目录时间与来源快照       | 保存连接身份、同步时间和脱敏后的响应；不保存 Key、认证头或敏感签名 URL                         |

后续同步只更新上游来源数据及变更提示，保留人工名称、介绍、能力修正、平台价格和发布状态。已发布能力与上游新证据冲突时标记待复核并暂停不兼容的新调用，不能静默扩展能力。单次刷新失败保留上次目录并显示过期状态；成功刷新后缺失的条目标记待核实，不自动删除平台模型、手工模型或调用历史。

`/v1/models` 不保证返回完整能力或费用，也不保证覆盖可通过精确 ID 调用的全部模型。目录是否出现与实际可调用性分别验证；确认绑定失效时，模型保留并显示暂不可用。

### 手动新建和手动定价

管理员无需先成功同步目录，即可手动新建平台模型：

1. 填写展示名称、介绍、分类、媒体类型、规格和排序，先保存为草稿。
2. 选择 API 连接，填写真实上游模型 ID、接口合同、能力及参数限制；记录依据和验证状态。
3. 配置计费单位、各规格单价和价格版本；未定价不等于免费。
4. 检查绑定、能力、计量来源、报价上限与售价后上架。需要真实生成验证时另按真实 Provider 验收规则执行。

手工创建的数据不会被上游刷新覆盖。模型 ID 可以尚未出现在目录中，但必须有可验证的调用合同，不能通过填写任意名称就宣称支持生成。展示名可自定义，能力和参数仍受实际 Provider 限制。

### 更换 API 后保留模型

| 操作                        | 平台模型与价格                   | 调用行为                                               |
| --------------------------- | -------------------------------- | ------------------------------------------------------ |
| 轮换同一连接的 Key          | 保留模型身份、人工字段和价格     | 验证新凭据权限后用于新任务，历史任务保留原版本         |
| 换到另一家 API              | 保留模型、规格、售价和画布引用   | 核对实际模型及合同，创建并启用新绑定；不因同名自动转接 |
| 新 API 不支持对应模型或规格 | 保留记录                         | 标记暂不可用或禁用不支持的规格，禁止提交对应新任务     |
| 停用或移除旧连接            | 保留平台模型、旧绑定、价格和账单 | 无有效绑定时禁止新调用；不级联删除平台数据             |
| 上游目录刷新失败或模型缺失  | 保留人工记录和已有价格           | 标记过期或待核实，确认失效后阻止新调用                 |

例如平台模型绑定 API A 后改用 API B，只替换经验证的调用绑定，平台模型编号与售价可以保持不变；真实上游模型、协议或规格不兼容时不能仅改地址继续发送。

已提交任务固定原连接、凭据版本、模型 ID、合同、参数和价格。停用旧 API 前先处理在途任务；旧凭据在供应商侧失效时记录恢复受阻，不能拿新 API 查询旧任务 ID，也不能自动换 API 重新发起付费创建请求。

### 模型状态

发布状态由管理员维护：

- `draft`：手动新建或从候选目录导入，未审核，普通用户不可见。
- `published`：已审核上架；实际选择和提交还需有效价格与可用绑定。
- `paused`：暂时下架，历史任务仍可查询。

可用状态单独记录：绑定经确认不可用时显示 `unavailable`，保留人工发布状态和历史记录；恢复可用需重新验证。同步失败仅标记来源过期，不能直接视为 Provider 已下架。

### 价格规则

平台价格由管理员手动配置为不可变版本，关联平台模型及规格，包含结算币种、精度、舍入规则、生效时间、计量来源和收费单位。更新价格创建新版本，不能覆盖旧任务和历史账单。

| 类型 | 首期收费方式              | 定价和计量边界                                                                       |
| ---- | ------------------------- | ------------------------------------------------------------------------------------ |
| 文本 | 输入、输出 Token 分别计价 | 缓存 Token 只有合同明确时才单列，避免重复统计；限制最大输出以形成冻结上限            |
| 图片 | 按成功交付的图片张数      | 根据模型、尺寸、质量分别定价，数量计入报价和明细                                     |
| 视频 | 按次或按秒                | 每个价格版本明确选择规则；按模型、分辨率、时长和数量报价，明确计费时长来源与取整方式 |
| 音频 | 按字符数或时长            | 按模型合同确定字符统计、输入或输出时长及单位，不能混用                               |

缺少可靠用量的模型可以预先发布固定单次价格；固定价覆盖哪些产物必须写清。已经按用量报价的任务若最终缺少计量数据，进入待核实，不能临时改用固定价或以估算用量扣款。

Provider 参考价格和成本倍率只能用于管理员生成定价草稿，审核发布后的平台价格才是用户收费依据；更换 API、刷新目录和上游涨价都不自动改变售价。缺价格时禁止平台付费提交；明确免费规则需发布零价版本并保留审计。

提交前冻结模型、绑定修订、参数、价格版本、报价有效期和最高扣费额度。文本按可验证输入量及最大输出额度报价；无法给出可执行上限的规则不得开放预付调用。报价过期或参数变化需重新报价，已受理任务继续按快照执行，最终用户扣费不得超过确认上限。工作流报价覆盖计划中的所有收费子调用，未执行部分释放，新增付费范围需重新确认。

## Provider 成本与用户收费

系统分别保存提交前报价及上限、用户实际消费 `user_charge`、上游实际成本 `provider_cost`。`Run.cost` 的历史估算不能作为实际用户扣款或真实 Provider 成本。普通用户查看自己的报价、消费、冻结和退款；管理员额外查看成本及对账证据。

用户结算与上游成本使用独立状态。结果可交付、固定价或真实计量已确认时，即使 Provider 没有返回费用，用户账单仍可按已发布规则结算，成本标为 `unknown` 或 `pending_reconciliation`。只有执行结果或收费所需用量不明时，才阻止该用户收费项结算；成本缺失不能让用户余额无限冻结，也不能伪记为零。

首期按成功交付收费：明确失败、未执行或最终无法交付的收费项释放冻结，若上游已产生费用仍记录为平台成本。局部成功的工作流结算已交付项并释放其他确定未收费项；已发出的请求不会因本地点击取消就自动认定失败或免费。

### Sub2/New API 处理规则

- 普通 `/v1/models` 响应用于同步模型目录，不保证提供完整计费规则。
- 普通生成响应不应假设包含 `actual_cost` 或 `total_cost`；Token 数、媒体时长等计量信息和明确金额分别保存。
- 视频报价使用固定的模型、分辨率、时长规则、数量和价格版本；结果中的媒体时长只有符合该价格版本的计量合同，才能作为结算用量。
- 本地规则计算的是平台售价或成本估算，不等于 Provider 已确认的实际扣款；不同币种成本保留原币种，首期不自动换汇计算利润。
- 保存 `request_id`/`task_id` 后，按具体部署已确认的管理 usage 接口和权限对账。不能把公开文档或普通聚合 usage 当作当前部署已支持逐任务查询的证明。
- Provider 返回明确金额和币种时，保存脱敏 usage、来源和解析时间；重复事件不能覆盖首条事实，冲突进入对账，修正保留关联记录。

## 钱包和账本设计

### 建议的数据模型

以下是实施时的 Prisma 设计方向，名称可以在编码阶段调整：

| 模型                 | 主要职责与身份                                                                 |
| -------------------- | ------------------------------------------------------------------------------ |
| `PlatformModel`      | 稳定模型身份、人工展示信息、规格、发布状态；不依赖凭据或上游目录行的生命周期   |
| `ModelBinding`       | 模型到连接、精确上游 ID、接口合同及已验证能力的版本化映射                      |
| `PricingVersion`     | 平台模型及规格的不可变规则、币种、单位、舍入与生效时间                         |
| `Wallet`             | 付款用户、结算币种、可用及冻结金额、并发版本；可由流水核对                     |
| `WalletEntry`        | 追加式额度发放、冻结、扣款、释放、退款和调整流水；关联原业务及幂等键           |
| `RunCharge`          | 一次运行的付款账户、报价快照、授权上限和收费项汇总，不替代逐项收费事实         |
| `ChargeItem`         | 按 Run、节点及逻辑子调用身份保存的冻结、实际用量、价格版本、最终消费和结算状态 |
| `ProviderCost`       | 逐 Provider 调用的成本状态、可空金额、原币种、来源及 `UsageLedger` 事实关联    |
| `ReconciliationItem` | 关联收费项或成本项、任务 ID、待核实原因、截止时间和处理审计                    |

金额使用统一高精度整数或 Decimal，精度应覆盖 Token 等微额计费，不能逐次先舍入到“分”；禁止用 JavaScript 浮点数作为账务来源。`UsageLedger` 继续保存上游金额事实，不直接承担用户余额或冻结状态；不覆写历史流水，退款和纠错以关联的反向或调整流水表达。

### 结算状态

每条 `ChargeItem` 的用户结算状态建议使用：

- `QUOTED`：已固定价格版本并生成报价；
- `HELD`：已冻结用户余额；
- `SETTLED`：结果可交付且已按确定规则结算；
- `RELEASED`：确定无需用户付费，已释放对应冻结；
- `PENDING_VERIFICATION`：执行结果或计费必需用量未知，保持冻结等待核实，不因超时或本地取消自动结算、释放；
- `REFUNDED`：已结算项完成全额退款；部分退款以独立金额与流水记录。

Provider 成本另用 `unknown`、`pending_reconciliation`、`confirmed`、`disputed` 等状态，不作为上述用户账单状态。Run 汇总允许部分结算、部分待核实，不用一个“任务失败”覆盖所有子项。

### 事务、幂等与恢复

- 冻结、结算、释放和退款在数据库事务内锁定钱包行或使用等价并发控制，保证可用和冻结金额均不为负。结算扣除实际消费并释放该项多余冻结，累计退款不得超过已结算金额，累计消费不得超过确认上限。
- 服务端从认证身份确定付款人；普通用户不能在请求中指定他人钱包、价格或绕过收费。内部测试额度也通过流水发放。
- 以持久化的 Run、节点、逻辑子调用身份建立收费项唯一约束，每个财务动作另有唯一 `idempotencyKey`。队列尝试次数、Worker 重启和回调 event ID 不得把同一调用变成新收费项。
- 任务、报价、冻结及待投递记录须原子写入；队列投递使用 outbox 或等价可恢复机制。投递失败可重投同一任务，不能再次冻结；未冻结成功不得发送 Provider POST。
- Provider 请求前持久化发送意图；已知任务 ID 优先查询或恢复归档。结果未知时不自动重发创建请求或切换 API，不把“本地取消”当作远程任务已经终止。
- 对待核实项设置截止时间、后台处理队列和用户可见状态。实施时按各 Provider 合同确定查询与人工处理时限；超时升级处理，不无限静默冻结，不依靠超时猜测成本为零或自动重发。经核实或管理员按既定规则决定释放时，转入 `RELEASED` 并记录依据；释放或退款后收到迟到结果只更新执行证据及上游成本，不自动重新冻结或补扣用户。

## API 与页面执行清单

### 管理员 API

以下是待实现接口草案，沿用当前 `/v1/admin` 权限边界；路由定稿时同步 OpenAPI、前端和测试，不代表接口已存在：

- `POST /v1/admin/model-marketplace/sync`：刷新指定连接的候选目录，返回新增、变化、缺失与待核实状态，不修改平台模型售价；
- `GET/POST /v1/admin/model-marketplace/models`：查询平台模型、从候选目录选中导入或手动新建草稿；
- `PATCH /v1/admin/model-marketplace/models/:id`：修改展示信息、规格、排序及人工发布状态；
- `GET/POST /v1/admin/model-marketplace/models/:id/bindings`：维护调用绑定版本和验证结果，显式启用兼容绑定；验证动作默认只做配置检查，真实生成单独授权；
- `GET/POST /v1/admin/pricing-versions`：创建和查看价格版本；
- `GET /v1/admin/wallets/:userId`、`POST /v1/admin/wallets/:userId/adjust`：查看余额和人工额度调整，要求原因、幂等键和审计；
- `GET /v1/admin/reconciliation`：查看执行结果、计量或 Provider 成本待核实项；处理和退款必须记录操作者、依据与关联流水。

### 普通用户 API

- `GET /v1/model-marketplace`：只返回已发布平台模型、能力、规格、平台价格和可用状态；
- `GET /v1/account/wallet`：余额、冻结余额和币种；
- `GET /v1/account/billing`：个人账单和任务关联信息；
- `POST /v1/billing/quotes`：按实际执行计划计算报价及最高额度，返回有有效期且绑定付款人、模型与参数的报价标识；
- `GET /v1/runs/:id/charge`：查看本人任务报价、子调用收费明细、冻结、结算及退款状态。

所有付费运行入口必须携带用户已接受的有效 `quoteId` 或等价服务端报价身份，服务端校验付款人、执行计划、模型绑定、参数及已确认最高额度后才冻结和投递。客户端自报金额无效；报价失效或需要重算时返回新报价等待用户确认，不得现场生成后直接扣款。既有 `/v1/models` 和旧节点引用需要明确迁移适配，不允许通过旧路由、原始 `modelAlias` 或 `credentialId` 绕过平台模型发布、价格和付款权限检查。

普通用户响应不返回 Provider Key、凭据 ID、内部成本明细或管理接口地址。管理员可以查看脱敏 usage、request/task ID、成本原币种及证据，不能回显 Key。

## 分阶段实施

P2-03 是总体待办编号；以下 P0/P1/P2 表示本功能内部实施优先级，不把未实现项标记为已完成。

### P0：独立模型身份与账务内核

- [ ] 确定结算币种、金额精度、计量与舍入规则，设计平台模型、绑定、价格版本和逐项账本。
- [ ] 完成可回滚的迁移与旧引用映射；保留资产、凭据、目录、任务和 usage，历史运行不追扣。
- [ ] 实现报价上限、冻结、结算、释放、退款及并发幂等；任务和冻结原子提交，队列发布可恢复。
- [ ] 普通生成、批量、工作流、优化、反推和重试统一接入，补齐重复提交、部分失败、未知结果与崩溃恢复回归。
- [ ] 独立记录用户结算及 Provider 成本状态，验证成本缺失不阻断已有计量的用户结算。

### P1：模型广场和管理员配置

- [ ] 实现候选目录同步、差异提示、选择导入及完整手动新建流程；同步不覆盖人工字段和售价。
- [ ] 在 `/admin/models` 完成绑定、能力验证、规格定价、审核和上下架；更换 API 保留平台模型及价格。
- [ ] 在主导航增加 `/models`，节点选择器读取同一目录，支持分类、搜索、详情和暂不可用提示。
- [ ] 增加内部额度发放、个人账单、管理员成本视图及提交前报价；退出与账户切换不泄漏上一账户数据。

### P1：Provider 对账

- [ ] 为每个 Provider 子调用保存任务身份、创建快照和脱敏最终响应，关联平台收费项。
- [ ] 对已确认合同解析明确金额及计量数据；成本未知、金额冲突和计量缺失分别处理。
- [ ] 验证具体 Sub2/New API 部署的查询权限和逐任务 usage 合同后接入对账，不猜测端点。
- [ ] 落实待核实处理时限、后台队列及告警；不能静默长期冻结或自动重发付费请求。

### P2：真实支付与商业化

- [ ] 内部测试余额、用户结算和成本对账稳定后再接真实支付。
- [ ] 增加充值订单、支付回调、支付退款与对账；支付回调只驱动幂等额度入账，不替代模型消费账本。
- [ ] 上线前完成备份恢复、签名验签、重复回调、退款及财务对账演练。

后置范围：多币种钱包、自动换汇、订阅套餐、优惠券、复杂积分体系及自动跨 API 故障转移，另行立项。

## 验收标准

### 功能验收

- 同步导入和手动新建均可完成草稿、绑定、定价、上架；同步失败、目录缺失和再次刷新不删除人工记录、不覆盖人工名称及售价。
- 更换或停用 API 后平台模型、画布引用、规格和价格保留；无有效绑定时新任务被阻止。同名模型不被自动合并，旧任务按原合同恢复。
- 主导航广场、管理员模型管理和节点选择器使用一致目录；权限在 API 生效，普通用户不能读取 Key、内部成本或他人账单。
- 余额不足、模型未发布、缺价格、绑定失效或报价超期时，付费 POST 次数为零；参数变化不能复用旧低价报价。
- 并发提交不超额冻结；重复提交、回调重放、队列重投和 Worker 重启不重复冻结、扣款、释放或退款。
- 多节点、批量、优化和反推逐真实子调用形成明细，部分成功、失败、取消、超时及归档恢复分别正确处理，不只按 Run 总状态结算。
- 已交付且计量明确时，上游费用缺失只使成本待对账；缺少用户计量时不按估算扣款。结果未知不自动重发或直接按零处理。
- 失效报价须重新确认后才冻结；已释放或退款项收到迟到结果不会自动补扣，处理依据与上游成本可追溯。
- 价格与绑定版本变更不影响历史账单和已受理任务，用户最终消费不超过确认上限，历史运行不追扣。

### 工程验收

本次仅更新 Markdown，以定向格式检查、相对链接检查、语义审查和 Git 差异检查作为文档验收，不宣称业务测试、构建或 Provider 验收通过：

```powershell
pnpm exec prettier --check docs/billing-and-model-marketplace-plan.md TODO-CONSOLIDATED.md
git diff --check -- docs/billing-and-model-marketplace-plan.md TODO-CONSOLIDATED.md
```

后续业务实施每个阶段运行相关单测及以下完整检查：

```powershell
pnpm lint
pnpm typecheck
pnpm test
pnpm build
```

涉及 Prisma 迁移时，另外在隔离数据库运行 `pnpm db:validate`、迁移及回滚回归、并发账务与崩溃恢复测试。PC Web 检查启动、管理员导入/手建/改价/换 API、用户选模型/报价/余额/账单的冒烟及控制台错误。真实 Provider 验收遵守 [`docs/live-provider-acceptance.md`](live-provider-acceptance.md) 的独立授权、次数限制、未知不重发与凭据脱敏规则。

## 迁移、回滚与未决问题

- 迁移前备份数据库、配置及必要密钥，确认可读和恢复路径。先建立独立模型、价格和账务表，再明确映射旧节点及项目默认引用；有歧义的模型标记待管理员绑定，不按名称猜测归属。
- 旧 Run、usage 和资产保留为历史记录；不能从 `estimatedCost` / `Run.cost` 构造历史消费，也不能把已有上游账单当作用户充值。切换前提交的任务按冻结合同继续完成且不追扣，切换后统一进入平台账务流程。
- 按接口、字段与迁移影响更新 OpenAPI、客户端及文档，验证所有旧入口都不能绕过新账务；不以保留兼容接口为理由维护长期双计费模式。
- 回滚优先停止新付费任务受理，保留已冻结任务的查询、结算与退款能力。已有真实流水后不能用旧数据库快照覆盖，不能降级到不识别账务或绑定快照的旧 Worker；通过兼容版本及补偿流水恢复。
- Provider 合同变化只产生待复核来源或绑定版本，不自动更新售价。具体结算币种、精度、计量/舍入合同、用量缺失处理时限及管理员审核规则须在对应编码阶段明确并测试。
- 当前 Helunox/Sub2 等部署的逐任务费用字段、管理 usage 权限和查询合同仍需外部验收；真实成本只能来自明确费用或可核对账单，不能由倍率反推。

## 本次文档检查点

- 基线：`codex/generate-to-new-node @ bcecfe4`；Node `v24.12.0`、pnpm `11.19.0`，项目依赖已存在。工作区已有其他任务的业务及文档修改，本次只拥有本方案与 P2-03 段落。
- 更新前后两份文档的定向 Prettier 检查及 `git diff --check` 通过，14 个本地相对链接目标均存在；已按上述边界完成语义复核。仅执行文档等价检查，未运行业务 lint/typecheck/test/build 或 Provider 验收。
- 下一步：独立实施 P0 的平台模型身份、价格与账务设计及迁移回归；本次未开始业务编码、数据库变更或真实付费调用。
- `TODO-CONSOLIDATED.md` 的 P2-03 及本文实施清单均保持未完成，文档通过不代表功能验收通过。
