# 通用资源 `@` 引用方案 TODO（待审核）

更新时间：2026-09-02
状态：方案草案，未修改实现

## 1. 目标与硬性结论

将“在提示词中输入 `@`，搜索并确认引入项目资源”的能力建设为**所有节点共用的基础能力**，不把它实现成视频节点的专属功能。

硬性结论：

- 所有带可编辑提示词、说明或内容输入的节点，都使用同一套资源提及编辑器。
- `@` 搜索范围是当前用户有权限访问的项目资源库，不是模型自行扫描电脑文件，也不是无确认地读取文件内容。
- 用户确认后才插入资源提及；执行时再根据不可变的资源 ID 和版本读取实际内容。
- 资源提及不能只保存为普通字符串。必须保存资源身份、媒体类型、版本、显示文本、引用顺序和可选语义绑定。
- 一个节点一次执行仍只选择一个模型；所有已确认提及由该模型统一处理。
- 模型不支持某种媒体或语义绑定时，必须在付费请求前给出可诊断错误，不能静默丢弃引用。
- 通用引用层负责表达和传递资源，不承诺所有模型都能处理所有媒体组合。

本文件是通用资源引用方案；视频首尾帧、角色图、音轨等专用语义继续由 [TODO-VIDEO-NODE.md](TODO-VIDEO-NODE.md) 细化。

## 2. 用户目标与示例

### 2.1 典型输入

用户在任意可执行节点中输入：

```text
萧炎 @图片1 @音频1 笑着
```

界面上显示文字和资源卡片，系统内部应表达为：

```text
角色：萧炎
外观参考：图片1
声音参考：音频1
动作描述：笑着
```

另一个通用示例：

```text
把 @产品图 放到 @场景图 中，保持 @品牌风格。
```

### 2.2 “引用”与“模型理解”的边界

- `@` 触发、搜索、确认和保存由画布应用完成。
- 应用将选中的资源解析为结构化引用，再交给 Provider 适配器。
- 如果只把 `@图片1` 当成文字发送，模型通常不会获得图片本身，也不会知道它与哪个角色或语义相关。
- 图片、音频、视频能否真正生效，取决于当前节点所选模型和供应商契约。

## 3. 当前基线与缺口

### 已有能力

- 项目已有资源库、资源类型和资产版本机制。
- `TextPromptEditor` 被快速编辑器和检查器复用，是替换为通用提及编辑器的自然入口：`apps/web/src/TextPromptEditor.tsx`。
- 运行快照已有 `inputs`、`sourceAssetId` 和节点快照，可继续用于冻结引用资源。
- `StoredAssetReferenceResolver` 已能按项目权限解析资产并加载指定版本：`apps/worker/src/asset-reference-resolver.ts`。
- 模型目录已有 `mediaTypes`、`capabilities`、`limitations` 字段，可作为引用能力校验的基础。

### 当前缺口

- 提示词当前只是 `nodeData.prompt: string`，没有富文本块、提及 ID 或资源绑定：`packages/domain/src/index.ts`。
- 当前编辑器是普通 `<textarea>`，没有 `@` 触发器、搜索弹层、资源卡片和确认动作。
- 运行提交只发送普通 `prompt` 和画布连线输入，没有独立的提示词提及快照。
- 画布端口连接表达的是节点间语义输入；它不能完整表达“某个资源在提示词的哪个位置、属于哪个角色”这类内联关系。
- 当前 Provider 对参考图、角色音频和多媒体组合的真实映射不完整，不能把 UI 做出来误认为供应商已经支持。

## 4. 范围与不包含内容

### 本方案包含

- 全节点共用的 `@` 搜索和资源提及编辑器。
- 项目资源索引、分类、权限过滤和确认引入。
- 图片、音频、视频、文本资源的统一引用表示。
- 角色/实体绑定、通用语义标签和节点专用角色映射。
- 运行快照中的资源版本冻结、恢复、重试、导入和导出。
- Provider 能力校验、错误诊断和 Mock 预览路径。

### 本方案不包含

- 不在本轮臆造任意供应商的字段名或能力。
- 不让一个节点同时调用多个模型来分别处理不同提及。
- 不把 `@` 自动变成画布连线；显式连线和内联提及是两种互补机制。
- 不默认扫描用户操作系统中所有文件；外部文件必须先上传或被明确授权进入项目资源库。
- 不要求所有节点都接受所有媒体类型；接受范围由节点媒体类型、模式和模型能力共同决定。

## 5. 统一用户流程

```text
输入 @
  -> 查询当前项目可访问资源索引
  -> 按名称/别名/类型/标签筛选
  -> 用户选择并确认
  -> 插入资源卡片（可删除、替换、查看详情）
  -> 可选：绑定到角色/实体和语义角色
  -> 保存节点文档
  -> 运行前冻结资源版本并校验模型能力
  -> Provider 将结构化引用映射为供应商请求
```

确认规则：

- `Enter`、鼠标点击或触屏点击确认；`Esc` 取消，不创建引用。
- 搜索结果默认显示“已引用”分组，以及图片、视频、音频、文本分类。
- 同一资源可以被多次引用；每次引用拥有独立 `mentionId`，删除其中一次不影响其他位置。
- 资源重命名不改变 `assetId`；界面显示名可随资源更新，但历史运行保留当时的快照标签。
- 选择资源不会立即把二进制内容发送给 Provider；只有运行并通过能力校验后才解析实际内容。

## 6. 节点通用性与能力矩阵

“所有节点可引用”指编辑和数据协议统一；“模型能处理”仍按能力矩阵判断。

| 节点媒体类型 | 常见可引用资源                         | 需要模型能力的部分                                           |
| ------------ | -------------------------------------- | ------------------------------------------------------------ |
| `text`       | 文本、图片、音频、视频                 | 标准文字模型通常只接受文本；多模态模型才可读取图片/音频/视频 |
| `image`      | 图片、角色图、风格图、文本             | 普通文生图接口通常不接受音频；参考图字段和数量由模型决定     |
| `audio`      | 文本、声音样本、音频                   | 声音克隆、音频编辑和图片条件输入需要专用模型                 |
| `video`      | 图片、视频、音频、首帧、尾帧、角色参考 | 首尾帧、多参考图、音频驱动、V2V 等必须逐项确认               |
| `source`     | 可作为其他节点的引用来源               | 来源节点本身不调用模型；其说明文本中的 `@` 默认只是元数据    |

节点面板应展示：

- 当前节点媒体类型和模式。
- 选中模型已声明支持的引用类型。
- 已引用数量、超限项和待确认能力。
- “可引用但当前模型不支持”的原因，而不是隐藏资源。

## 7. 数据模型（候选）

### 7.1 提示词文档

建议增加版本化的 `promptDocument`，以块结构保存文字和提及；保留现有 `prompt` 作为旧画布兼容字段或纯文本派生值。

```ts
type PromptDocument = {
  version: 1;
  blocks: Array<
    | { type: 'text'; text: string }
    | {
        type: 'mention';
        mentionId: string;
        assetId: string;
        label: string;
        mediaType: 'text' | 'image' | 'audio' | 'video';
        semanticRole?: string;
        entityName?: string;
      }
  >;
};
```

约束：

- `mentionId` 只用于文档内稳定定位，不能代替资产身份。
- `assetId` 必须指向项目中有权限访问的资产。
- 运行快照必须记录明确的 `assetVersion`；不能依赖“当前最新版本”。
- `label` 是显示信息，不得作为资源查找主键。
- 未识别的 `semanticRole` 要保留，不能导入时静默丢弃。

### 7.2 角色/实体绑定

通用层只保存中性的绑定信息，不直接写供应商字段：

```ts
type MentionBinding = {
  entityName?: string; // 例如“萧炎”
  semanticRole?: string; // 例如 characterAppearance、characterVoice、style
  scope?: 'local' | 'node' | 'scene';
};
```

推荐交互：

- 用户选中提及后，可以选择“绑定到角色/实体”。
- 当连续输入“萧炎 + 图片 + 音频”时，可提供推测绑定，但必须让用户确认。
- 不能只依据提及距离或资源文件名静默判断角色归属。
- `characterVoice` 与全局 `audioTrack` 必须是不同语义；不能因为都是音频就自动合并。

### 7.3 运行快照

建议在 `RunSnapshot` 增加独立的、已冻结的内联引用集合，或将其作为版本化输入文档的一部分：

```ts
type FrozenPromptMention = {
  mentionId: string;
  assetId: string;
  assetVersion: number;
  mediaType: 'text' | 'image' | 'audio' | 'video';
  label: string;
  blockOrder: number;
  binding?: MentionBinding;
};
```

这样可以同时保留：

- 提示词中的出现顺序。
- 资源的不可变版本。
- 角色/实体绑定。
- Provider 适配所需的媒体类型和诊断上下文。

内联引用不应通过拼接临时 URL 或只保存 `@文件名` 来实现。

## 8. 资源索引与搜索

### 8.1 索引范围

- 默认索引当前项目资源和用户明确授权的个人资源。
- 只索引元数据、缩略图和可搜索文本；原始二进制按需读取。
- 资源权限、归档状态和项目归属必须在搜索结果和执行时都再次校验。
- 操作系统文件只有在上传或明确导入后才进入索引。

### 8.2 搜索字段

第一阶段至少支持：

- 文件名和用户别名。
- 资源类型、MIME 类型、标签。
- 最近使用和当前节点已引用资源。

后续可加入：

- 图片 OCR/自动标签。
- 音频转录和说话人标签。
- 视频标题、时长、场景标签和关键帧描述。
- 语义向量搜索；必须和权限过滤同时生效。

### 8.3 索引一致性

- 上传、重命名、归档、恢复和新版本生成后更新索引。
- 索引不可用时，允许按资源列表做降级搜索，但要显示“搜索能力受限”。
- 搜索结果只返回可确认引入的资源；已失效或无权限资源不应生成幽灵提及。

## 9. Web 交互 TODO（PC 优先）

- [ ] 将普通 `TextPromptEditor` 抽象为通用 `ResourceMentionEditor`，供快速编辑器和检查器共用。
- [ ] 保留中文输入法组合态、粘贴、撤销/重做和光标位置，不因弹层打断输入。
- [ ] 输入 `@` 后打开搜索弹层，支持键盘上下选择、Enter 确认、Esc 取消。
- [ ] 结果按“已引用 / 图片 / 视频 / 音频 / 文本”分组并显示缩略图、名称、类型和版本提示。
- [ ] 已确认资源以不可误删文字的提及卡片显示，支持删除、替换、查看详情和绑定角色。
- [ ] 允许从资源库拖拽到提示词编辑器，并复用同一套确认和绑定流程。
- [ ] 能力不兼容时显示原因和建议切换的模型，不在界面上假装已经接通。
- [ ] 移动端复杂交互后置；桌面端先完成可用的弹层和键盘流程。

## 10. API、领域层与持久化 TODO

- [ ] 在领域 schema 中增加 `PromptDocument`、`PromptMention` 和版本校验。
- [ ] 明确 `prompt` 与 `promptDocument` 的兼容优先级，旧画布没有提及时保持原行为。
- [ ] 扩展项目资源查询接口，支持查询词、媒体类型、标签、分页和权限过滤；优先复用现有资源列表接口。
- [ ] 运行提交时把提示词文档和引用清单一并发送，不能只发送渲染后的纯文本。
- [ ] 在 API 边界冻结资产版本和资源权限，生成 `RunSnapshot` 后不再跟随资源最新版本漂移。
- [ ] 导入/导出保存提及块、资源 ID、版本、绑定和顺序；缺失资源要给出逐项诊断。
- [ ] 更新 OpenAPI、前端类型、持久化映射和审计字段。
- [ ] 保证引用数据不包含 API Key、临时签名细节或未授权的本地路径。

## 11. Provider 与模型能力 TODO

- [ ] 定义 Provider-neutral 的 `ResolvedMention`，画布层不写供应商字段名。
- [ ] 为模型能力矩阵增加可选的引用能力，例如媒体类型、角色语义、最大数量、是否支持同请求组合。
- [ ] 运行前按“节点媒体类型 + 节点模式 + 模型 + 提及媒体/语义”做完整预检。
- [ ] 能力未知时对真实付费 Provider 默认 fail-closed；可选允许 Mock 预览并明确标注为模拟结果。
- [ ] 保留提及顺序、重复引用、角色绑定和资源版本，交给 Provider 适配器决定具体字段。
- [ ] Provider 返回不支持或超限错误时，错误中包含节点、资源、媒体类型、语义角色、模型和请求 ID（不含密钥）。
- [ ] 不支持的资源不能静默转成普通文字，也不能只发送第一张并声称全部生效。
- [ ] 对文本、图片、音频、视频节点分别维护能力测试；不能用 Mock 能力证明真实模型能力。
- [ ] New API 的具体参考图、音频、角色和多媒体字段必须以正式契约或隔离实测为准。

## 12. 安全、隐私与资源控制

- [ ] 搜索结果严格按用户、项目和资产状态过滤；不能通过猜测 ID 读取其他项目资源。
- [ ] 用户确认前不向 Provider 上传或发送原始媒体。
- [ ] 运行前检查资源大小、MIME、版本、可访问性和模型输入上限。
- [ ] 日志只记录资源 ID、类型和脱敏诊断，不记录音频/图片内容、签名 URL 或凭据。
- [ ] 对转录、OCR 和文本资源设置内容边界，避免把引用文件中的指令无条件当成系统指令。
- [ ] 索引和缩略图更新失败要可重试并可观察，不阻塞已有资源的安全读取。
- [ ] 对批量 `@` 搜索、重复引入和大型多媒体提交设置限流与预算保护。

## 13. 兼容性与迁移

- [ ] 没有 `promptDocument` 的旧节点继续使用现有 `prompt` 字符串。
- [ ] 旧提示词中的普通 `@` 文本默认不自动解析为资源，避免文件名碰撞造成语义变化；用户重新选择后才建立提及。
- [ ] 导出文件保留提及块和资产版本；导入到缺少资源的项目时保留占位并报告迁移问题。
- [ ] 资源重命名、归档和新版本不会改变历史运行快照中的资源身份。
- [ ] 复制节点/复制画布时生成新的 `mentionId`，但保留或重新校验 `assetId` 权限。
- [ ] 撤销、重做、刷新、重试和 Worker 重启后，提及顺序和绑定保持一致。

## 14. 测试与验收 TODO

### 14.1 编辑器单元测试

- [ ] 输入 `@` 后弹层出现，按名称、别名、类型和标签搜索结果正确。
- [ ] 中文输入法组合态、粘贴、删除、撤销/重做不丢字或丢提及。
- [ ] Enter/点击确认插入提及；Esc 或点击外部取消不创建提及。
- [ ] 图片、音频、视频、文本提及显示正确卡片和媒体标签。
- [ ] 同一资源多次引用、删除中间提及和重新排序后文档结构稳定。
- [ ] 角色绑定必须显式确认；取消绑定不改变资源本身。

### 14.2 领域/API/Worker 测试

- [ ] `promptDocument` schema 拒绝不存在的资源 ID、错误媒体类型、非法版本和重复 `mentionId`。
- [ ] 运行快照保存每个提及的资源版本、顺序和绑定，重试时不漂移。
- [ ] 资产归档、权限变化、缺失版本和 MIME 不匹配均返回可诊断错误。
- [ ] 导入/导出后文档块、资源 ID、版本、标签和绑定完全一致。
- [ ] 旧画布只有 `prompt` 时仍能正常运行；普通 `@` 文本不被静默解释为资源。

### 14.3 Provider 契约测试

- [ ] Mock Provider 能回显所有已解析提及，覆盖四类节点媒体类型。
- [ ] 真实 Provider 支持某组合时，验证图片/音频/视频实际进入正确字段，而非只进入文字 prompt。
- [ ] Provider 不支持某媒体、语义角色、组合或数量上限时，在付费 POST 前失败。
- [ ] 验证一个节点仍只调用一个模型；多个提及不会触发隐式多模型调用。
- [ ] 失败、重试、Worker 重启和幂等恢复不会改变提及顺序或重复扣费。

### 14.4 E2E 验收样例

```text
图片节点：@产品图 + @品牌风格 -> 生成产品海报
音频节点：@台词文本 + @声音样本 -> 生成角色对白
视频节点：萧炎 @角色图 @角色音频 笑着
文字节点：根据 @采访音频 和 @资料文档 撰写摘要
```

每个样例都必须验证：搜索、确认、保存、刷新恢复、运行前能力提示、快照版本和最终 Provider 输入。

## 15. 分阶段执行顺序

### P0：通用编辑器与数据协议

- [ ] 建立 `PromptDocument`/`PromptMention` 版本模型，保留旧 `prompt` 兼容。
- [ ] 实现项目资源索引查询和 `@` 搜索弹层。
- [ ] 将所有节点的普通提示词输入替换为同一通用编辑器。
- [ ] 完成资源卡片、确认、删除、重复引用、撤销/重做和导入导出。
- [ ] Mock Provider 支持回显提及，形成端到端可验证闭环。

### P1：冻结、绑定与能力预检

- [ ] 增加角色/实体绑定和通用语义角色。
- [ ] 将提及资源版本写入运行快照，并接入现有资产解析器。
- [ ] 建立模型引用能力矩阵和提交前 fail-closed 校验。
- [ ] 完成文字、图片、音频、视频四类节点的兼容性提示和错误诊断。

### P2：真实 Provider 适配

- [ ] 按正式供应商契约实现图片参考、音频参考、角色绑定和多媒体组合映射。
- [ ] 完成视频首帧/尾帧/角色图/音轨等专用角色与通用提及的互操作。
- [ ] 增加隔离真实任务、计费幂等、取消、恢复和归档验证。
- [ ] 增加语义搜索、OCR、转录和批量引用等增强能力。

## 16. 待审核决策与风险

- [ ] **文档表示**：采用块结构 `promptDocument`，还是纯字符串加 `mentions` 区间；块结构更适合资源卡片和角色绑定。
- [ ] **提及绑定策略**：完全手动绑定，还是允许“按邻近角色名推测后确认”；不得静默推测。
- [ ] **索引范围**：仅当前项目，还是同时包含用户个人资源；个人资源必须经过权限和版本校验。
- [ ] **纯文本导出格式**：使用 `@资源名`、`[资源:资源名]`，还是只导出可读文本并附带资源清单。
- [ ] **能力未知策略**：真实 Provider 一律阻断，还是允许明确标记的 Mock/预览模式。
- [ ] **大型文件策略**：搜索阶段只读元数据，运行阶段按需转码、压缩或生成临时可访问版本。
- [ ] **文本资源安全**：引用文档中的指令是否默认作为内容而非控制指令，需要统一提示词边界规则。

主要风险：

- 只保存显示名称会在重命名或重名文件时引用错误；必须使用资产 ID 和版本。
- 只实现 UI 而没有 Provider 预检，会造成用户以为资源已生效但实际被丢弃。
- 把角色音频和全局音轨混用，会导致视频口型、旁白和背景音乐语义错位。
- 把内联提及强行转换成端口连线，会丢失文本位置、实体绑定和重复引用信息。

## 17. 完成定义

只有同时满足以下条件，通用资源 `@` 引用能力才可标记完成：

1. 文字、图片、音频、视频节点共用同一套搜索、确认和编辑交互。
2. 提及以结构化文档保存，包含资源 ID、媒体类型、顺序、版本和可选绑定。
3. 保存、刷新、复制、导入、导出、重试和 Worker 恢复不会改变引用语义。
4. 运行前会按节点、模式、模型和引用组合完成能力校验；不支持时不会发起付费请求。
5. 至少一个 Mock 闭环和每类真实 Provider 能力都有对应测试证据；Mock 不被当成真实模型证明。
6. 资源权限、MIME、大小、版本、日志脱敏和内容边界均有测试与文档证据。
7. 视频专用方案可以复用通用提及层，同时保留首帧、尾帧、角色、风格、音轨等明确语义。

本文件只描述待审核方案，不代表上述功能已经实现。
