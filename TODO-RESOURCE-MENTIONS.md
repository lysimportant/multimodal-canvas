# 通用资源 `@` 引用方案 TODO（P0/P1 已完成，P2 部分完成）

更新时间：2026-09-04
状态：P0/P1 已实现并通过验收；New API Chat Completions 多媒体映射已实现，生成专用端点仍待契约

## 1. 目标与硬性结论

将“在提示词中输入 `@`，搜索并确认引入项目资源”的能力建设为**所有节点共用的基础能力**，不把它实现成视频节点的专属功能。

硬性结论：

- 所有带可编辑提示词、说明或内容输入的节点，都使用同一套资源提及编辑器。
- `@` 搜索范围是当前用户有权限访问的项目资源库，不是模型自行扫描电脑文件，也不是无确认地读取文件内容。
- 用户确认后才插入资源提及；执行时再根据不可变的资源 ID 和版本读取实际内容。
- 资源提及不能只保存为普通字符串。必须保存资源身份、媒体类型、版本、显示文本、引用顺序和可选语义绑定。
- 一个节点一次执行仍只选择一个模型；所有已确认提及由该模型统一处理。
- 模型不支持某种媒体或语义绑定时，必须在真实 Provider 请求前给出可诊断错误，不能静默丢弃引用；这是输入契约保护，不是本地收费闸门。
- 通用引用层负责表达和传递资源，不承诺所有模型都能处理所有媒体组合。

### 1.0 当前费用边界

- 本项目当前不向用户收费、不做本地额度扣减或账单结算；真实请求产生的费用由上游 API 供应商计算和结算。
- 项目只保留供应商返回的 `usage`、状态、请求 ID 和可选费用字段，用于展示、审计、诊断和幂等追踪；价格未知、费用字段缺失或没有本地收费策略都不阻塞资源提及功能。
- 资源权限、MIME、大小、版本和模型能力校验仍必须在真实 Provider 请求前完成，以避免静默丢失输入；这类技术预检与收费无关。

### 1.1 本轮实现契约（已定稿）

- 节点同时存在 `promptDocument` 和旧 `prompt` 时，以 `promptDocument` 为唯一执行来源；`prompt` 只作为兼容的纯文本派生值。只有没有 `promptDocument` 时才读取旧 `prompt`。
- 保存和提交前会校验提及的资产归属、状态、MIME 和版本；任一提及不可访问、版本不存在、资产已归档或媒体类型不匹配，运行在真实 Provider 请求前失败，并返回 `RESOURCE_MENTION_*` 结构化诊断（包含节点、`mentionId`、`assetId`、媒体类型和请求 ID，不包含 URL、凭据或媒体内容）。
- 运行快照保存完整的冻结提及集合和每项资产版本；重试、队列恢复和 Worker 重启只使用快照版本，不重新解析“最新版本”。
- 导入遇到缺失或无权限资产时，保留原始 `assetId`、`mentionId`、标签、媒体类型、顺序和绑定，生成不可执行占位；导入报告逐项问题，不能静默删除或降级成普通文字。
- Provider 能力未知时，真实 Provider 请求默认阻断；Mock 只有在明确的预览路径中允许继续，并在结果和诊断中标记为模拟。该阻断用于避免输入被静默丢弃，不是价格或收费检查。
- 本轮已实现 New API Chat Completions 的结构化内容映射：文本资源还原为 UTF-8 `text` 块，图片、音频和视频资源分别映射为 `image_url`、`input_audio` 和 `video_url`（按当前网关运行时解析器使用字符串形状），保持块顺序与重复引用。图片/音频/视频生成专用端点当前仍只有纯文本主字段（视频另有已验证的首帧字段），无法表达的内联媒体会显式失败，不能静默丢弃；后续专用字段仍须以正式契约或隔离实测为准。

### 1.2 本轮验收结论

- [x] P0：通用编辑器、结构化提示词协议、项目资源搜索、确认/卡片、复制粘贴、导入导出和 Mock 闭环已完成。
- [x] P1：资源版本冻结、角色/语义绑定、权限与 MIME/大小校验、能力矩阵和提交前 fail-closed 预检已完成。
- [~] P2：New API Chat Completions 的文本、图片、音频、视频内容块映射已完成并有 Provider 单元测试；图片/音频/视频生成专用端点的参考图、角色和混合多媒体字段仍待正式供应商契约或隔离实测。

验收证据：`pnpm test`（API 328 passed、5 skipped；Web 290 passed；Worker 149 passed；Provider 127 passed；Domain 24 passed；Observability 15 passed；UI 3 passed）、`pnpm test:e2e`（20 passed）、`pnpm lint`、`pnpm typecheck`、`pnpm build`、相关 Prettier 检查和 `git diff --check`。本轮同步了 Provider 映射和收费边界，未把生成专用端点的未知字段标记为完成。

本文件是通用资源引用方案；视频首尾帧、角色图、音轨等专用语义继续由 [TODO-VIDEO-NODE.md](TODO-VIDEO-NODE.md) 细化。

## 2. 用户目标与示例

### 2.1 典型输入

用户在任意可执行节点中输入：

```text
萧炎 @图片1 @音频1 笑着
```

界面上显示文字和资源卡片，系统内部应表达为：

```text
角色：萧炎
外观参考：图片1
声音参考：音频1
动作描述：笑着
```

另一个通用示例：

```text
把 @产品图 放到 @场景图 中，保持 @品牌风格。
```

### 2.2 “引用”与“模型理解”的边界

- `@` 触发、搜索、确认和保存由画布应用完成。
- 应用将选中的资源解析为结构化引用，再交给 Provider 适配器。
- 如果只把 `@图片1` 当成文字发送，模型通常不会获得图片本身，也不会知道它与哪个角色或语义相关。
- 图片、音频、视频能否真正生效，取决于当前节点所选模型和供应商契约。

## 3. 当前基线与缺口

### 已有能力

- 项目已有资源库、资源类型和资产版本机制。
- `TextPromptEditor` 被快速编辑器和检查器复用，是替换为通用提及编辑器的自然入口：`apps/web/src/TextPromptEditor.tsx`。
- 运行快照已有 `inputs`、`sourceAssetId` 和节点快照，可继续用于冻结引用资源。
- `StoredAssetReferenceResolver` 已能按项目权限解析资产并加载指定版本：`apps/worker/src/asset-reference-resolver.ts`。
- 模型目录已有 `mediaTypes`、`capabilities`、`limitations` 字段，可作为引用能力校验的基础。

### 当前缺口

- 提示词当前只是 `nodeData.prompt: string`，没有富文本块、提及 ID 或资源绑定：`packages/domain/src/index.ts`。
- 当前编辑器是普通 `<textarea>`，没有 `@` 触发器、搜索弹层、资源卡片和确认动作。
- 运行提交只发送普通 `prompt` 和画布连线输入，没有独立的提示词提及快照。
- 画布端口连接表达的是节点间语义输入；它不能完整表达“某个资源在提示词的哪个位置、属于哪个角色”这类内联关系。
- 当前 Provider 对参考图、角色音频和多媒体组合的真实映射不完整，不能把 UI 做出来误认为供应商已经支持。

## 4. 范围与不包含内容

### 本方案包含

- 全节点共用的 `@` 搜索和资源提及编辑器。
- 项目资源索引、分类、权限过滤和确认引入。
- 图片、音频、视频、文本资源的统一引用表示。
- 角色/实体绑定、通用语义标签和节点专用角色映射。
- 运行快照中的资源版本冻结、恢复、重试、导入和导出。
- Provider 能力校验、错误诊断和 Mock 预览路径。

### 本方案不包含

- 不在本轮臆造任意供应商的字段名或能力。
- 不让一个节点同时调用多个模型来分别处理不同提及。
- 不把 `@` 自动变成画布连线；显式连线和内联提及是两种互补机制。
- 不默认扫描用户操作系统中所有文件；外部文件必须先上传或被明确授权进入项目资源库。
- 不要求所有节点都接受所有媒体类型；接受范围由节点媒体类型、模式和模型能力共同决定。

## 5. 统一用户流程

```text
输入 @
  -> 查询当前项目可访问资源索引
  -> 按名称/别名/类型/标签筛选
  -> 用户选择并确认
  -> 插入资源卡片（可删除、替换、查看详情）
  -> 可选：绑定到角色/实体和语义角色
  -> 保存节点文档
  -> 运行前冻结资源版本并校验模型能力
  -> Provider 将结构化引用映射为供应商请求
```

确认规则：

- `Enter`、鼠标点击或触屏点击确认；`Esc` 取消，不创建引用。
- 搜索结果默认显示“已引用”分组，以及图片、视频、音频、文本分类。
- 同一资源可以被多次引用；每次引用拥有独立 `mentionId`，删除其中一次不影响其他位置。
- 资源重命名不改变 `assetId`；界面显示名可随资源更新，但历史运行保留当时的快照标签。
- 选择资源不会立即把二进制内容发送给 Provider；只有运行并通过能力校验后才解析实际内容。

## 6. 节点通用性与能力矩阵

“所有节点可引用”指编辑和数据协议统一；“模型能处理”仍按能力矩阵判断。

| 节点媒体类型 | 常见可引用资源                         | 需要模型能力的部分                                           |
| ------------ | -------------------------------------- | ------------------------------------------------------------ |
| `text`       | 文本、图片、音频、视频                 | 标准文字模型通常只接受文本；多模态模型才可读取图片/音频/视频 |
| `image`      | 图片、角色图、风格图、文本             | 普通文生图接口通常不接受音频；参考图字段和数量由模型决定     |
| `audio`      | 文本、声音样本、音频                   | 声音克隆、音频编辑和图片条件输入需要专用模型                 |
| `video`      | 图片、视频、音频、首帧、尾帧、角色参考 | 首尾帧、多参考图、音频驱动、V2V 等必须逐项确认               |
| `source`     | 可作为其他节点的引用来源               | 来源节点本身不调用模型；其说明文本中的 `@` 默认只是元数据    |

节点面板应展示：

- 当前节点媒体类型和模式。
- 选中模型已声明支持的引用类型。
- 已引用数量、超限项和待确认能力。
- “可引用但当前模型不支持”的原因，而不是隐藏资源。

## 7. 数据模型（候选）

### 7.1 提示词文档

建议增加版本化的 `promptDocument`，以块结构保存文字和提及；保留现有 `prompt` 作为旧画布兼容字段或纯文本派生值。

```ts
type PromptDocument = {
  version: 1;
  blocks: Array<
    | { type: 'text'; text: string }
    | {
        type: 'mention';
        mentionId: string;
        assetId: string;
        label: string;
        mediaType: 'text' | 'image' | 'audio' | 'video';
        semanticRole?: string;
        entityName?: string;
      }
  >;
};
```

块结构还必须满足以下边界：文档至少包含一个块（可为空文本块），`mentionId` 在同一文档内唯一且长度受限，提及块的 `assetId`、`label`、`mediaType` 必须非空，块顺序即引用顺序。实现允许保留未知绑定字段用于前向兼容，但不得把未知媒体类型当成可执行引用。

约束：

- `mentionId` 只用于文档内稳定定位，不能代替资产身份。
- `assetId` 必须指向项目中有权限访问的资产。
- 运行快照必须记录明确的 `assetVersion`；不能依赖“当前最新版本”。
- `label` 是显示信息，不得作为资源查找主键。
- 未识别的 `semanticRole` 要保留，不能导入时静默丢弃。

### 7.2 角色/实体绑定

通用层只保存中性的绑定信息，不直接写供应商字段：

```ts
type MentionBinding = {
  entityName?: string; // 例如“萧炎”
  semanticRole?: string; // 例如 characterAppearance、characterVoice、style
  scope?: 'local' | 'node' | 'scene';
};
```

推荐交互：

- 用户选中提及后，可以选择“绑定到角色/实体”。
- 当连续输入“萧炎 + 图片 + 音频”时，可提供推测绑定，但必须让用户确认。
- 不能只依据提及距离或资源文件名静默判断角色归属。
- `characterVoice` 与全局 `audioTrack` 必须是不同语义；不能因为都是音频就自动合并。

### 7.3 运行快照

建议在 `RunSnapshot` 增加独立的、已冻结的内联引用集合，或将其作为版本化输入文档的一部分：

```ts
type FrozenPromptMention = {
  mentionId: string;
  assetId: string;
  assetVersion: number;
  mediaType: 'text' | 'image' | 'audio' | 'video';
  label: string;
  blockOrder: number;
  binding?: MentionBinding;
};
```

快照中的冻结集合按 `blockOrder` 升序保存；同一 `assetId` 的重复提及仍分别冻结。冻结失败使用统一错误对象：`code`、`message`、`requestId`、`nodeId`、`mentionId`、`assetId`、`mediaType`、`reason`，其中 `reason` 至少区分 `not_found`、`forbidden`、`archived`、`version_missing`、`mime_mismatch` 和 `size_exceeded`。

这样可以同时保留：

- 提示词中的出现顺序。
- 资源的不可变版本。
- 角色/实体绑定。
- Provider 适配所需的媒体类型和诊断上下文。

内联引用不应通过拼接临时 URL 或只保存 `@文件名` 来实现。

## 8. 资源索引与搜索

### 8.1 索引范围

- 默认索引当前项目资源和用户明确授权的个人资源。
- 只索引元数据、缩略图和可搜索文本；原始二进制按需读取。
- 资源权限、归档状态和项目归属必须在搜索结果和执行时都再次校验。
- 操作系统文件只有在上传或明确导入后才进入索引。

### 8.2 搜索字段

第一阶段至少支持：

- 文件名和用户别名。
- 资源类型、MIME 类型、标签。
- 最近使用和当前节点已引用资源。

后续可加入：

- 图片 OCR/自动标签。
- 音频转录和说话人标签。
- 视频标题、时长、场景标签和关键帧描述。
- 语义向量搜索；必须和权限过滤同时生效。

### 8.3 索引一致性

- 上传、重命名、归档、恢复和新版本生成后更新索引。
- 索引不可用时，允许按资源列表做降级搜索，但要显示“搜索能力受限”。
- 搜索结果只返回可确认引入的资源；已失效或无权限资源不应生成幽灵提及。

## 9. Web 交互 TODO（PC 优先）

- [x] 将普通 `TextPromptEditor` 抽象为通用 `ResourceMentionEditor`，供快速编辑器和检查器共用。
- [x] 保留中文输入法组合态、粘贴、撤销/重做和光标位置，不因弹层打断输入。
- [x] 输入 `@` 后打开搜索弹层，支持键盘上下选择、Enter 确认、Esc 取消。
- [x] 结果按“已引用 / 图片 / 视频 / 音频 / 文本”分组并显示缩略图、名称、类型和版本提示。
- [x] 已确认资源以不可误删文字的提及卡片显示，支持删除、替换、查看详情和绑定角色。
- [x] 允许从资源库拖拽到提示词编辑器，并复用同一套确认和绑定流程。
- [x] 能力不兼容时显示原因和建议切换的模型，不在界面上假装已经接通。
- [x] 移动端复杂交互后置；桌面端先完成可用的弹层和键盘流程。

## 10. API、领域层与持久化 TODO

- [x] 在领域 schema 中增加 `PromptDocument`、`PromptMention` 和版本校验。
- [x] 明确 `prompt` 与 `promptDocument` 的兼容优先级，旧画布没有提及时保持原行为。
- [x] 扩展项目资源查询接口，支持查询词、媒体类型、标签、分页和权限过滤；优先复用现有资源列表接口。
- [x] 运行提交时把提示词文档和引用清单一并发送，不能只发送渲染后的纯文本。
- [x] 在 API 边界冻结资产版本和资源权限，生成 `RunSnapshot` 后不再跟随资源最新版本漂移。
- [x] 导入/导出保存提及块、资源 ID、版本、绑定和顺序；缺失资源要给出逐项诊断。
- [x] 更新 OpenAPI、前端类型、持久化映射和审计字段。
- [x] 保证引用数据不包含 API Key、临时签名细节或未授权的本地路径。
- [x] 导入报告采用 `{ issues: Array<{ code, mentionId, assetId, message }> }`，占位提及保留原始身份但禁止提交执行。

## 11. Provider 与模型能力 TODO

- [x] 定义 Provider-neutral 的 `ResolvedMention`，画布层不写供应商字段名。
- [x] 为模型能力矩阵增加可选的引用能力，例如媒体类型、角色语义、最大数量、是否支持同请求组合。
- [x] 运行前按“节点媒体类型 + 节点模式 + 模型 + 提及媒体/语义”做完整预检。
- [x] 能力未知时对真实 Provider 请求默认 fail-closed；可选允许 Mock 预览并明确标注为模拟结果，不因价格或本地收费策略阻断。
- [x] 保留提及顺序、重复引用、角色绑定和资源版本，交给 Provider 适配器决定具体字段。
- [x] Provider 返回不支持或超限错误时，错误中包含节点、资源、媒体类型、语义角色、模型和请求 ID（不含密钥）。
- [x] 不支持的资源不能静默转成普通文字，也不能只发送第一张并声称全部生效。
- [x] 对文本、图片、音频、视频节点分别维护能力测试；不能用 Mock 能力证明真实模型能力。
- [~] New API Chat Completions 的 `text`、`image_url`、`input_audio`、`video_url` 映射已按网关运行时契约实现并测试；生成专用端点的具体参考图、音频、角色和混合字段仍必须以正式契约或隔离实测为准。

能力矩阵最小字段为 `mediaTypes`、`mentionMediaTypes`、`semanticRoles`、`maxMentions`、`supportsMixedMentions`、`modes`；字段缺失或无法判断时视为未知。预检必须输出逐项诊断，并在真实 Provider 下 fail-closed。

## 12. 安全、隐私与资源控制

- [x] 搜索结果严格按用户、项目和资产状态过滤；不能通过猜测 ID 读取其他项目资源。
- [x] 用户确认前不向 Provider 上传或发送原始媒体。
- [x] 运行前检查资源大小、MIME、版本、可访问性和模型输入上限。
- [x] 日志只记录资源 ID、类型和脱敏诊断，不记录音频/图片内容、签名 URL 或凭据。
- [x] 对转录、OCR 和文本资源设置内容边界，避免把引用文件中的指令无条件当成系统指令。
- [x] 索引和缩略图更新失败要可重试并可观察，不阻塞已有资源的安全读取。
- [x] 对批量 `@` 搜索、重复引入和大型多媒体提交设置限流与运行资源保护；这些限制不用于本地收费或额度扣减。

实现说明：当前资源搜索直接读取受权限约束的资源存储，没有独立异步索引；元数据/缩略图提取失败会降级并记录可观测告警，重新加载或重试上传即可恢复。文本资源始终作为普通用户内容传递，不会提升为 system 指令；API 通用限流与运行资源策略覆盖批量搜索和大型提交，不承担收费或额度结算。

## 13. 兼容性与迁移

- [x] 没有 `promptDocument` 的旧节点继续使用现有 `prompt` 字符串。
- [x] 旧提示词中的普通 `@` 文本默认不自动解析为资源，避免文件名碰撞造成语义变化；用户重新选择后才建立提及。
- [x] 导出文件保留提及块和资产版本；导入到缺少资源的项目时保留占位并报告迁移问题。
- [x] 资源重命名、归档和新版本不会改变历史运行快照中的资源身份。
- [x] 复制节点/复制画布时生成新的 `mentionId`，但保留或重新校验 `assetId` 权限。
- [x] 撤销、重做、刷新、重试和 Worker 重启后，提及顺序和绑定保持一致。

## 14. 测试与验收 TODO

### 14.1 编辑器单元测试

- [x] 输入 `@` 后弹层出现，按名称、别名、类型和标签搜索结果正确。
- [x] 中文输入法组合态、粘贴、删除、撤销/重做不丢字或丢提及。
- [x] Enter/点击确认插入提及；Esc 或点击外部取消不创建提及。
- [x] 图片、音频、视频、文本提及显示正确卡片和媒体标签。
- [x] 同一资源多次引用、删除中间提及和重新排序后文档结构稳定。
- [x] 角色绑定必须显式确认；取消绑定不改变资源本身。

### 14.2 领域/API/Worker 测试

- [x] `promptDocument` schema 与 API 边界拒绝不存在的资源 ID、错误媒体类型、非法版本和重复 `mentionId`。
- [x] 运行快照保存每个提及的资源版本、顺序和绑定，重试时不漂移。
- [x] 资产归档、权限变化、缺失版本和 MIME 不匹配均返回可诊断错误。
- [x] 导入/导出后文档块、资源 ID、版本、标签和绑定完全一致。
- [x] 旧画布只有 `prompt` 时仍能正常运行；普通 `@` 文本不被静默解释为资源。

### 14.3 Provider 契约测试

- [x] Mock Provider 能回显所有已解析提及，覆盖四类节点媒体类型。
- [~] 真实 New API Chat Completions 已验证图片/音频/视频进入对应内容块，而非只进入文字 prompt；图片/音频/视频生成专用端点的参考字段仍待契约确认。
- [x] Provider 不支持某媒体、语义角色、组合或数量上限时，在真实 Provider POST 前失败。
- [x] 验证一个节点仍只调用一个模型；多个提及不会触发隐式多模型调用。
- [x] 失败、重试、Worker 重启和幂等恢复不会改变提及顺序或重复创建 Provider 请求；供应商返回的 `usage` 仅作为可观察结果保存。

### 14.4 E2E 验收样例

```text
图片节点：@产品图 + @品牌风格 -> 生成产品海报
音频节点：@台词文本 + @声音样本 -> 生成角色对白
视频节点：萧炎 @角色图 @角色音频 笑着
文字节点：根据 @采访音频 和 @资料文档 撰写摘要
```

每个样例都必须验证：搜索、确认、保存、刷新恢复、运行前能力提示、快照版本和最终 Provider 输入。

验收说明：Web/API 单元测试覆盖内联 `@` 搜索、确认、刷新恢复、四类媒体节点、快照版本和 Mock Provider 回显；Provider 单元测试覆盖 Chat Completions 的四类内容块和生成专用端点的显式不支持错误；E2E 已覆盖四类节点、资源上传、画布连接、刷新和结果预览。真实供应商是否实际理解媒体仍需隔离请求结果或供应商回显证据。

## 15. 分阶段执行顺序

### P0：通用编辑器与数据协议

- [x] 建立 `PromptDocument`/`PromptMention` 版本模型，保留旧 `prompt` 兼容。
- [x] 实现项目资源索引查询和 `@` 搜索弹层。
- [x] 将所有节点的普通提示词输入替换为同一通用编辑器。
- [x] 完成资源卡片、确认、删除、重复引用、撤销/重做和导入导出。
- [x] Mock Provider 支持回显提及，形成端到端可验证闭环。

### P1：冻结、绑定与能力预检

- [x] 增加角色/实体绑定和通用语义角色。
- [x] 将提及资源版本写入运行快照，并接入现有资产解析器。
- [x] 建立模型引用能力矩阵和提交前 fail-closed 校验。
- [x] 完成文字、图片、音频、视频四类节点的兼容性提示和错误诊断。

### P2：真实 Provider 适配

- [ ] 按正式供应商契约实现图片参考、音频参考、角色绑定和多媒体组合映射。
- [ ] 完成视频首帧/尾帧/角色图/音轨等专用角色与通用提及的互操作。
- [ ] 增加隔离真实任务、请求幂等、取消、恢复和归档验证，并记录供应商返回的 `usage`/状态；不引入本地收费或额度扣减。
- [ ] 增加语义搜索、OCR、转录和批量引用等增强能力。

## 16. 待审核决策与风险

- [x] **文档表示**：采用块结构 `promptDocument`；旧节点保留 `prompt`，有文档时文档优先。
- [x] **提及绑定策略**：绑定必须显式确认；可以提供推测建议，但不能静默建立绑定。
- [x] **索引范围**：默认当前项目，可包含用户明确授权的个人资源；搜索和执行都再次校验权限。
- [x] **纯文本导出格式**：结构化导出保留块和资源清单；兼容纯文本使用 `@` 加标签，不能把纯文本重新导入为已确认提及。
- [x] **能力未知策略**：真实 Provider 请求一律阻断；仅明确标记的 Mock/预览模式允许继续。阻断原因是能力契约未知，不是价格或收费。
- [x] **大型文件策略**：搜索只读元数据和缩略图，运行阶段按冻结版本读取并执行大小/MIME 上限检查。
- [x] **文本资源安全**：文本资源作为内容数据传递，不提升为系统或控制指令；必要的边界标记由 Provider 适配器负责。

主要风险：

- 只保存显示名称会在重命名或重名文件时引用错误；必须使用资产 ID 和版本。
- 只实现 UI 而没有 Provider 预检，会造成用户以为资源已生效但实际被丢弃。
- 把角色音频和全局音轨混用，会导致视频口型、旁白和背景音乐语义错位。
- 把内联提及强行转换成端口连线，会丢失文本位置、实体绑定和重复引用信息。

## 17. 完成定义

完整方案只有同时满足以下条件才可标记完成；本轮只对 P0/P1 做验收：

1. 文字、图片、音频、视频节点共用同一套搜索、确认和编辑交互。
2. 提及以结构化文档保存，包含资源 ID、媒体类型、顺序、版本和可选绑定。
3. 保存、刷新、复制、导入、导出、重试和 Worker 恢复不会改变引用语义。
4. 运行前会按节点、模式、模型和引用组合完成能力校验；不支持时不会发起不符合契约的 Provider 请求。
5. 至少一个 Mock 闭环和已确认的真实 Provider 映射都有对应测试证据；Mock 不被当成真实模型证明，生成专用端点的未知字段继续保持显式不支持。
6. 资源权限、MIME、大小、版本、日志脱敏和内容边界均有测试与文档证据。
7. 视频专用方案可以复用通用提及层，同时保留首帧、尾帧、角色、风格、音轨等明确语义。

验收结论：条件 1-4、6-7 的 P0/P1 部分已满足；条件 5 的真实 Provider 部分尚未满足。因此当前状态可标记为“P0/P1 已完成、P2 待正式契约”，不能宣称完整方案已经完成。
