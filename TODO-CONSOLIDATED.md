# 未完成任务汇总

更新时间：2026-09-05

本文件只保留尚未完成、部分完成、外部依赖或明确暂缓的事项。已经完成的实现、测试、
验收记录和历史检查点均已删除，不在本文件重复维护。

状态说明：

- `[~]` 已有部分实现或部分证据，仍需完整验收。
- `[!]` 依赖外部平台、部署权限、正式契约或具体配置。
- `[>]` 明确暂缓，当前不进入实施范围。

## P0：生产基础设施与恢复

### `[~]` P0-REAL-INFRA-01 真实供应商任务跨进程恢复

- 需要对真实供应商异步视频任务执行 Worker 崩溃后的接管、继续查询、下载和归档验证。
- 当前已有真实视频创建、轮询、下载和归档成功证据，但没有该供应商任务的崩溃接管证据。
- 验收条件：隔离数据库、Redis、对象存储和真实供应商任务均使用独立 namespace；任务身份不丢失、不重复创建、不重复记录 usage。
- 前置条件：允许一次真实视频任务故障演练，并允许使用对应的供应商任务查询接口。

### `[!]` P0-PROD-STARTUP-02 实际生产入口与安全边界

- 需要验证实际部署的域名证书、反向代理、入口路由、真实客户端地址传递、HSTS、告警接收端和配置回滚。
- 需要在实际生产网络边界验证 Redis 全局限流、TLS、S3、API/Worker 启动和故障关闭行为。
- 前置条件：生产域名、证书、反向代理配置、告警地址、回滚版本和部署权限。
- 本地 HTTPS 代理和隔离 Docker 验收不能替代实际生产部署验收。

### `[~]` P0-MEDIA-OPS-03 生产媒体处理与外部可观测性

- 需要验证真实业务 S3 桶的上传、下载、派生对象、权限边界、回滚和孤立对象处理。
- 需要验证生产 OTLP/Sentry 接收端的投递、脱敏、容量、超时、503、重定向和故障隔离。
- 前置条件：生产 S3 bucket/prefix/IAM、FFmpeg 运行环境、OTLP/Sentry 地址、告警和回滚权限。

## P1：Provider、媒体覆盖与多 Key

### `[~]` P1-VIDEO-CONTRACT-04 真实视频完整契约

- 需要取得并验证供应商正式的取消接口、Webhook 原始 body 签名与编码、时间戳重放保护、幂等范围、重复计费规则和安全重试规则。
- 需要完成真实供应商视频任务的取消、回调、重放和异常恢复演练。
- 当前仅验证了 sub2 的 `legacy-v1` 创建、查询、内容下载和归档；不能据此推断取消、Webhook 或重试契约。
- 前置条件：供应商正式契约、回调地址/签名配置、测试任务权限和明确的故障演练授权。

### `[~]` P1-MEDIA-COVERAGE-05 音频、参考输入与供应商扩展

- 需要明确真实 TTS 模型、`voice`、格式和语速组合，并完成一次真实音频生成及 Worker/Prisma/对象存储归档。
- 需要取得多参考图、负面提示、尾帧、音轨、角色绑定、蒙版及其它扩展字段的正式契约，并逐项完成授权实测。
- 不得使用未明确授权的音频模型、voice 或供应商扩展字段进行猜测性请求。
- 前置条件：TTS 模型和 voice、正式字段文档、测试凭据及真实请求授权。

### `[~]` P1-MULTIKEY-ROTATION-06 供应商侧多 Key 轮换

- 需要验证供应商后台实际撤销、轮换和失效行为。
- 需要验证同一真实 DAG 使用多把供应商 Key 时的冻结、幂等、失败恢复和历史任务行为。
- 需要在实际业务部署中演练轮换、兼容窗口、暂停写入、回滚和旧任务恢复。
- 前置条件：供应商后台权限、两把以上可区分的测试 Key、部署配置位置和回滚授权。

### `[~]` P1-PROVIDER-ROLES-07 真实角色与扩展字段映射

- 需要针对 `negativePrompt`、`lastFrame`、`audioTrack`、`style`、`character`、`mask` 和多参考图取得正式字段契约。
- 需要用目标部署完成每个已确认字段的真实请求和结果验证，确认字段确实被供应商读取而非被忽略。
- 未确认的角色和参数必须继续在请求前显式拒绝，不能静默降级。
- 前置条件：目标部署正式 API 文档、模型适用范围、样例响应和测试授权。

### `[!]` P1-PRODUCTION-09 生产环境完整验收

- 生产 S3 上传/下载、Redis/BullMQ 队列接管、跨实例恢复、TLS/反向代理和全局限流。
- 生产 OTLP/Sentry 投递、Provider 回调、媒体归档、凭据轮换和故障演练。
- 迁移兼容窗口、回滚步骤、对象存储前缀、队列 namespace、数据备份和恢复记录。
- 前置条件：生产部署地址、数据库/Redis/S3 连接配置、证书、IAM、监控接收端、回滚版本和操作授权。
- 本地 Docker、Mock、历史测试和隔离测试不能替代生产证据。

## P2：明确暂缓

### `[>]` P2-SSO-10 外部身份系统

- 等待 OAuth 2.0/OIDC 或兼容 JWT 正式契约后接入。

### `[>]` P2-USAGE-11 上游 usage 展示与对账增强

- 仅在产品优先级允许时补充 usage 展示、费用字段展示和可选对账；本项目不实现本地收费、额度扣减或账单结算。

### `[>]` P2-RESOURCE-12 资源提及增强能力

- 语义搜索、OCR、音频转录、视频关键帧描述、批量引用、索引异步化和大规模性能验证。

### `[>]` P2-DESKTOP-13 桌面端封装

- Web MVP 和生产链路稳定后再接入 Tauri 桌面壳。

### `[>]` P2-COLLAB-14 协作与后置编辑能力

- 多人协作、插件市场、代码节点、完整视频剪辑器和复杂移动端画布。

## 统一阻塞条件

- 真实生产验收：需要生产域名、证书、入口、S3、Redis、遥测和回滚配置及操作权限。
- 真实音频验收：需要明确 TTS 模型、voice、格式、测试 Key 和一次请求授权。
- Provider 高级契约：需要正式文档、回调签名配置、幂等/取消规则和故障演练授权。
- 供应商轮换验收：需要供应商后台权限和至少两把可区分的测试 Key。
- 真实视频跨进程恢复：需要允许一次真实任务中断/接管演练，不能用合成任务替代。
