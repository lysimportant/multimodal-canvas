# 待完成任务汇总

更新时间：2026-09-14
整理基线：`main @ 43c69b6`
来源：[视频节点任务历史版本](https://github.com/lysimportant/multimodal-canvas/blob/c750763925a2fd63988c8a84402dc982a6ec460a/TODO-VIDEO-NODE.md)、[本地任务历史版本](https://github.com/lysimportant/multimodal-canvas/blob/c750763925a2fd63988c8a84402dc982a6ec460a/TODO-LOCAL.md)。

本文件仅汇总两份来源文档尚需完成、受阻和后置的任务，不代表本轮已审查实现或重新执行验收。重复事项已合并；来源文档前部旧缺口与后部完成记录冲突时，以较新的完成记录为准。两份来源文档已从根目录删除，历史内容可通过上方固定版本链接或 `git show c750763:TODO-LOCAL.md`、`git show c750763:TODO-VIDEO-NODE.md` 查阅。

状态：`[ ]` 待完成；`[~]` 已有部分实现或证据，仍需补齐验收。PC Web 核心流程优先，复杂移动端适配后置。下列编号为本汇总文档内的任务标识。

## P1：供应商合同与真实验收

### [~] P1-01 视频任务状态、取消、Webhook 与幂等合同

来源：TODO-LOCAL 的 `P1-ADAPTER-CONTRACT-04`、本地执行检查点。

- 取得供应商正式的状态、取消接口、Webhook 原始 body 签名及编码、时间窗口、重放保护、幂等范围、重复计费和安全重试合同。
- 按确认合同补齐取消、状态、签名、重放、幂等及创建结果不明的回归测试，并保存对应供应商验收证据。
- 现有本地轮询取消与原始 UTF-8 字节验签不能证明远程取消或供应商回调合同已成立；未知合同继续阻断扩展。

完成条件：每项合同都有可追溯依据、回归结果和所需的独立验收证据；结果不明的创建请求不重发。

### [~] P1-02 视频角色与扩展字段映射

来源：TODO-LOCAL 的 `P1-INPUT-MAPPING-07`、`P1-ISOLATED-AUDIO-05`；TODO-VIDEO-NODE 第 1、8、10、11 节。

- 按 Provider、模型、合同整理 `negativePrompt`、`lastFrame`、`audioTrack`、`style`、`character`、`mask`、多参考图与其它扩展输入的字段、适用范围和样例响应。
- MiniMax-H3、Wan3.0 与 Seedance 2.0/2.5 的官方首尾帧及多媒体参考已完成插件字段映射；Moon Wan3、当前 Seedance 三模型与精确小写 `minimax-h3` 的配套修复见 `next.md` 第 16 节。Wan3/Seedance 编辑、延长按已确认版本开放。仍需补齐其它拟接入模型的正式字段取证，以及各模型真实上游验收，不能把本地映射测试视为供应商验收。
- 为需要 URL 的视频参考配置可外部访问同一冻结对象的 `MC_S3_PROVIDER_ENDPOINT`，验收短期签名 URL 的外部 GET；Moon 所有参考素材均要求可访问 URL，Wan 视频还要求冻结版本时长。线上网关需更新 Moon 1.1.0 及支持 H3 精确大小写共存的宿主；官方 Seedance 自动时长要求 Doubao 1.0.3 插件。
- 共享 Wan 模型 ID 尚不能在 Canvas 区分百炼与 Moon 的全部参数差异；Moon 的总引用数量、参考视频合计时长及不支持负向提示/自动时长等限制由网关插件在预扣与供应商 POST 前明确拒绝。后续按渠道身份细分预检，不以 Moon 限制收窄百炼合同。
- 对已确认字段补齐序列化、响应解析、数量/组合边界和未知字段拒绝测试。现有 Grok 1.5 尾帧与参考图映射不重复列为待实现项。
- 明确各模型重复端口上限，取产品、模型、供应商限制的最小值；来源文档中的示例配额不能当成确认值。

完成条件：拟开放组合均有字段证据与测试，未确认角色、超限输入和不支持的组合在供应商创建 POST 前明确失败，不静默丢弃或降级；共享模型 ID 的渠道差异需另行完成 Canvas 预检。

### [~] P1-03 视频模式与输入组合的隔离真实验收

来源：TODO-VIDEO-NODE 第 4、9、12 节；TODO-LOCAL 的 `P1-ISOLATED-AUDIO-05`。

- 对单首帧、首尾帧、多参考图及后续确认的扩展组合，按 Provider、精确模型 ID、合同和输入组合分别取得请求授权并验收。
- 记录输入与输出、任务身份、查询/下载/归档结果以及供应商实际返回的 usage、请求 ID 和费用字段；未返回的费用保持未知。
- 复用已有文生视频跨进程恢复证据，不为文档汇总重复付费创建任务；Mock 和合同单测不作为多参考真实验收。

完成条件：每个拟宣称开放的组合均有独立脱敏报告、成功归档证据和请求次数记录。已有 `legacy-v1` + `grok-imagine-video-1.5（按次）` 文生视频恢复闭环不覆盖其它组合。

### [~] P1-04 音频生成组合扩展

来源：TODO-LOCAL 的 `P1-ISOLATED-AUDIO-05`。

- 确认拟新增的 TTS 模型、voice、格式、采样率和语速组合，补齐建连前校验、拒绝和结果解析测试。
- 对获授权的新组合完成真实生成、Worker 归档、Prisma 记录及隔离对象存储读回验证。
- 既有讯飞 `xiaoyan`、MP3、16 kHz、语速 50 的真实验收不重复执行，也不外推为其它组合已开放；视频中的音频参考合同归入 P1-02。

完成条件：新增组合有正式依据与独立验收证据；未确认的模型、voice、格式及扩展输入在请求前失败。

## P1：末帧派生与资产治理

### [~] P1-05 独立派生消费者与恢复机制

来源：TODO-VIDEO-NODE 第 5、6、10.1 节。

- 在现有视频归档后末帧提取主路径基础上，补齐独立 outbox 消费者与租约领取，明确任务生命周期及恢复行为。
- 验证跨进程接管、重复消费和失败恢复，沿用运行/源节点/动作构成的幂等身份。
- 保持派生失败不改变原视频成功状态，填充冲突保留资产且不覆盖已有内容。

完成条件：生命周期及持久化方案明确，重复消费和恢复测试通过，原视频成功状态与用户资产保持正确。

### [ ] P1-06 临时对象 TTL、来源映射与引用治理

来源：TODO-VIDEO-NODE 第 6、10、11 节。

- 补齐独立临时对象 TTL 和清理机制。
- 完善导入导出时派生资产与源视频、源节点的关系映射。
- 明确派生资产配额、保留周期、删除后的引用状态及画布提示。
- 明确是否调整新节点默认末帧动作；未确认前维持 `none`，不自动生成额外资产或节点。

完成条件：到期清理、关系往返、引用失效和配额边界有验证证据；删除和覆盖操作执行前另行确认。

### [~] P1-07 FFmpeg/ffprobe 部署与降级验收

来源：TODO-VIDEO-NODE 第 11 节；TODO-LOCAL 的执行准备和本地执行检查点。

- 明确目标运行环境的 FFmpeg/ffprobe 安装、版本与配置要求，并保留可复现测试配置。
- 验证正常末帧提取、依赖缺失、解码失败时的派生状态和用户提示。
- 复用已有本地媒体测试记录；本地安装或一次通过不能代表所有部署环境已验收。

完成条件：目标环境有启动与提取证据，依赖缺失或提取失败不污染原视频成功状态。

## P2：后置能力

### [ ] P2-01 视频编辑、视频延长与高级参考

来源：TODO-VIDEO-NODE 第 3、4、10 节。

- 独立确认 New API / 经典 Grok 等视频编辑、视频延长端点及合同后，分别实现模式、输入校验、Provider 映射和验收；不在 Grok 1.5 生成合同中猜字段。
- 取证后实施 `motionReference`、`keyframe`、mask 绑定和多镜头。关键帧内部时间单位统一为 `timeMs`，不支持的中间帧明确拒绝。
- 保持“一节点、一次运行一种模式”；用上一视频末帧生成新片段不等同于视频延长。

### [ ] P2-02 外部身份系统

来源：TODO-LOCAL 的 `P2-SSO-10`。

取得 OAuth 2.0/OIDC 或兼容 JWT 正式合同后接入，并完成认证流程与兼容性验收。

### [~] P2-03 平台计费、模型广场与 Provider 成本对账

来源：TODO-LOCAL 的 `P2-USAGE-11`；TODO-VIDEO-NODE 第 1.2 节。

按[平台计费与模型广场执行方案](docs/billing-and-model-marketplace-plan.md)和[实施检查点](docs/billing-implementation-checkpoint.md)继续验收。当前剩余事项如下，不把阶段实现或隔离测试当成上线完成：

- P1 优先恢复模型调用，执行顺序见[模型调用恢复计划](docs/model-call-recovery-plan.md)：先让已提交的 H3 两端修复生效并同步，再补齐两个 Wan 的媒体能力和费用预估，验证旧节点、多 Key 选择、费用确认及真实调用。保留人民币钱包与 New API 原价；本轮仅形成计划，不代表已部署或已授权删除计费模块。
- 补齐手工定价的可信输入 Token、各 Provider 按秒计量和具体 Sub2/New API 部署的逐任务 usage 权限/合同；手工 Token 报价仍阻止，按秒 `provider_usage` 缺计量时保持待核实，不能猜测字段收费。
- 落实 24 小时待核实事项的运营处理时限、人工处置与告警，验证真实成本对账；上游成本保留原币种，不以估算或自动换汇补齐事实。
- 线上 `CANVAS_BRIDGE_ENABLED` 已启用；8080 的 H3 已上架。图片提及仍被桥接目录的仅文字能力拦截，两端能力声明与媒体预估修复已通过本地回归，等待生产宿主更新授权及配套 Canvas 更新、目录同步核验；仅更新 Hailuo 插件不能生效。仍需真实费用预估/最终回执、生产同步写库容量、pending 运营恢复及各媒体调用合同验收。8080 已接入原广场价格，见[同步检查点](docs/newapi-pricing-sync-checkpoint.md)；线上管理授权、价格写回与真实付费验收待单独授权，后台定时同步后置。
- 补齐已发布 outbox 对应 Redis job 丢失后的安全重建入口，以及 `settlement_conflict` 核账、退款和事项关闭流程；恢复原 job 会继续尚未发送的授权下游节点，不能笼统承诺整个 DAG 零新增 POST。
- 在生产操作获授权后完成数据库、资产与密钥备份恢复演练及迁移上线验收；保留历史数据，历史运行不追扣，已有流水不能被旧备份覆盖。
- 内部账务验收稳定后再接真实支付、充值订单、回调和支付退款对账，并完成签名、重复通知和资金核对验收。

完成条件：上述验收取得明确证据后才关闭；人民币钱包计费、模型配置与 Provider 成本分别核对，缺少价格或报价仍阻止新付费提交，成本未知不能当作零，也不能阻断已有确定交付与计量的用户结算。

### [ ] P2-04 资源提及增强

来源：TODO-LOCAL 的 `P2-RESOURCE-12`。

补齐语义搜索、OCR、音频转录、视频关键帧描述、批量引用、索引异步化与大规模性能验证。

### [ ] P2-05 桌面端封装

来源：TODO-LOCAL 的 `P2-DESKTOP-13`。

Web MVP 稳定后接入 Tauri 桌面壳；桌面封装与 PC Web 核心流程优化是不同任务。

### [ ] P2-06 协作与后置编辑能力

来源：TODO-LOCAL 的 `P2-COLLAB-14`。

多人协作、插件市场、代码节点、完整视频剪辑器及复杂移动端画布。默认后置，复杂移动端工作待明确调整优先级。

## 执行与关闭条件

六项 PC Web 验收的本地缺口已补齐，最新证据见 [next.md 第 14 节](next.md#14-功能补齐与最终检查点)及 [验收记录第 7 节](docs/six-feature-acceptance.md#7-功能补齐后的验收)。仍需追加额度完成文字/视频成功取证，音频需可用的精确模型；不要重发已用完授权次数的创建请求。固定大画布已取得基线/当前样本，后续性能工作先拆分自动化等待与实际渲染耗时，再决定是否优化。

- 先完成合同取证，再开放字段映射与真实验收；末帧治理按生命周期、恢复机制、TTL/关系映射及部署验收推进；P2 能力单独立项。
- 保持 `source` / `generate` 和历史 `transform` 兼容，以及 `VideoInputSet`、`referenceImage`、冻结快照、输入顺序和未知能力 fail-closed 等既有约束，不将这些已完成事项重新列为开发任务。
- PostgreSQL、Redis、S3/MinIO、队列及凭据使用专用 namespace、bucket/prefix 和合成或获授权测试配置；执行前核验当前环境，不照搬历史端口状态。
- 真实请求按 Provider、精确模型 ID、合同、组合和次数取得授权；保留已知任务 ID，结果不明不重复发送可能计费的创建 POST，不记录真实密钥。
- 数据模型、迁移、依赖升级、删除或覆盖前说明影响与回滚；生产操作和破坏性操作另行确认。
- 关闭条目时记录命令、环境、脱敏报告及结果；启用的验收测试必须报告非空、零失败、零 skip/TODO。普通测试的设施跳过不算通过，历史证据不等于本轮或生产验收。

## 来源状态处理

TODO-VIDEO-NODE 第 2 节的“没有 VideoInputSet / referenceImage”“末帧尚未设计”等旧缺口，以及第 10.1 节部分“实施中”描述，与后面的完成清单冲突，本汇总不据此重新生成任务。第 11 节未勾选的结构冻结和未知能力拒绝属于持续约束；配额、默认末帧动作及部署待确认项已分别保留在 P1-02、P1-06、P1-07。

TODO-LOCAL 的 `P0-ISOLATED-RECOVERY-01` 已记有真实恢复验收，不再列入待办。两份文档没有一致指向的新增 P0 未完成任务；这不代表项目所有 P0 问题均已排除。
