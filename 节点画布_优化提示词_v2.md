# 节点画布优化提示词

> 复制本文给执行者即可开工。口径已按当前代码拍板，不要再问方向问题。
>
> 执行基线：`main` = `3d085fe`，代码基线为其父提交 `55aa2bf`。
> 这是提示词与执行计划，不是实现记录。未完成项不得标成完成。

---

## 0. 先看结论

| 顺序 | 条目 | 类型 | 页面？ | 为何这时做 |
| --- | --- | --- | --- | --- |
| 1 | **#8 图片生成成功但不回显** | **阻断 Bug** | 画布节点 | 生成主路径坏了，后面所有回显/描边都建立在错误状态上 |
| 2 | **#9 描边/轻阴影 + #13 侧栏拖入节点旧样式 + #7 悬浮头卡片 + #2 默认尺寸** | 画布视觉（可合并） | 画布节点 | 同一套 `AssetNode` 外壳；上传按钮和回显都依赖它 |
| 3 | **#3 Hover 打开参数 + #4 小参数多列 + #5 删视频自定义宽高 + #6 新节点默认模型/高强度** | 生成参数（可合并） | 画布节点编辑器 | 同在 `NodeQuickEditor`；可与批次 1 并行 |
| 4 | **#10 悬浮头上传 + 文本双击编辑** | 节点交互 | 画布节点 | 必须等悬浮头改完，避免和 #7 抢同一块 DOM |
| 5 | **#1 资源卡片删除按钮** | 产品补齐 | 画布左侧资源栏（不是独立页） | 上传已成功，只缺操作；可与批次 2 并行 |
| 6 | **#11 我的资源真实回显/预览 + #12 跨页记住项目并左下角返回** | 管理页（可合并） | **独立页面** `/resources` 等 | 不碰画布节点外壳，可与批次 2、3、4 并行 |

**唯一必须先修的 Bug：#8。**  
**样式不一致按 Bug 修：#13。**  
**布局缺陷接近 Bug：#4（360p/480p 仍一行一个）。**  
其余都是交互/视觉改动，不是功能损坏。

---

## 1. 多 Agent 怎么拆

原则：按**文件锁**拆，不按条目号拆。`App.tsx` 永远由主 Agent 独占接线。

```text
主 Agent
  ├─ 批次 0  #8 回显 Bug          可写 AssetPreview / 必要时 API
  ├─ 批次 1  #9 #13 #7 #2         可写 AssetNode、asset-node.css、canvas-utils
  │
  ├─ 并行 A  批次 2  #3 #4 #5 #6  可写 NodeQuickEditor*、CompactSelect、相关 CSS/测试
  ├─ 并行 B  批次 4  #1           可写 ResourcePanel*
  └─ 并行 C  批次 5  #11 #12      可写 ResourcePages、management.css、routes、导航

批次 3  #10 必须等批次 1 结束（都改 AssetNode 悬浮头）
```

| 角色 | 负责 | 可写文件 | 禁止 |
| --- | --- | --- | --- |
| 主 Agent | 批次 0、1、3 串行；所有 `App.tsx` 接线；最终验证/提交 | `App.tsx`、批次 0/1/3 节点文件 | 不要把参数面板、资源页、导航塞进同一次 diff |
| Agent A | 生成参数 | `NodeQuickEditor.tsx` / `.test.tsx` / `.css`、`CompactSelect.tsx`、`index.css` 里 compact-select / option 段、`node-generation-defaults` 相关测试 | 不改 `App.tsx`、`AssetNode.tsx` |
| Agent B | 资源栏删除 | `ResourcePanel.tsx`、`ResourcePanel.test.tsx` | 不改 `App.tsx`；删除 API 接线留给主 Agent |
| Agent C | 我的资源 + 返回项目 | `ResourcePages.tsx`、`management.css`、`routing/routes.ts`、`AppNavigation.tsx`、`AccountMenu.tsx`、相关测试 | 不改画布节点、不改 `App.tsx` 保存逻辑 |

`index.css` 由主 Agent 独占；Agent A 将参数样式放在所属组件 CSS。共享文件按串行交接修改。

---

## 2. 全局约束

- PC Web 工作台优先；不为手机单独改布局，除非现有响应式测试被破坏。
- 只做增量。不改数据库结构，不引入新依赖，不重构无关模块。
- 节点外框尺寸**只允许用户拖拽**改变；回显内容不得撑大节点。继续用 `.flow-node-preview { contain: layout paint; min-height: 0; overflow: hidden; }`。
- Hover 必须保留 click / keyboard / `:focus-within` 触屏回退。
- 颜色、边框、阴影走 `apps/web/src/index.css` 的 `--mc-*`（默认 / light / eye-care / dark / sepia / contrast）。节点相关新增 token 六套主题都要补。
- 中文 JSDoc / 注释；改签名时同步注释。
- 每条交付：改动文件、行为说明、验收命令。有歧义停下来问，不要用未声明默认值掩盖。

当前尺寸与样式事实（改之前先认清）：

- 所有新节点默认 `230 × 216`（`DEFAULT_FLOW_NODE_WIDTH/HEIGHT`，`apps/web/src/canvas-utils.ts`）。
- 生成/转换节点：`.flow-generate-node` 已 `border: 0; box-shadow: none; background: transparent`。
- 来源节点（侧栏添加/拖入）：没有 `flow-generate-node`，仍走 `.flow-asset-node` 旧白卡片 + 1px 边框 + 明显阴影。
- 悬浮头名称前仍有媒体类型 icon（`AssetNode.tsx` 里 `media-icon`）。
- 「生成参数」按钮是 **click 开关**（`setMediaSettingsOpen`），不是 Hover。
- 清晰度 / 秒数走 `CompactSelect`，菜单是**单列 grid**；比例才是三列的 `QuickOptionMenu`。
- 视频仍有「宽度/高度（像素）」自定义输入。
- 新节点默认模型来自 `readNodeModelPreference`（上次选择），不是目录第一项；推理强度来自 `secondAvailableOption`（第二项），不是「高」。
- 资源栏卡片只有「添加到画布 / 重命名」，没有删除。归档 API 已存在：`POST /v1/assets/:id/archive|restore`。没有永久删除接口，不要发明。
- 我的资源页：只有 `image` 走 `ResourceThumbnail`，视频/音频/文本仍是 icon；栅格是 4 列等高卡片，不是流式。
- 跨页：只有设置页带 `?project=`；账户菜单已有有效链接，部分默认新标签页打开而跳过画布保存拦截，缺少来源项目上下文。

---

## 3. 已拍板口径（不要再问）

1. **#2 是相对当前默认值的增量，不是写死固定尺寸。** 只影响「用户没拖过」的新节点；已保存宽高一律保留。
   - 文本 / 音频：`230+40 × 216+30` → **`270 × 246`**
   - 图片 / 视频：`230+170 × 216+50` → **`400 × 266`**
   - 图片和视频现在一样大，不再给视频额外加宽。
2. **#9 推荐：细边框 + 极轻阴影，两者都要。** 阴影必须不晃眼：低透明度、小 blur、无大偏移、无选中光晕。选中只改边框色和 1px 级描边，禁止 `0 8px 22px` 或 `0 0 0 3px` 发光。来源节点和生成节点共用这套外壳。
3. **#7 推荐：整条悬浮头放进一张卡片，不要每个按钮一张卡。** 左侧名称、右侧操作组，组内用分隔线。多卡片在窄节点上会碎、会挡画面、阴影叠加晃眼。
4. **#5 视频自定义宽高整块 UI 删除。** 清晰度 + 比例已经表达尺寸。类型里的 `width`/`height` 可保留以兼容旧数据，但界面不再展示，新建不再自动填像素宽高。
5. **#6 新建生成/转换节点：当前媒体类型目录的第一项可用模型，同时保存其 `credentialId`；推理强度依次选择 `high`、标签「高」、目录第一可用值。** 不再用 `readNodeModelPreference` 当新建默认。手动改模型仍可写偏好，但新建不读它；空目录不编造模型或档位，加载、复制和撤销保留已有值。
6. **#1 删除 = 归档。** 二次确认后走现有 archive。已归档列表现有「恢复」保留。不要做永久删除（后端没有这条路由）。
7. **#3 Hover 打开生成参数，同时保留点击和键盘。** 不要做成「只能 Hover、点击无效」。
8. **#10 文本回显：双击进入编辑，单击不进入。** 必须能粘贴；Enter 换行；不要把按键冒泡成画布快捷键或删除节点。
9. **#12 从项目画布跳走时记住项目，目标页左下角固定「返回项目」。** 设置页兼容 `?project=`；其余页用独立的 `returnProjectId` 保存返回来源，`projectId` 仅作资源筛选。兼容读取旧参数，显式返回来源优先；管理子页间继续传递。普通点击同页保存后跳转，保存失败留在画布；保留浏览器修饰键打开链接的行为。
10. **#8 先复现再改。** 不许在没看网络请求的情况下改 URL 拼接。

---

## 4. 批次 0 · Bug：图片生成成功但不回显（#8）

### 现象

本地图片节点上游已成功，节点 `runStatus` 也是成功，但节点内部回显失败（空白 / 加载失败 / 「产物不存在」）。

### 先取证（必须留下记录）

1. 优先读取已有成功任务或隔离 Mock 成功任务，记下 nodeId、runId、`result.asset`（`assetId` / `version` / `contentUrl` / `mimeType`）。不为排查重复发起收费生成。
2. 看节点 data 里的 `resultAsset` 是否写上。
3. 看 Network：
   - `POST /v1/assets/:id/access-url` 状态和 JSON
   - 最终 `<img src>` 是签名 URL、blob:、还是未带鉴权的 `/v1/assets/.../content`
4. 对比「我的资源」详情预览（那边用 `apiFetch` + `blob` + `URL.createObjectURL`，是成功路径）。

### 已知高嫌疑（验证后只修被证实的）

- `AssetNode` 在缺少 `contentUrl` 时会拼 `/v1/assets/:id/versions/:n/content`；`inline_` / `remote_` 会得到空字符串。
- `useAuthenticatedAssetUrl` 使用媒体标签，Bearer 不随标签发送。原始地址已按 API origin 解析，但 `access-url` 返回的相对签名地址被直接使用，可能请求到 Web origin；失败又被静默忽略。必须用请求证据区分地址解析、鉴权与资产缺失。
- 文本结果已经走 `apiFetch`，图片/视频没有对齐。

### 修复方向

根据复现结果做最小修复：签名地址统一按 API origin 解析；签名/CDN 地址直接供媒体标签使用，不附加 Bearer。受保护字节读取若确需 blob，仅用于适合的预览，必须取消过期请求并释放 object URL；不得为了图片故障统一预下载完整视频。签名申请失败显示明确错误和重试，不静默退回未鉴权地址。覆盖相对签名、绝对 CDN、过期重试及切换资产的竞态。

### 验收

- 隔离成功任务或已有真实成功任务在节点内显示图片，记录网络结果；Mock 验收不得宣称真实 Provider 验收。
- 视频/音频回归：成功后也能出预览，不被这次修改打坏。
- 更新 `AssetPreview.test.tsx` / `AssetNode.test.tsx`。
- 控制台无 401/404 刷屏。

---

## 5. 批次 1 · 节点外壳统一（#9 #13 #7 #2）

一次改完，因为都在 `AssetNode.tsx` + `asset-node.css` + `canvas-utils.ts`。

### #13 侧栏添加/拖入仍是旧版

`mode === 'source'` 时不加 `flow-generate-node`，所以仍是旧卡片（内边距、类型 icon、模式徽章、硬阴影）。

来源节点也必须用同一套内容铺满 + 顶部悬浮栏。不要来源一张脸、生成一张脸。来源节点没有生成参数，但名称、启用、删除、上传与回显区一致。

### #9 边框 + 轻阴影

- 默认：`1px solid var(--mc-border)`，圆角沿用 8px。
- 阴影：例如 `0 1px 2px rgb(0 0 0 / 6%)`，深色主题用 `--mc-shadow` 但透明度更低。禁止大投影。
- Hover：边框略加深即可，不要额外光晕。
- 选中：`border-color: var(--mc-accent)`，不要 3px ring。
- 回显图/视频必须被边框托住，不能融进画布背景。
- 占位区（尚未生成）也要有可辨认底 + 边框，不能全透明。

### #7 悬浮头

- 删掉名称前面的类型 icon。
- 整栏一张半透明表面卡片（`var(--mc-surface)` + 细边框 + 与节点同级的轻阴影）。
- 左：可改名称；右：启用、状态、上传（批次 3 再加按钮；批次 1 先把删除/启用收进卡片并留出上传位）、删除。
- 继续 Hover / 选中 / focus-within 显示；保留名称到节点的 10px 指针桥，防止闪退。
- 悬浮头不计入节点宽高。

### #2 默认尺寸

按媒体类型提供默认宽高，不要继续四类共用 230×216：

| 类型 | 默认宽高 |
| --- | --- |
| text / audio | 270 × 246 |
| image / video | 400 × 266 |

仅新建生成/转换/来源工厂显式使用媒体默认尺寸。`withNodeAutoGrowthLimit` 的历史缺失尺寸补齐仍保留 230×216，已保存尺寸逐项保留；复制、粘贴、恢复及撤销不套用新建默认。测试分别覆盖新建和历史兼容，不能批量替换所有 230 断言。

### 验收

- 底部工具栏新建、右键新建、资源栏添加、拖入画布，四种节点同一外壳。
- 文本/音频新节点约 270×246，图片/视频约 400×266。
- 生成一张图后，图边缘与画布背景分离。
- `pnpm --filter @multimodal-canvas/web test` 里 canvas-utils / AssetNode / WorkflowCanvas 相关用例更新后通过。

---

## 6. 批次 2 · 生成参数（#3 #4 #5 #6）

可与批次 1 并行（文件不重叠）。主 Agent 最后才改 `App.tsx` 的 `createOperationNode`。

### #3 生成参数改为 Hover

`NodeQuickEditor.tsx` 里媒体参数按钮目前 `onClick` 切换 `mediaSettingsOpen`。改成与 `CompactSelect` 相同的 `openOnHover`：悬停打开、移出关闭、click 仍可钉住、Esc / 外点关闭、触屏 tap 可用。

### #4 清晰度 / 秒数不要一行一个

根因：这两项是 `CompactSelect`，`.compact-select-menu` 没有 `grid-template-columns`，所以 360p、480p、5s、10s 各占一行。

短枚举（清晰度、时长、图片 1K/2K…）菜单改为 3 列（极窄 2 列）。模型名、推理强度长文本保持单列。选项要短、可点、不要被拉成整行大按钮。

### #5 删除视频自定义宽高

删掉「宽度（像素）/ 高度（像素）」那一组。更新 `NodeQuickEditor.test.tsx` 里宽高用例：界面不再出现；旧节点 data 里残留宽高不在 UI 暴露，也不要在加载时清掉用户历史数据。

### #6 新节点默认

- 第一项模型：与 CompactSelect 当前媒体类型列表顺序一致。
- 推理强度：有 `high` 用 `high`；否则用标签「高」；再否则用模型声明的第一可用值。
- `applyNodeGenerationDefaults` 不要再对推理强度用 `secondAvailableOption`。
- `createOperationNode` 不要再把 `readNodeModelPreference` 当新建默认（主 Agent 接线）。

### 验收

- Hover 参数按钮即出现面板；把鼠标移到 360p 能点中。
- 视频参数里看不到自定义宽高。
- 新建图片/视频/文本/音频节点：模型是列表第一项，推理强度为高（模型支持时）。
- `NodeQuickEditor.test.tsx`、`node-generation-defaults` 测试、`canvas-editor.test.tsx` 更新后通过。

---

## 7. 批次 3 · 节点上传与文本双击编辑（#10）

等批次 1 的悬浮头卡片完成后再做。

### 上传

悬浮头加上传，走现有 `uploadAsset`，按节点类型限制：

| 节点 | accept |
| --- | --- |
| image | 图片 |
| video | 视频 |
| audio | 音频 |
| text | `.txt` `.md` 等文本 |

上传成功后在**当前节点**回显，不要再长一个节点。生成/转换节点保持原 mode。进度和失败重试复用现有上传逻辑。允许拖到节点上触发同一条路径。

### 文本双击编辑

`TextResultContent` 已有 `editable`，目前只修改组件状态，不能当作持久化。双击回显区进入编辑，可粘贴、可复制、可换行；失焦提交保存，Esc 放弃草稿，中文输入法组合期间不提交。编辑态加 `nodrag nopan nowheel` 并隔离画布快捷键。

### 保存与下游契约

- 上传和文本编辑共用手动内容替换路径：文本保存为新的文本资源，保留原资产和历史运行；不覆盖其他节点引用的资产。
- 使用已有 `assetId/contentUrl/mimeType` 保存手动资源引用；增加最小可选的手动输出标记时，必须同步 domain、API 白名单与运行输入解析，无数据库结构迁移。不能只改不会持久化的 `resultAsset`。
- 手动输出优先于历史生成结果，刷新、撤销、重做一致；下游读取手动资源并标记已有下游结果过期。原节点 mode、模型、提示词、尺寸及连线不变。
- 运行中禁止手动替换；上传期间禁止同节点重复替换和发起生成。上传完成但节点已删除或项目切换时不复活节点，已上传资源仍保留在资源库。
- 明确重新生成后，以新的成功结果替代手动输出；生成失败保留手动资源。过期运行事件不得覆盖后续手动修改。
- 保存失败保留草稿和错误，允许重试；刷新恢复、下游消费和失败恢复属于完成条件。

### 验收

- 图片节点上传本地图后节点内能看到图。
- 文本节点双击可把剪贴板文本贴进去并保留。
- 上传中节点不被内容撑高。

---

## 8. 批次 4 · 画布左侧资源栏删除（#1）

这是画布里的 `ResourcePanel`，不是「我的资源」页面。

每张卡片现有：添加（归档态是恢复）+ 重命名。在旁边加删除：

- 可用资源：确认后 `POST /v1/assets/:id/archive`
- 已归档：保留恢复，不要再放一个无效删除
- 按钮要有 `aria-label`，样式与现有小按钮一致，危险色仅在 hover

主 Agent 把 `handleArchiveAsset` 接到新按钮（现在归档只在「已归档 + 添加键」这条岔路上）。

### 验收

- 上传成功后，卡片上能删除（归档），列表消失；到已归档里能恢复。

---

## 9. 批次 5 · 独立页面（#11 #12）

### #12 跨页项目上下文 + 左下角返回

从 `/projects/:projectId` 出发的应用内跳转都带上项目：

- 抽 `appPaths.withProject(href, projectId)`，用 URL/URLSearchParams 合并返回来源，不覆盖 `create`、筛选、hash 等已有参数；返回来源与筛选独立
- 覆盖：顶栏、抽屉、logo、账户菜单（个人信息 / 安全 / 我的资源 / 我的任务 / 管理后台）
- 普通画布跳转先 `saveCanvas()` 再走带参 href；失败显示错误并留在画布。账户菜单普通点击统一同页，修饰键遵循浏览器约定

进入带项目上下文的管理页后，**左下角固定「返回项目」**，指向 `appPaths.project(projectId)`，文案带项目名（没有名字就「返回项目」）。不要只做资源页。不要用浏览器 Back 代替。

### #11 我的资源

- 卡片上方必须真实回显：图片=图，视频=封面/首帧，音频=波形或类型化预览，文本=摘录。不要只放 icon。缩略图失败再回退类型占位。
- 改为流式 / 自适应列，媒体按比例 `object-fit: cover` 不拉伸；高矮可以不同，避免现在 16:10 一律裁切的死板网格。
- 点击预览：图看大图，视频能播放，左右切换，关闭按钮，Esc 关闭。
- 未加载用同比例骨架，避免布局跳。
- 与 #12 联动：从画布进来带 `?returnProjectId=`，首次可同时筛当前项目，但修改筛选不能更改返回来源。

`ResourceThumbnail` 复用已有鉴权读取及可见时加载；图片用 thumbnail、视频用 poster、音频用 waveform，文本用有上限的摘录。完整视频/音频只在详情按需读取。利用现有尺寸元数据预留比例，缺失时用稳定占位比例；失败降级，不在卡片中批量下载完整媒体。

### 验收

- 项目画布 → 我的资源，左下角能回到同一项目，画布没丢。
- 资源页图片/视频卡片一眼能认出内容，点击能预览。

---

## 10. 建议时间线

```text
Day 0    主：#8 取证 + 修回显
Day 0-1  主：#9 #13 #7 #2 外壳
同时     A：#3 #4 #5 #6 参数
同时     B：#1 删除按钮（主 Agent 接 App.tsx）
同时     C：#11 #12 管理页
Day 1-2  主：#10 上传 + 文本双击（等外壳）
最后     主：接线、全量测试、按 Git 规则提交
```

---

## 11. 验证清单

至少：

```bash
pnpm --filter @multimodal-canvas/web test -- AssetPreview AssetNode NodeQuickEditor ResourcePanel ResourcePages canvas-utils WorkflowCanvas
pnpm --filter @multimodal-canvas/web test
pnpm --filter @multimodal-canvas/web typecheck
pnpm --filter @multimodal-canvas/web lint
pnpm build
```

涉及回显 / access-url / 资源内容时补 API 测试。关键路径手动点一次：生成图片回显、Hover 参数、侧栏拖入节点外观、资源栏删除、我的资源预览、左下角返回项目。

补齐：上传/文本编辑刷新与下游消费、运行冲突、上传中删除节点、保存失败禁止跳转、中文输入法与快捷键隔离、旧尺寸恢复、六套主题截图。全量 lint/typecheck/test/build 完成后检查 diff、敏感信息和 Git 状态。执行证据和未完成项维护在 `docs/canvas-optimization-checkpoint.md`。

---

## 12. 明确不做

- 不改 Provider 协议、计费、Agent 节点。
- 不做资源永久删除。
- 不把节点改成固定不能拖拽。
- 不把生成参数改成只能 Hover、禁用点击。
- 不恢复「视频再单独加宽 70」的旧口径。
- 不把来源节点做成和生成节点两套外观。
- 没有复现证据前，不把 #8 当成「加个边框就能好」。
