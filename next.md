# 下一阶段：节点信息、设置与画布交互增强计划

当前任务：完善节点悬浮信息、反推提示词、资源预览、竖向右键菜单与组移动。范围、验收与检查点见 [节点交互与反推提示词](docs/node-interaction-reverse-prompt.md)。

前序资源输入兼容检查点见 [资源输入兼容](docs/resource-input-compatibility.md)。

图片引用兼容后续任务：用户已授权取消图片流程的强制声明限制，并接通资源引用到 `edits`。当前执行状态、边界及回滚说明见 [图片引用与编辑兼容](docs/image-reference-compatibility.md)。

更新时间：2026-09-17。

本文最初用于制定实施计划。2026-09-17 已补齐第 13 节列出的代码缺口，并完成本地回归；最新状态见第 14 节及 [六项需求验收记录](docs/six-feature-acceptance.md)第 7 节。**真实供应商整体验收仍未通过**：获授权的三次生成中图片成功，文字 HTTP 524，视频因验收脚本缺少时长返回 HTTP 400。没有操作生产数据库。第 3～8 节保留详细需求与约束，不能把自动化通过扩大为所有供应商条件已满足。

主目标级别为 **P1：完善 PC Web 创作流程**。涉及真实请求记录、凭据边界和画布存储的准备工作列为 P0 前置条件。每次推进一个可验收阶段，阶段完成后再进入下一阶段。

## 1. 已核对的基线

| 项目       | 本轮确认结果                                                                                                  |
| ---------- | ------------------------------------------------------------------------------------------------------------- |
| 工作目录   | `G:\multimodal-canvas`，是 Git worktree                                                                       |
| 分支与起点 | `codex/generate-to-new-node @ f631109`，`fix(canvas): 将新节点落点间距调整为 22px`                            |
| 起始工作区 | 干净，无待保留的未提交修改                                                                                    |
| 配置的上游 | `origin/codex/generate-to-new-node`                                                                           |
| 远程       | `origin` 为 GitHub `lysimportant/multimodal-canvas`；另有 `gitee`，本轮按当前上游交付                         |
| 环境       | Windows PowerShell 7.6.5，Node v24.12.0，pnpm 11.19.0                                                         |
| 本地依赖   | 已存在 `node_modules`、Prettier 和 Vitest；未安装或升级依赖                                                   |
| 技术基础   | React 19、React Flow `@xyflow/react` 12、Zustand、共享 Domain/Zod、API/Worker/Provider、Prisma                |
| 适用说明   | 本轮检索未发现工作目录及适用父目录中的 `AGENTS.md`，使用本会话提供的规则；根目录没有 `README.md` 或 `TODO.md` |
| 任务上下文 | 已读 `TODO-CONSOLIDATED.md`、节点设置、画布优化、节点预览检查点，以及对应实现和测试                           |

本轮执行的业务基线检查：

```powershell
pnpm --filter @multimodal-canvas/web exec vitest run src/workspace/AssetNode.test.tsx src/workspace/FlowingCanvasEdge.test.tsx src/workspace/node-generation-defaults.test.ts src/settings-panel.test.tsx src/settings-utils.test.ts src/state/workspace-preferences.test.ts src/canvas-utils.test.ts src/run-update-utils.test.ts --reporter=dot
```

结果：**8 个测试文件、123 项通过**。相关现有 Markdown 的 Prettier 检查通过。本轮文档的最终检查记录见第 12 节。

这些结果只代表已运行的专项。2026-09-13 的检查点曾记录全仓库格式、Worker 测试和旧浏览器夹具问题；本轮没有重跑这些全量检查，不能把历史问题认定为仍然存在，也不能宣称已修复。实施开始前须重新建立全仓库基线。

### 1.1 当前实现与真实缺口

| 需求           | 已有能力                                                                | 本次需要补齐                                                   |
| -------------- | ----------------------------------------------------------------------- | -------------------------------------------------------------- |
| 真实提示词预览 | 节点提示词、结构化提及、运行冻结快照、结果资产版本、节点悬浮栏和 Dialog | 最终请求提示词留存、结果版本关联、整体摘要、独立复制与查看入口 |
| 生成耗时       | Run/ProviderJob 的创建和更新时间、运行状态                              | 节点粒度的明确起止时间、耗时语义、恢复与展示                   |
| 设置增强       | 统一连接、Key 管理、模型目录；后端已有类型默认模型和节点独立凭据        | 宽版界面、类型默认入口、单节点独立连接入口、默认值优先级协调   |
| 连线增强       | `flow/pulse/minimal` 三种组合模式，实际路径均为贝塞尔                   | 路径样式与动画特效解耦，至少五种路径和五种特效                 |
| 分组           | 节点移动、手动缩放、撤销、剪贴板                                        | 区域组、成员关系、组移动与缩放、完整持久化                     |
| 清空增强       | 清空画布、确认、一次撤销，保留资源库资产                                | hover 二级菜单、空节点判定、清空前预览与分组一致性             |

主要依据：

- 节点与结果：[AssetNode.tsx](apps/web/src/workspace/AssetNode.tsx)、[canvas-utils.ts](apps/web/src/canvas-utils.ts)、[run-update-utils.ts](apps/web/src/run-update-utils.ts)、[result-versions.ts](apps/web/src/result-versions.ts)。
- 请求与存储：[Domain](packages/domain/src/index.ts)、[Provider](packages/providers/src/index.ts)、[讯飞 Provider](packages/providers/src/xfyun.ts)、[Worker](apps/worker/src/index.ts)、[结果归档](apps/worker/src/result-archiver.ts)、[运行持久化](apps/api/src/run-persistence.ts)。
- 设置：[SettingsPanel.tsx](apps/web/src/workspace/SettingsPanel.tsx)、[SettingsPage.tsx](apps/web/src/pages/SettingsPage.tsx)、[API 路由](apps/api/src/app.ts)、[设置存储](apps/api/src/settings.ts)。
- 画布：[CanvasNodeToolbar.tsx](apps/web/src/workspace/CanvasNodeToolbar.tsx)、[AppearancePicker.tsx](apps/web/src/workspace/AppearancePicker.tsx)、[FlowingCanvasEdge.tsx](apps/web/src/workspace/FlowingCanvasEdge.tsx)、[项目存储](apps/api/src/projects.ts)、[Prisma](prisma/schema.prisma)。

## 2. 范围与交互决策

1. 优先 PC Web，验收 1366×768、1440×900、1920×1080。较窄窗口允许收起左侧导航、纵向滚动，复杂手机交互后置。
2. “每个节点的默认模型和 Key”覆盖两个层次：文字/图片/音频/视频生成的类型默认，以及单个画布节点的独立覆盖。来源节点、纯输入用途和分组区域不强制配置模型。
3. 提示词 Dialog 使用短标题“生成提示词”，第一块显示可复制的整体摘要，第二块显示可复制的完整提示词。长摘要不挤进标题栏。
4. 耗时默认放在节点悬浮卡片的次级信息区，与模型、状态并列；不覆盖媒体右下角和手动缩放控制点。
5. 连线保留两个独立选择区：路径样式、动态特效。首版沿用当前浏览器的工作区外观偏好范围，不增加每条边单独配置。
6. 分组是画布布局区域，不是第五种媒体类型，不增加生成调用，不改变现有节点输入输出语义。
7. **用户已确认：清空空节点只删除内容为空的提示词节点，保留已填写内容的提示词节点。** 不再使用“只要没有产物就删除”的判定。
8. 现有设置总览、统一连接和模型目录保留。代码里未发现独立命名为“总结”的设置模块，本计划按保留现有汇总信息处理，不以重排界面为由删除功能。

不在本次范围：供应商新模型接入、计费系统、自动批量付费生成、远程取消合同扩展、多人协作、嵌套分组、整组一键运行、手机专用布局、节点缩略图重新设计。原有待办仍由 [TODO-CONSOLIDATED.md](TODO-CONSOLIDATED.md) 管理，本文件只承接本次六项需求。

## 3. 需求一：保存并预览结果的真实提示词

### 3.1 留存位置与数据流

当前 `RunSnapshot` 保存输入，但 Provider 还会合并上游输入、解析 `promptDocument`、选择参数来源，文字请求可能发送多条 `messages`，音频可能发送 `input`。因此不能把“当前编辑框的 prompt”直接标成“生成该结果的真实提示词”。

实施链路：冻结运行输入 -> Provider 完成最终请求组装 -> 持久化本次请求文本快照 -> 发起请求 -> 收到结果并归档 -> 将快照绑定到结果资产版本 -> 节点按当前展示的版本查询。

- [ ] 在最终 JSON、multipart 或 WebSocket 输入组装处采集文本，复用实际发送的变量；不在前端重新拼一份推测结果。
- [ ] 图片记录最终 `prompt`，图生图同时保留原图资产版本引用；视频记录最终提示词和已支持的负向提示词；音频记录真正发送的朗读文本/指令；文字记录有序角色消息及文本块。
- [ ] 有多个输入、角色、消息时按原顺序保存结构，完整提示词视图保留分隔和角色，不拼成难以辨认的一段文字。
- [ ] 若执行链路存在翻译或改写，保留最终发送文本及原始输入的区分；模型 ID、字段名、引用身份不翻译，不把原始中文错误标为已发送文本。
- [ ] 调用前持久化失败时阻止请求，避免发生已计费却无法追溯的结果。快照已写入但发送状态不明时记录不确定状态，不据此自动重发创建请求。
- [ ] 以 `runId + nodeId + attempt/requestIdentity` 标识请求记录，以 `assetId + assetVersion` 关联展示结果。一个 DAG 内每个实际执行节点分别留存，不能把目标节点的提示词附给所有上游结果。
- [ ] “生成到新节点”“修改图片”、原地重新生成、历史版本切换、手动覆盖、刷新和服务重启均保持正确归属；新结果未成功时不把旧产物的说明换成新提示词。
- [ ] 用户删除原节点后，仍能从保留的结果资产版本找到其生成说明；不要只把说明放在临时 React 状态或画布 `node.data`。

建议定义版本化生成说明记录，字段名在 P0 阶段与现有 Domain 合同一起冻结：

| 内容       | 语义与约束                                                               |
| ---------- | ------------------------------------------------------------------------ |
| 记录身份   | schemaVersion、runId、nodeId、attempt、请求身份及发送状态                |
| 结果身份   | assetId、assetVersion；归档完成后建立关系                                |
| 原始输入   | 冻结输入的只读关联，明确与最终请求文本不同                               |
| 请求提示词 | 有序文本字段/消息、正向与负向内容、格式版本，不保留原始完整 HTTP body    |
| 参考资源   | 资产 ID、版本、角色及顺序；不保存 base64、临时签名 URL 或媒体二进制      |
| 模型信息   | 精确 Provider 与 modelAlias；界面可显示凭据标签/指纹摘要，不返回原始 Key |
| 摘要信息   | 摘要正文、生成方式、状态；用户修改摘要不修改真实请求文本                 |
| 计时关联   | 对应本节点本次执行的时间记录，见需求二                                   |

请求文本是用户内容，应在现有项目/资产权限下读取。列表和 SSE 只携带必要摘要或记录 ID，完整内容按需读取；通用日志、错误消息和监控中不输出提示词全文。对系统凭据、URL、二进制的过滤必须按字段处理，不能用粗暴字符串替换悄悄改写正常提示词。

优先复用 Run 的持久化信息与 AssetVersion 的 metadata/关联能力；如现有 JSON 字段不适合大小、查询或权限要求，再增加独立记录。不能只给 Zod 加字段而遗漏 API 映射、Worker 存储、公共响应裁剪、资产接口和导出链路。

### 3.2 摘要、Dialog 与复制

- [ ] 悬浮卡片增加带图标的“提示词”动作；有生成记录时打开只读 Dialog。与节点输入编辑入口区分，点击时不触发拖拽、重命名或媒体预览。
- [ ] Dialog 推荐宽度 800px，受视口边距约束，最大高约 `84dvh`。标题和关闭按钮固定；正文独立滚动，长词、长 URL 自动换行，不改变节点外壳尺寸。
- [ ] 摘要位于第一块，建议 30～80 个中文字符，可自然换行。例如：“月白布衫，青裙，发髻松一缕，袖口有薄面灰，右腕旧红绳。”摘要旁有独立复制图标。
- [ ] 完整提示词放在下一块，按实际请求顺序显示，保留换行；提供“复制完整提示词”。多消息结果的复制文本使用稳定的角色分隔；纯图片 prompt 的复制值与实际发送字符串一致。
- [ ] 模型、生成时间、耗时、结果版本显示为紧凑信息行，不挤占正文；参考资源用名称与版本表示。
- [ ] 复制成功有短暂状态反馈；剪贴板失败保留可选中文本并显示错误，不谎报成功。两个复制动作互不影响。
- [ ] Dialog 支持键盘进入、Esc、焦点圈定及关闭后返回触发按钮；加载、加载失败、记录缺失状态均可理解，失败可重试。

**摘要策略必须与真实提示词分离。** 摘要应概括主体、外观、动作、环境和风格等已有信息，不增加原文没有的细节；不能简单截取前几十个字后称为整体总结。

建议提供本地提炼/手动摘要作为基础，优先复用已有结构化梗概；缺少可用依据时明确提示待补充。若希望任意长提示词都自动得到自然语言整体总结，可启用“自动总结提示词”，使用已配置的文字默认模型异步生成并单独记录其任务和费用性质。摘要失败不影响原图成功、完整提示词查看和复制，不重复生成原图。该自动总结选项属于第 11 节待讨论的产品决策，不能在实现中悄悄增加付费调用，也不能把缺失摘要当成已满足整体总结验收。

### 3.3 历史与其他节点

- [ ] 文字、图片、音频、视频共用读取和展示行为，各类型保留真实请求的字段结构。
- [ ] 纯输入节点可查看“当前提示词”，明确它不是某次生成记录；导入/上传的资产没有生成记录时显示“未记录生成提示词”，不从文件名或当前编辑框伪造。
- [ ] 历史任务若仅有冻结输入，只能显示“历史输入快照，未记录最终请求”；不能无证据回填为真实请求。
- [ ] 导出/导入明确是否携带生成说明，并随资源版本和权限检查一起处理；包含用户提示词的导出需要清楚的内容范围，不夹带 Key。

验收：四类生成各有实例；提交后立刻修改编辑框、上游输入合并、图生图、文字多消息、旧结果保留、新节点分叉、历史版本切换、手动覆盖、长文本与双复制、无记录与加载失败均有用例。使用 Mock 捕获的实际请求字段与保存记录逐项比较，不能只比较两份由同一个模拟 helper 生成的对象。

## 4. 需求二：节点生成耗时

### 4.1 计时口径

`createdAt/updatedAt` 会被状态更新影响，不能直接相减后称为生成耗时。每个节点在服务端记录自己的生命周期时间，前端只负责显示运行中的递增值。

| 指标         | 定义                                                                                             |
| ------------ | ------------------------------------------------------------------------------------------------ |
| 排队/等待    | 本节点进入待执行状态到真正开始执行，包含依赖等待，按需在详情展示                                 |
| 节点执行耗时 | `finishedAt - startedAt`，包括本节点准备输入、供应商处理、轮询及结果归档；悬浮卡片默认显示这一值 |
| 请求阶段耗时 | 从发起供应商请求到取得可用结果；仅有明确时间戳时展示，不冒充供应商内部计算时间                   |
| 总等待时间   | 从提交到终态，适合详情查看，不能替代 DAG 中每个节点的执行耗时                                    |

- [ ] 时间戳使用服务端 UTC，时长使用非负毫秒整数；界面短耗时如 `12.4 s`，长耗时如 `2 分 08 秒`。
- [ ] 开始时间写入后不因轮询或 Worker 重启重置；终态时间仅写入一次。取消、失败同样有终态，但标为取消/失败耗时。
- [ ] 暂时离线、页面切换、SSE 重连和 Worker 接管后，从持久化时间恢复；拒绝旧事件倒退状态，不累计多次重复执行的时间。
- [ ] 重试使用本次 attempt 的时间，保留上一结果的耗时；前端展示跟随当前结果版本，不混用“新任务计时”和“旧结果耗时”。
- [ ] 未执行的来源节点不伪造耗时；历史记录没有准确时间时显示“未记录”，不显示 0 秒或推测值。
- [ ] 对异常时间顺序显式标记不可用，避免负数。运行中显示参考服务端时间偏移，最终以服务端落库值为准。

### 4.2 展示与验收

- [ ] 默认在悬浮卡片增加时钟图标及耗时；运行中可显示当前阶段和已用时间，完成后冻结。详情里解释统计口径即可，不在画布铺说明文字。
- [ ] 定时更新只服务可见、活动节点，使用共享时钟或现有状态机制，不给每个静态节点各开一个定时器。
- [ ] 长时间显示、中文状态、缩小画布、悬浮卡片换行均不遮挡操作，不改变节点尺寸。

验收覆盖同步生成、异步视频、DAG 上游等待、重试、失败、取消、刷新、重连、重复终态和进程恢复。使用受控时间测试计算，并验证显示值与持久化记录一致。

## 5. 需求三：宽版设置、类型默认模型与独立 Key

### 5.1 界面布局

当前设置 Dialog 约 640px 宽，现有默认模型编辑入口曾被主动移除，但后端默认值能力仍在。本次是重做入口与规则，需要同步更新“默认模型入口已移除”的旧测试。

- [ ] 设置 Dialog 改为宽版，推荐 `min(1080px, calc(100vw - 48px))`，最大高约 `86dvh`；独立设置页复用同一内容组件。
- [ ] 左侧约 176px 分类导航，右侧内容可滚动；标题、保存状态和底部保存/取消区保持稳定，不随列表跳动。
- [ ] 分类为“总览”“节点默认”“连接与 Key”“画布外观”。以对齐的表单行和分隔线组织，不堆叠卡片。
- [ ] 保留现有当前连接、Key 指纹、超时、测试连接、刷新模型、切换/删除 Key、模型目录和外观能力。保留已有汇总信息，修正设置页与实际功能不一致的说明。
- [ ] 密钥输入默认隐藏；只允许查看尚未提交的当前输入，不从服务端回显已保存密钥。图标按钮有可访问名称和 tooltip。

节点默认区使用四行：

| 类型     | 默认模型                | 凭据来源                     | 行操作             |
| -------- | ----------------------- | ---------------------------- | ------------------ |
| 文字生成 | 当前 Key 支持的文字模型 | 继承 / 已保存连接 / 独立连接 | 配置连接、恢复继承 |
| 图片生成 | 当前 Key 支持的图片模型 | 同上                         | 同上               |
| 音频生成 | 当前 Key 支持的音频模型 | 同上                         | 同上               |
| 视频生成 | 当前 Key 支持的视频模型 | 同上                         | 同上               |

每行默认紧凑展示。展开当前行才显示 Base URL、Key 输入、保存连接和刷新模型，不让四套密码表单同时占满屏幕。模型用可搜索的选择器，保留精确模型 ID；相同模型来自不同 Key 时显示来源，不能合并成无法区分的一项。全局/当前项目用范围切换控件，明确正在修改哪个范围。

### 5.2 继承与独立覆盖

- [ ] 保留服务端解析顺序：本次运行显式配置 > 单节点显式配置 > 项目类型默认 > 全局类型默认。模型与凭据按已验证的组合解析，不产生模型 A 配上 Key B 的混合状态。
- [ ] 新建节点优先遵守显式类型默认；修正当前“最近同类节点 > 浏览器记忆 > 目录首项”遮蔽设置的问题。无配置时才兼容最近选择，并把最终来源显示清楚。
- [ ] 现有已保存的节点配置不批量覆盖。“继承”状态不强制写死当前默认值；运行创建时冻结最终模型和凭据版本。
- [ ] 单节点编辑区支持选择已保存连接或新增独立连接，并能恢复继承。修改一个节点不改其他节点或全局活动连接。
- [ ] 分叉到新节点沿用现有继承配置约定；同时明确复制的是显式覆盖还是继承状态，不能在分叉时暗中换 Key。
- [ ] 删除 Key 后，受影响的默认值和新任务显示明确失效状态，不自动选择另一个 Key；已提交任务继续按已有冻结版本规则执行。

### 5.3 凭据保存与权限

当前新增 Key 的统一设置保存流程会改变全局活动连接，因此不能直接在每个节点行重复调用它来冒充独立 Key 保存。

- [ ] 为现有凭据存储补充“创建独立凭据并返回 ID/摘要，不自动激活全局连接”的操作；内存、文件、Prisma 三种实现同时覆盖。
- [ ] 选择独立连接后，通过该凭据读取/刷新模型目录，无须临时切换全局 Key。
- [ ] 画布和默认配置只存凭据引用，队列快照存必要版本；原始 Key 仅经受保护的保存请求进入现有服务端加密存储，不进 localStorage、剪贴板导出、日志或源码。
- [ ] 保持现有管理员管理凭据的权限边界。普通账号界面按实际权限显示可用配置；个人自带 Key 的权限隔离不因为本次 UI 改造自动开放。
- [ ] 保存连接成功但刷新模型失败时，分别显示两个状态，保留草稿和已保存连接；重复提交、晚到响应、切换账号和关闭未保存表单有确定行为。

验收：四类默认各用不同合成 Key；同名模型不同 Key；项目覆盖全局；实例覆盖类型；恢复继承；新增独立 Key 不切换全局；改默认不影响在途任务；删除/失效/无模型/无权限；重启恢复；长模型名和 URL；中文输入法、键盘导航、保存反馈。真实 Key 可用性需要实施时另行核验，Mock 不等于供应商验收。

## 6. 需求四：连接线样式与特效独立选择

### 6.1 至少五种路径样式

| 路径     | 预期形态             | 实现依据                       |
| -------- | -------------------- | ------------------------------ |
| 标准曲线 | 当前贝塞尔形态       | 复用 `getBezierPath`           |
| 轻弧曲线 | 曲率更低，较接近直连 | 同一贝塞尔 API 的曲率配置      |
| 圆角折线 | 横纵走线，转角圆润   | React Flow 的 smooth step 路径 |
| 直角折线 | 横纵走线，转角明确   | 同一路径 API 的零圆角配置      |
| 直线     | 两端直接相连         | React Flow 的 straight 路径    |

### 6.2 五种动态特效，另加关闭选项

| 特效     | 预期形态                         |
| -------- | -------------------------------- |
| 流光     | 沿路径行进的短亮线，兼容当前观感 |
| 虚线行进 | 动态虚线覆盖层沿方向移动         |
| 单点巡航 | 单个标记沿路径循环移动           |
| 多点流动 | 多个间隔标记沿路径移动           |
| 呼吸脉冲 | 覆盖层透明度或亮度轻微变化       |
| 无特效   | 保留基础路径，完全静止           |

- [ ] 用独立的 `edgePathStyle` 与 `edgeEffect` 偏好表达组合。选择特效不能改路径几何；选择路径不能重置特效。
- [ ] 外观面板分两组选择区，各项有真实小预览，并提供最终组合预览；默认标准曲线 + 流光。
- [ ] 落定连线与拖拽连接预览共用路径策略，保留现有端点圆心修正，不引入松手跳点。
- [ ] 特效叠加层不接收鼠标事件；基础边始终负责选择命中、状态颜色和连线删除等现有交互。
- [ ] 旧 `flow` 映射到标准曲线 + 流光，旧 `pulse` 映射到标准曲线 + 呼吸脉冲，旧 `minimal` 映射到标准曲线 + 无特效。保留旧值读取和明确迁移，不在升级时强制开启动画。
- [ ] 尊重 `prefers-reduced-motion`，关闭非必要动画；选中与运行状态仍可由静态颜色/线型辨认。
- [ ] 无效本地偏好回退到已定义默认值；切换立即预览，刷新后保持，不改变画布边 ID、顺序、端口和 DAG。

验收：自动覆盖 **5 × 6 = 30 个组合**的独立性，浏览器重点检查曲线/折线/直线与不同动画、拖线预览、选中、缩放、深浅主题、减少动态效果。实施前后用同一合成大画布比较交互与帧时间，发现性能瓶颈后再优化，不预先增加动画框架。

## 7. 需求五：胶囊工具栏创建可移动、可调大小的区域组

### 7.1 数据与持久化

目前 Domain 只接受四种媒体节点，Prisma Canvas 通过关系表存 nodes/edges，没有组字段。仅添加 React Flow `parentId` 无法完成保存、恢复和导出。

- [ ] 推荐在 `CanvasDocument` 增加可选 `groups`，组包含 `id/name/position/width/height/nodeIds`，旧画布缺省按空组列表读取。
- [ ] 组信息与媒体节点分开，不扩展 `MediaType`，不加入运行 DAG，不提供输入输出端口，不出现在模型选择和资源生成请求中。
- [ ] 优先为 Prisma Canvas 增加组布局 JSON 字段，并在同一 revision 更新事务里保存；若实施前证明需要独立组表，再记录选择依据和迁移。不能宣称现有数据库无改动就能持久化。
- [ ] 同步 Memory/File/Prisma 项目存储、校验、保存冲突处理、草稿、剪贴板、导入导出和撤销快照，避免某条路径默默丢失组。
- [ ] 媒体节点持久化继续使用画布绝对坐标。视图层优先复用 React Flow 的组、`parentId` 和 `NodeResizer`；转换为组内相对坐标只发生在视图边界，读回时恢复绝对坐标。
- [ ] 组 ID 唯一、成员 ID 存在、一个节点最多属于一个组；位置必须有限，宽高为正且有合理上限。首版禁止嵌套和循环归属。

### 7.2 交互规则

- [ ] 胶囊新增 lucide 分组图标和 tooltip。有已选节点时创建包围选区的组；无选区时在视口中心附近创建约 640×420 的空区域，可调长宽。
- [ ] 组用低饱和半透明底色、细边框和可重命名标题条；层级低于媒体节点，不形成嵌套卡片，也不遮挡端口和连线。
- [ ] 拖动节点时预览目标组，松手后按节点中心是否落入组内容区域决定入组；拖出后解除归属并保持绝对位置。
- [ ] 多个组重叠时优先最小包含区域，同面积以稳定 ID 决定；一次拖拽只能归属一个组，预览与落地规则一致。
- [ ] 拖组标题条时，所有成员以同一位移移动；不能因同时选中组和子节点重复移动。跨组连线继续跟随原节点。
- [ ] 拖组的边/角调整宽高，不缩放成员，不改变图片/视频尺寸，不因组移动或缩放隐式吸入路过节点。
- [ ] 有成员时最小尺寸覆盖成员包围盒及内边距；缩小操作不挤压成员、不自动把节点推出组。空组有固定最小尺寸。
- [ ] 默认“解散组”只移除区域并保留成员、坐标和连线。删除成员属于明确的独立动作，不把删除组当成删除全部内容。
- [ ] 成员删除、清空空节点后同步成员列表，空组保留；“清空画布”同时移除所有组。
- [ ] 整组移动、入组/出组、改名、缩放、解散分别形成一次撤销操作，支持重做；中间的拖拽帧不挤满历史记录。

### 7.3 与现有生成流程兼容

- [ ] 组内“生成到新节点”和“修改图片”使用绝对坐标进行避让，保持当前新节点 22px 落点间距的已验证行为。
- [ ] 推荐派生子节点继承来源节点的组；空间不足时只扩展组区域以包容新成员，不改变成员大小。该扩展与创建子节点进入同一历史操作。
- [ ] fit view、框选、复制、粘贴和跨项目导入使用正确坐标；复制整组重建组/成员 ID，复制部分成员不携带悬空组归属。
- [ ] 运行快照中只保留参与执行的媒体节点及业务边，证明有无分组不会改变请求内容、执行顺序或计费次数。

验收：空组、选区成组、拖入/拖出、重叠组、整组移动、单独移动成员、独立调整宽高、缩放画布后拖动、解散、撤销重做、跨组连线、组内分叉生成、保存刷新、File 重启、Prisma 恢复、剪贴板和 JSON 导入导出往返。接口层增加非法成员、重复归属和 revision 冲突测试。

## 8. 需求六：清空按钮 hover 菜单与清空空节点

### 8.1 菜单行为

- [ ] 现有橡皮擦按钮在 hover 后展开“清空画布”“清空空节点”两个动作；按钮点击和键盘聚焦也能展开。
- [ ] 从触发按钮移入菜单时保持展开，避免闪退；Esc、外部点击和离开完整交互区域后关闭。点击触发器本身只展开，不直接删除。
- [ ] 两个动作显示本次候选数量；没有可清理内容时禁用对应动作，不弹空确认框。
- [ ] 执行前的确认展示节点/连线/组数量和作用范围，取消后没有任何数据变化。

### 8.2 已确认的空节点判定

**仅当没有自身资源、没有保留的生成结果、没有提示词内容、没有有效输入引用，且没有进行中的操作时，才加入清空空节点候选。** 无法确定资源或运行状态时先保留。

| 情况                                       | 是否清空空节点                           |
| ------------------------------------------ | ---------------------------------------- |
| 新建节点，未输入文字、未绑定资源、无任务   | 是                                       |
| 提示词为空或仅空白，且没有其他内容         | 是                                       |
| 提示词已填写，即使从未生成过               | 否，用户已明确要求保留                   |
| `promptDocument` 含有效文本或资源提及      | 否，不能只看旧 `prompt` 字段             |
| 有 `assetId`，但 URL 缺失、过期或预览失败  | 否，不能把加载失败当空节点               |
| 有当前/历史保留结果或手动输出              | 否，包括本次生成失败但旧结果仍在的节点   |
| 只有参考资源、图生图来源或有效上游输入     | 否，视为已有创作输入                     |
| 排队、准备、生成、归档、取消请求中或上传中 | 否，避免操作竞态                         |
| 历史/运行记录尚未恢复，资源状态待查询      | 否，先完成读取再计算候选                 |
| 只选了模型、Key 或尺寸参数，没有实际输入   | 是；确认中可说明会移除空模板             |
| 空分组区域                                 | 否，清空空节点保留组；清空画布才一并移除 |

现有 `nodeHasEcho` 用于判断能否回显，不适合直接作为安全清理判定，因为它依赖 URL。应以持久化资源身份、当前内容、保留输出和操作状态判断，形成可单测的纯函数；前端候选预览和真正执行共用同一规则。

### 8.3 删除范围、历史与竞态

- [ ] “清空画布”移除当前画布的节点、边和组；“清空空节点”只移除确认候选、关联边及组成员引用。
- [ ] 两者都不删除资源库资产、资产版本、生成说明、运行记录或 Key，不额外发起 Provider 取消/重试。
- [ ] 候选预览后到确认之间，若节点输入、资源、运行或上传状态改变，重新计算并要求用户看到更新后的数量，不照旧名单删除已有内容。
- [ ] 所有删除作为一次历史事务，撤销恢复节点、提示词、边、组和成员关系；保存失败保留本地可恢复状态并提示错误。
- [ ] 清空整个画布若有在途任务，明确列出数量并提示任务继续执行、结果仍归档；后续完成事件不得把已清空的节点重新插回画布。运行记录仍可从对应历史入口查阅。
- [ ] 清空后清理选择状态和失效编辑器，避免 Dialog/快速编辑器继续操作不存在的节点；保留画布视角，避免突然跳动。

验收覆盖上表全部情况，以及菜单 hover/focus/click、移入子菜单、取消确认、状态在确认期间改变、组内外混合、两端节点同时删除、一次撤销恢复、重做、保存刷新和迟到运行事件。

## 9. 分阶段执行清单

阶段 A～F 已有实现与专项测试，联合验收按第 13 节恢复；阶段关闭以实际证据为准。阶段执行顺序是依赖顺序，不是固定工期承诺。

### 9.1 阶段 A 已冻结的合同（实施记录）

以下字段与权限决定已落到领域包与数据库迁移，作为后续阶段的实现依据。

| 合同           | 位置                                                                                                     | 决定                                                                                                                     |
| -------------- | -------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| 生成说明记录   | `REQUEST_PROMPT_SCHEMA_VERSION`、`requestPromptRecordSchema`                                             | 版本化记录，身份为 `runId + nodeId + attempt + requestIdentity`；结果身份 `assetId + assetVersion` 归档后补写            |
| 请求文本结构   | `requestPromptPartSchema`、`requestPromptFormatSchema`                                                   | `plain` 单块与 `messages` 有序角色消息两种格式；复制文本用 `renderRequestPromptText` 保留角色分隔                        |
| 参考资源       | `requestPromptResourceSchema`                                                                            | 只存资产身份、版本、角色与顺序；不存 base64、签名 URL 或媒体二进制                                                       |
| 发送状态       | `requestPromptSendStatusSchema`                                                                          | `pending`（发送前持久化）/`sent`/`failed`/`unknown`；不确定状态不触发自动重发                                            |
| 提示词读取结果 | `NodePromptView`、`requestPromptOrigins`                                                                 | `request` / `input-snapshot` / `none` 三态；历史记录只显示“历史输入快照，未记录最终请求”，不回填                         |
| 计时记录       | `nodeTimingSchema`、`nodeTimingDuration`、`formatNodeDuration`                                           | 服务端 UTC 时间戳；终态耗时 `finishedAt - startedAt`；时间顺序异常返回 `invalid`，缺时间返回 `unrecorded`，绝不显示 0 秒 |
| 运行记录扩展   | `runRecordSchema.nodeTimings`、`Run.nodeTimings`（迁移 `20260916120000_canvas_groups_run_node_timings`） | 按节点存生命周期时间，旧记录保持 NULL                                                                                    |
| 分组布局       | `canvasGroupSchema`、`canvasDocumentSchema.groups`、`Canvas.groups`（同一迁移）                          | 组只表达布局，不进入 DAG；成员必须存在且最多属于一个组；尺寸下限 `CANVAS_GROUP_MIN_SIZE`                                 |
| 空节点判定     | `apps/web/src/workspace/empty-node-rules.ts`                                                             | 纯函数；无法确定资源或运行状态时保留；`nodeHasEcho` 不参与该判定                                                         |

迁移已在本机隔离 PostgreSQL 16 容器上完整应用，`prisma migrate diff` 复核为空迁移（无残留漂移），旧数据不回填。

| 阶段          | 等级  | 交付与依赖                                                     | 关闭条件                                                        |
| ------------- | ----- | -------------------------------------------------------------- | --------------------------------------------------------------- |
| A：冻结合同   | P0    | 请求记录、计时、配置继承、分组存储、空节点规则；选择摘要策略   | 字段与权限有依据，迁移/备份/回滚可执行，关键反例转成测试清单    |
| B：请求与耗时 | P0/P1 | 请求前留存、每节点计时、资产版本绑定、提示词 Dialog 和悬浮显示 | 四种媒体、版本切换、重试/恢复、复制链路通过；无额外隐式生成请求 |
| C：设置增强   | P0/P1 | 独立凭据保存、默认优先级、宽版设置、实例覆盖                   | 四类不同配置生效；全局不被独立 Key 改写；冻结任务和权限回归通过 |
| D：连线组合   | P1    | 两种独立偏好、五种路径、五种特效加关闭、旧偏好迁移             | 30 种组合与拖拽预览一致，减少动态效果和性能检查通过             |
| E：区域分组   | P0/P1 | 组存储迁移、界面交互、坐标、历史、剪贴板和导出                 | 本地/数据库恢复及往返测试通过，组不进入运行 DAG                 |
| F：清空菜单   | P1    | 菜单、候选规则、确认、原子历史，依赖 E 的成员规则              | 非空提示词/资源/在途任务保留，清理与撤销一致                    |
| G：联合验收   | P1    | 六项功能联动、文档同步、发布与交接                             | 必需检查通过，风险与证据完整，提交/Tag/推送核验完成             |

- [x] A 完成并记录决定：采用手动摘要，真实请求文本只读，不自动增加付费总结。
- [ ] B 完成，记录请求字节/字段对照与生命周期证据。
- [x] C 完成，独立 Key、类型默认继承、冻结配置及权限回归通过，见第 14 节。
- [x] D 完成，旧偏好迁移、30 种组合及固定规模性能比较有证据，未宣称性能提升。
- [x] E 完成，隔离数据库恢复、分叉入组、25%/100%/200% 往返有证据。
- [x] F 完成，非空提示词保留、确认竞态、在途提示、迟到事件及一次撤销通过。
- [ ] G 完成，逐项填写验收记录再关闭任务。

可并行安排独立模块：Provider/Worker 负责请求与计时，设置模块负责表单和凭据，画布模块负责连线/分组/清空。`packages/domain/src/index.ts`、`apps/web/src/App.tsx`、`apps/api/src/app.ts`、`prisma/schema.prisma` 等共享文件由主执行者整合或安排串行交接，禁止多方同时编辑。每个子任务报告修改文件、验证结果和未完成项。

## 10. 验证、迁移与回滚

### 10.1 实施阶段检查

先按模块运行现有专项并补业务反例，再执行全仓库检查：

```powershell
pnpm lint
pnpm typecheck
$env:WEB_PORT = '5173'
pnpm test
pnpm build
pnpm --filter @multimodal-canvas/web test:e2e
```

`WEB_PORT=5173` 用于现有 API 测试的默认配置。浏览器/集成测试使用独立端口和明确的 `TEST_*` 配置，先检查占用，不覆盖用户正在运行的开发服务。服务断连后检查进程、日志、Git 状态和本文件检查点，再从最后成功步骤恢复。

- 单测：Domain 新结构与旧数据解析；请求字段留存；计时口径；配置继承；五类路径与特效组合；分组坐标和历史；空节点分类。
- 集成：内存/文件/Prisma 存储一致；任务恢复和资产版本关联；独立凭据创建不激活全局；迁移、导入导出和 revision 冲突。
- 浏览器冒烟：每项需求至少一条完整用户路径，并加入“设置图片独立 Key -> 建组 -> 输入提示词生成 -> 看摘要/全文/耗时 -> 移组 -> 清空空节点 -> 撤销 -> 刷新”的联合流程。
- 视觉：三个 PC 视口、浅/深主题、25%/100%/200% 画布缩放、长模型 ID、长摘要、长提示词、多个组选区。截图检查文本/菜单/端口/缩放点不重叠，控制台没有新增错误。
- 性能：使用固定的合成节点/连线数量记录改动前后数据，重点看动画、大组移动、计时更新和 Dialog 打开；无测量不宣称优化。
- 实际供应商：本计划和 Mock 验收不产生真实请求。只有获得具体 Provider、精确模型、输入与次数的授权后再执行真实生成；保存已知任务 ID，不因超时重复付费 POST。

测试含 skip/TODO 或设施缺失时分别记录，不能称作该项集成验收通过。请求记录及 Key 路径应覆盖敏感信息泄露反例，但不得使用真实 Key 作为测试夹具。

### 10.2 影响与回滚

| 改动               | 影响与迁移                                                               | 备份和回滚要求                                                                                      |
| ------------------ | ------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------- |
| 生成说明、计时     | Run/Worker/资产版本及 API 读取字段；优先可选版本化扩展，旧数据不伪造回填 | 迁移前保存数据库/本地持久化快照；回滚 UI 可隐藏新字段，后端降级需证明不丢说明与时间                 |
| 独立凭据、默认设置 | 新增不激活的凭据操作；保留旧字符串默认读取与现有加密/历史版本            | 备份受控凭据存储及所需加密材料，不写入仓库；回滚保留新建凭据，不静默重新激活已删 Key                |
| 连线偏好           | 浏览器旧偏好映射到两字段；不改变业务图                                   | 保留明确旧值映射和默认值；降级最多影响外观，不丢节点/边                                             |
| 分组               | 新增 Canvas 持久化字段，三种存储和导入导出均需升级                       | 先在隔离数据库迁移和往返验证；旧客户端保存可能抹去组，升级后须版本门禁/拒绝不兼容写入，不能直接混用 |
| 清空操作           | 修改当前画布文档和关联边/成员                                            | 执行前确认，单次撤销；资源库和运行历史独立保留；本轮不会执行任何真实清空                            |

分组的回滚优先禁用新建/编辑组并继续保留后端组数据；必须降级到完全不识别组的版本时，先导出布局，再按明确方案解除分组且保留媒体节点绝对坐标。不得用旧客户端重新保存后“看起来正常”作为数据无损证明。生产迁移、批量删除和数据覆盖另行确认。

### 10.3 Git 与完成条件

- 每阶段更新本文件的实际完成项、证据、最后成功命令和下一步。未通过的任务保持未完成，不用口头状态代替代码和测试。
- 代码/公开类型/复杂逻辑按项目约定补中文文档注释；参数、单位、返回值、错误及副作用与实现一致。
- 合并前检查完整 diff、Git 状态、凭据/调试输出、无关格式化和生成文件；保留用户已有修改。
- 本轮仅新增计划文档，属于小改动：检查通过后创建中文 Conventional Commit，推送当前已配置上游，不创建空提交或版本 Tag。
- 后续六项整体实现涉及跨模块、公开结构与数据库，属于大改动：验证完成后按仓库版本约定创建带中文说明的 annotated Tag，并推送目标分支和 Tag；不把本轮文档提交当成实现发布。
- 提交正文记录目标、影响、验证、兼容和剩余风险；推送前核对远程 URL/分支/上游，推送后用远程引用核验。不强推，不为解决冲突丢弃定制行为。

## 11. 体验建议与待讨论选项

以下是方案建议，不会在本轮变成未授权实现，也不会替代上文六项核心验收。

| 建议                                 | 收益                                                         | 建议安排                                |
| ------------------------------------ | ------------------------------------------------------------ | --------------------------------------- |
| 摘要放 Dialog 第一块，标题保持短     | 长句能完整阅读，两份复制入口清楚                             | 作为默认方案                            |
| 耗时放悬浮卡片                       | 不挡画面和右下角缩放点，四类节点一致                         | 作为默认方案                            |
| 摘要允许修改，真实提示词只读         | 用户可起有意义的摘要，但不破坏生成来源证据                   | 随提示词功能实现                        |
| 自动语义总结使用单独开关             | 明确额外模型调用；全文查看不依赖总结成功                     | 开工前确认本地/手动与自动总结的默认策略 |
| 连线选择旁显示组合预览               | 调整路径和特效时能直接看到最终效果                           | 随连线功能实现                          |
| 清理前显示候选数量，可定位候选       | 避免误删，也便于检查为什么某节点被保留                       | 数量属于首版；逐项定位可后置            |
| 设置显示“继承自项目/全局/节点独立”   | 解释当前模型和 Key 为什么生效，减少配置疑惑                  | 随设置功能实现                          |
| 组标题显示成员数量，默认解散保留节点 | 组移动范围直观，降低误删风险                                 | 随分组功能实现                          |
| 保存一份脱敏生成参数用于复用         | 后续可从旧结果恢复模型/提示词/参数，但不承诺完全复现随机输出 | P2 独立待办，不扩入首版                 |

已解决的歧义：清空空节点保留有内容的提示词。其余建议可调整，但摘要生成方式、默认配置继承和存储兼容等涉及合同的决定，必须在对应阶段开始前写回本文件。

## 12. 本轮检查点

- [x] 核对分支、工作区、运行时、本地依赖和适用说明。
- [x] 阅读现有 TODO、检查点、六项需求相关实现及测试。
- [x] 设置与画布模块完成独立只读核查，结果已纳入本计划。
- [x] 用户确认只删除内容为空的提示词节点，已更新第 2、8 节。
- [x] 运行 8 个 Web 专项测试文件，123 项通过。
- [x] 完成根目录 `next.md` 的详细计划，六项业务功能仍待实施。
- [x] `pnpm exec prettier --check next.md` 通过，20 处本地链接均存在；已检查完整内容、六项需求覆盖和凭据模式。

文档提交前继续执行暂存差异与空白检查；提交和推送到 `origin/codex/generate-to-new-node` 后核对远程引用，实际提交 ID、推送结果和最终工作区状态记录在本轮交接中。

最近成功的业务检查命令：第 1 节 Web 专项测试命令，8 文件 / 123 项通过。本轮未重跑全仓库 lint/typecheck/test/build、浏览器或数据库验收；文档改动使用 Markdown 格式、路径与差异检查，不宣称业务实现已完成。

以上为计划编写时的历史检查点。当前执行入口见第 13 节；现有供应商合同及生产验收未完成项继续保留。

## 13. 2026-09-17 联合验收检查点

任务级别：P1；目标是验证第 3～8 节已有实现，修复已复现的验收阻断缺陷，并保存可复现证据。生产迁移、真实付费请求、新供应商合同与手机布局不在本轮范围。

- 起点：`codex/generate-to-new-node @ d02930e`，工作区干净，跟踪 `origin/codex/generate-to-new-node`。
- 环境：Node v24.12.0、pnpm 11.19.0，使用已有本地依赖；没有安装或升级依赖。
- [x] 基线 `pnpm lint`、`pnpm typecheck`、`WEB_PORT=5173 pnpm test` 通过。普通测试仍有设施跳过，不计入对应集成验收。
- [x] 专用设施 `mc-acceptance-test-next-20260917` 启动，PostgreSQL/Redis/MinIO 使用 19432/19379/19900/19901 回环端口；保留原开发容器和数据。
- [x] 18 个迁移应用完成，schema 无漂移；API 隔离设施 38 项和 Redis 9 项通过，均零跳过。
- [x] HTTPS 验收 22 项通过，均零跳过。修复夹具中 Prisma 自动读取根 `.env` 导致 JWT 配置进入隔离子进程的问题。
- [x] 整组剪贴板调用补传组数据，应用级回归 41 项通过。
- [x] 完成 Worker 归档后请求记录补绑定的恢复回归与 Prisma 持久化验证；隔离 PostgreSQL 下 Worker 239 项、API 专项 104 项通过，均零跳过。
- [x] 修复整组移动累计偏移、嵌套提示词弹层、分组与工具栏层级、结果覆盖节点尺寸和动态视频端口连线显示。
- [x] 完成浏览器测试修订及六项专项；全量 79 通过、2 跳过、0 失败，专项 11 项已包含在内。
- [x] 检查 1366×768、1440×900、1920×1080 明暗主题截图共 18 张；所测正文对比度至少 4.5:1，控制台和页面无错误。
- [x] 最终 lint、typecheck、test、build 均通过。普通测试仍有设施跳过，build 保留 Vite 大包警告，详见验收记录。
- [x] 对照第 3～8 节记录未实现和未证明条件，六项总体不予通过，阶段 G 保持未关闭。
- [x] 独立内存 Mock 预览完成登录、节点编辑、分组、保存及刷新冒烟，无页面运行时错误；入口为 `http://127.0.0.1:5184`。

交付使用当前上游分支及 annotated Tag `v2026.09.17-six-feature-acceptance-fixes`，代表本轮验收修复快照。提交前执行文档格式、链接、差异与敏感信息检查；提交和远程引用核验结果在任务交接中报告。

剩余功能：整体摘要创建/编辑；提示词及耗时跟随当前资产版本；内存模式请求记录写入；项目/全局类型默认继承；运行时服务端时钟偏移；分叉节点继承分组；清空在途任务提示和确认竞态。尚缺完整联合流程、全部缩放组合、性能前后比较和真实供应商证据。本轮不扩大为这些功能的完整实现。

最近成功业务命令：`WEB_PORT=5183 pnpm --filter @multimodal-canvas/web test:e2e --output=../../test-results/next-e2e-verified --reporter=line`，79 通过、2 跳过；隔离设施最终日志为 `test-results/next-final-integration.log`。完整命令、测试范围、兼容与回滚说明在验收记录中汇总。后续从剩余功能清单继续，不能关闭阶段 G。

## 14. 功能补齐与最终检查点

用户要求继续修复未通过项，本阶段由审计转为完整实现。P1 主目标仍为六项 PC Web 流程；起点 `56739d1`、同一分支及上游，起始工作区干净。环境与依赖沿用第 13 节，没有依赖升级或新增迁移。

- [x] 手动摘要创建、编辑、清空、失败重试和双复制；真实请求文本只读，不增加自动总结调用。
- [x] API 按资产精确版本读说明和修改摘要，权限校验先于记录查询；历史冻结输入明确标注，手动新版本和错误资产不冒用旧输入或耗时。
- [x] 内存服务记录实际请求、绑定归档版本及节点时间。明确 4xx 拒绝记 failed，网络/5xx 记 unknown；已有平台任务身份记 sent，不自动重发。
- [x] Web 按当前结果版本读取说明；新任务失败保留旧结果、旧耗时并显示失败原因。资源库提供版本历史，删除原节点后仍可查看、编辑摘要和预览。
- [x] 版本预览期间暂时隐藏提示词弹层，关闭预览后恢复草稿，修复遮罩挡住关闭按钮。
- [x] 新建节点优先继承项目/类型默认，不跨模型复制旧参数。普通账号未显式选模型时由服务端解析全局默认，保持管理员凭据权限。
- [x] HTTP/SSE 提供服务端时间；Web 用单调时钟推进运行耗时，只有展开信息面板的运行节点订阅共享计时器。
- [x] 分叉节点继承来源组并扩边；清空空节点重新确认变化后的范围；清空全部提示在途任务数量与继续执行；迟到事件不复活节点或提示词弹窗。
- [x] lint、typecheck、test、build 通过；Web 861 项、API 680 项通过。普通 API/Worker 分别有 59/3 项设施跳过，不计作对应集成通过。
- [x] 隔离 PostgreSQL 5 文件 116 项通过、零跳过；主画布 49 项通过。浏览器全量 84 项通过、3 项跳过、零失败；联合流程、版本历史、三种 PC 尺寸明暗主题及分组缩放往返均通过。
- [x] `56739d1` 与当前版本固定 100 节点/99 连线/100 成员组比较已完成，帧时间 P95 33.3/33.4 ms；没有稳定提升结论，大组操作的自动化总耗时需进一步分解。
- [x] 本地真实 HTTP + 内存 Mock 完成登录、生成、请求记录、摘要保存、分组、保存刷新和资源历史冒烟；页面无运行时错误。
- [ ] 真实文字/视频成功取证：现有三次额度已用完，没有重发。追加请求已离线核对，视频显式发送 duration=4、seconds="4"；等待独立追加额度。
- [ ] 音频真实验收：当前供应商目录没有音频模型，保留 Mock/合同验证边界。

本轮没有依赖升级或数据库迁移；回滚先备份请求记录与项目数据，保留资产版本。新接口为增量扩展，旧端仍可读原记录；内存预览按进程生命周期保存数据。工作流/结果导出沿用现有格式，不导出这次新增的说明查询结果或手动摘要，导入不会伪造请求记录。

预览入口 `http://127.0.0.1:5184`，独立 Mock API `http://127.0.0.1:19301`。最近检查日志为 `test-results/next-completion-{lint,typecheck,tests,build,postgres,e2e}.log`，真实调用证据为 `test-results/next-live-provider.json`，不含凭据。交付 Tag 使用 `v2026.09.17-six-feature-completion`；分支与远程核验在最终交接中记录。阶段 B/G 的真实供应商条件保持未关闭，不能把本地通过认定为全面验收完成。
