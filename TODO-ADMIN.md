# 后台、账户与用户资源管理 TODO

更新时间：2026-09-06。状态：需求讨论稿，尚未开始功能开发。

## 本轮目标与检查点

本轮只完成现状诊断、需求拆分和验收方案。后续与用户讨论确认后，按阶段实现 `/admin`、账户管理、用户资源隔离和全站体验改进；不能把文档完成标为功能完成。

- [x] 读取全局 AGENTS.md、现有认证 TODO、部署文档及相关代码；仓库没有 README.md 或额外的项目级 AGENTS.md。
- [x] 基线：`main @ b9157e1`，初始工作区干净；上游 `origin/main`，另配置 `gitee`。
- [x] 运行时与依赖：Node `v24.12.0`、pnpm `11.19.0`；已有 `pnpm-lock.yaml` 和本地依赖，无需安装。
- [x] 运行状态：既有 Compose 七个常驻服务 healthy；Web 入口 `http://localhost:8080`。
- [x] 只读确认桌面 `email.txt` 存在；未读取凭据、未导入配置、未发送邮件。
- [x] 定位用户头像误触退出、检查现有角色与资源归属基础。
- [x] 运行现有认证/资源基线及浏览器冒烟，具体结果见文末。
- [x] 完成三份讨论稿格式、链接、清单数量、敏感信息和 Git diff 检查；126 个唯一任务编号、13 个本地链接有效，未发现真实凭据。
- Git 交付：本轮文档按小改动提交到 `main` 并推送 `origin/main`，不创建发布 Tag；实际完成状态以提交和远程引用核验为准，不部署应用或执行数据迁移。
- [ ] 与用户确认文末产品决策，然后再进入实现阶段。

交接阅读顺序：本文件、[UI/UX 与全站质检清单](docs/admin-ui-ux-review.md)、[首页视觉、动画与鼠标特效 TODO](docs/homepage-ui-motion-todo.md)、[现有认证入口记录](TODO-AUTH-ENTRY.md)、当前 Git 状态及最新验证结果。中断后从最近一次已验证状态继续。

## 已核实的问题与现有基础

| 项目             | 当前证据                                                                                                                                                  | 对方案的影响                                                                         |
| ---------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------ |
| 头像点击直接退出 | `apps/web/src/App.tsx:2293` 的已登录 `UserCircle` 绑定 `onLoggedOut`；`:3047` 的 `handleLogout` 调用注销接口，并在 finally 中清查询缓存、置匿名和返回首页 | 确定的 P0 交互缺陷，需要账户菜单和单独的退出命令；网络失败时不代表服务端撤销已成功   |
| 测试固化错误流程 | `apps/web/e2e/smoke.spec.ts:731`、`:770` 直接点击该“退出登录”按钮作为前置步骤                                                                             | 现有用例通过不代表头像体验正确，需新增预期行为的回归                                 |
| 会话到期处理     | `apps/api/src/auth-service.ts:32` 默认访问令牌有效期 15 分钟；`apps/web/src/auth-client.ts` 在 401 时清会话，未发现会话续期 API                           | 属于另一项会话体验风险，不作为本次头像误退出的原因；需验证长时间运行、续期和草稿恢复 |
| 后台路由         | `apps/web/src/routing/routes.ts` 没有 `/admin` 或个人中心路由                                                                                             | 增加真实路由、深链、刷新回退和权限状态                                               |
| 用户与角色       | `prisma/schema.prisma` 已有 `USER`/`ADMIN`、邮箱、昵称、密码哈希和 `AuthSession`                                                                          | 复用现有认证，不新建平行用户体系；补充状态、验证和管理能力                           |
| 既有管理员入口   | `scripts/docker/admin.mjs` 提供将已注册账户提升为管理员的受控命令                                                                                         | 新增首次 Web 初始化，并兼容已有管理员；不重复初始化或重建账户                        |
| 注册行为         | 当前认证 API 覆盖注册、登录、当前用户、退出及退出全部会话；注册直接创建普通用户并签发令牌                                                                 | 现有输入校验不能替代邮箱所有权验证；新增邀请/激活流程                                |
| 资源归属         | 已有 `Project.ownerId`、`Asset.ownerId`、`UploadSession.ownerId`、`Run.userId`；部分归属字段允许为空                                                      | 需要完整历史盘点与归属策略，不能仅按前端列表过滤                                     |
| 管理员资源范围   | `apps/api/src/app.ts:2878` 的项目范围及 `:2883` 的资源范围目前仍按当前用户过滤，管理员角色并不自动获得所有资源                                            | 新增受控跨用户管理范围及分组 API，不能只增加前端菜单                                 |
| 资源分组信息     | `packages/domain/src/index.ts:87` 的资源契约和 `apps/api/src/assets.ts:1068` 的映射没有后台需要的完整 owner/project/date 字段                             | 增加经过权限约束的管理 DTO，保留现有画布契约兼容性                                   |
| 导航动画         | 主菜单已有淡入和滑入；`AppNavigation.tsx` 关闭时直接卸载，页面框架没有统一出入场机制                                                                      | 补齐退场生命周期、连续切换及减少动态效果支持                                         |

以上结论基于当前源码和既有模拟 API 测试。尚未做真实管理员初始化、真实邮件投递或新增后台的验收；不存在“全站已质检通过”的结论。

## 页面与导航草案

所有以下 URL 均为拟新增页面。管理后台固定入口为 `/admin`，账户与个人资源使用独立入口，权限由服务端执行。

| 页面                   | 建议 URL                                 | 访问者                                               | 核心内容                                                                     |
| ---------------------- | ---------------------------------------- | ---------------------------------------------------- | ---------------------------------------------------------------------------- |
| 后台入口：首次初始化   | `/admin`                                 | 系统尚未初始化时显示表单，由部署者完成身份核验后提交 | 直接让用户配置并创建管理员，成功后持久化初始化完成标记                       |
| 后台入口：初始化完成后 | `/admin`                                 | 管理员；未登录先登录                                 | 用户数量、待验证用户、资源分布、失败任务摘要、待处理事项；不再显示初始化表单 |
| 个人信息               | `/account/profile`                       | 已登录用户，包含管理员                               | 头像、昵称、个人简介、邮箱绑定状态及修改入口                                 |
| 账户安全               | `/account/security`                      | 已登录用户                                           | 修改密码按钮、绑定/更换邮箱、会话列表、退出其他会话                          |
| 用户管理               | `/admin/users`                           | 仅管理员                                             | 创建/邀请普通用户、搜索筛选、状态、编辑入口                                  |
| 用户详情               | `/admin/users/:userId`                   | 仅管理员                                             | 基本资料、验证状态、项目、资源、任务与操作记录                               |
| 全站资源               | `/admin/resources`                       | 仅管理员                                             | 先展示用户分组及数量，再进入指定用户；禁止默认平铺混合所有资源               |
| 指定用户资源           | `/admin/users/:userId/resources`         | 仅管理员                                             | 该用户的上传、生成、派生和历史版本，支持项目/类型/状态筛选                   |
| 我的资源               | `/resources`                             | 已登录用户                                           | 只显示自己的所有项目及个人资源，上传与生成统一进入                           |
| 我的任务               | `/runs`                                  | 已登录用户                                           | 自己的运行状态、失败原因、结果及返回画布入口                                 |
| 全站任务               | `/admin/runs`                            | 仅管理员                                             | 按用户/项目查看任务，定位失败，不自动重试收费请求                            |
| 操作记录               | `/admin/audit`                           | 仅管理员                                             | 操作者、对象、动作、时间、结果与脱敏变更摘要                                 |
| 邮件与系统状态         | `/admin/settings/email`、`/admin/system` | 仅管理员                                             | 脱敏配置状态、邮件失败与重试、存储/队列健康                                  |

用户已明确的规则：**第一次进入尚未初始化的 `/admin`，该 URL 直接显示“初始化并创建管理员”的页面，让用户配置创建；成功后，后续进入 `/admin` 不再要求初始化。** 不要求先找到或手动输入另一个设置 URL。

`/admin` 状态规则：未初始化显示设置页面；建议提交时采用部署者身份核验，具体保护方式待讨论，避免公开站点被抢占。提交成功后，在同一事务中保存管理员与服务端一次性完成标记（例如 `initializedAt`）。之后未登录显示登录，管理员进入后台，普通用户被拒绝并可返回个人工作区。换浏览器、清缓存、服务重启、重复访问都不能重新显示初始化。

初始化完成标记独立于管理员数量，不得用“当前没有管理员”自动重新开放初始化。已有管理员的旧部署在受控升级中认定已初始化；管理员丢失或被禁用使用独立恢复流程。填写中或提交失败不算完成，允许用户重进 `/admin` 继续；并发成功只能有一次。

个人头像行为：匿名点击打开登录；已登录点击打开账户菜单，提供个人信息、账户安全、我的资源、管理员后台（仅管理员）和独立的“退出登录”。点击头像、关闭菜单及页面切换均不得清除登录状态。

## 权限与数据边界

| 能力                                   | 普通用户             | 管理员                                        |
| -------------------------------------- | -------------------- | --------------------------------------------- |
| 查看/编辑个人资料                      | 自己                 | 自己及其他用户的可编辑资料                    |
| 创建普通用户、禁用/恢复用户            | 不允许               | 允许，保留审计                                |
| 设置密码                               | 校验后修改自己的密码 | 可发起他人重置，不读取或回显任何人的密码/哈希 |
| 修改绑定邮箱                           | 验证本人身份与新邮箱 | 允许发起更换；所有权验证不得静默跳过          |
| 上传/生成/预览/下载/导出资源           | 仅自己的授权数据     | 全部用户，明确当前用户分组和操作对象          |
| 修改资源名称、标签、元数据、归档状态   | 自己                 | 所有用户；修改历史内容需要版本与引用检查      |
| 改资源归属、迁移用户数据               | 不允许               | 单独的显式管理操作，校验关联数据并记录审计    |
| 查看全站审计和邮件设置                 | 不允许               | 允许，敏感字段始终脱敏                        |
| 查看密码、SMTP 密钥、Provider 密钥原文 | 不允许               | 不回显；只提供受控更新/轮换能力               |

“编辑所有信息和资源”按业务可编辑字段定义，详细字段白名单在实现前落盘；标识、创建时间、审计记录和密码哈希不能作为任意编辑表单。管理员的跨用户操作必须记录操作者与资源所有者，代操作不得把资源归属改成管理员自己。

服务端统一校验列表、详情、计数、搜索、上传完成、内容下载、缩略图、历史版本、资源引用、运行事件、导出和短效访问链接。普通用户伪造 `ownerId`、`userId`、`projectId` 或直接调用管理 API 均不能扩大权限；分页总数、候选建议及错误响应也不能泄露他人数据。

内容访问令牌及对象存储预签名链接可能独立于登录会话继续有效。禁用、归属变更和权限撤回后的失效窗口与撤销方式必须分别定义和验收；撤销 AuthSession 不能直接宣称所有已签发链接立即失效。

## 功能 TODO

状态：`[ ]` 表示尚未实现或未验收。每次只推进一个主要目标；P0 完成后再扩展 P1/P2。

### P0：账户与权限正确性

- [ ] **P0-AUTH-01 头像账户菜单**：统一画布和普通页面入口；头像只开菜单，独立退出才调用注销接口；保留项目路由与当前运行。
- [ ] **P0-AUTH-02 会话恢复**：确认访问/续期策略、闲置与绝对期限、跨标签同步；普通业务请求临时网络失败不注销，旧会话晚到的 401 不能清除新会话；真正失效保留所属用户的安全草稿，重新认证后不自动重放创建/生成 POST。
- [ ] **P0-ADMIN-03 后台路由权限**：实现 `/admin` 路由和服务端授权，覆盖刷新、深链、浏览器前进后退、401/403/404 与登录后返回目标页。
- [ ] **P0-ADMIN-04 首次管理员初始化**：首次进入未初始化的 `/admin` 直接显示配置创建页面；按讨论确认的部署者核验方案提交，以数据库事务/锁保存管理员和独立完成标记。成功后永不因换浏览器、重启或管理员数量变化再次显示初始化；并发/重放只能成功一次。
- [ ] **P0-ADMIN-05 现有账户接续**：先检查是否已有管理员和待使用的普通账户；明确是验证后提升指定账户还是创建新账户，不把第一个注册者自动提升或覆盖已有资料。
- [ ] **P0-VERIFY-06 邮箱验证**：创建/邀请账户必须验证邮箱所有权；一次性令牌只存哈希，绑定用途/用户/目标邮箱，限制有效期、尝试次数、重发频率与原子消费。
- [ ] **P0-MAIL-07 邮件接入**：实现前私下读取桌面 `email.txt`，核对 SMTP 主机、端口、TLS、发件人与认证方式；通过服务端 Secret/受控环境配置接入，不写入源码、前端、日志、镜像或提交。
- [ ] **P0-USER-08 用户状态与后台校验**：新增邀请中/待验证/正常/禁用状态及合法流转；邮箱格式/唯一性、昵称长度、密码规则前后端一致，并用数据库约束处理并发重复。
- [ ] **P0-USER-09 身份字段白名单**：普通注册不能提交角色提升；管理接口使用显式更新字段，防止批量赋值篡改角色、归属和审计字段。
- [ ] **P0-RESOURCE-10 全链路资源隔离**：建立可复用服务端权限策略，覆盖列表及所有字节读取路径；普通用户 A/B 互访、ID 替换及分页计数必须拒绝越权。
- [ ] **P0-RESOURCE-11 历史与新增资源归属**：盘点历史上传、生成结果、文本结果、版本和派生文件；按可靠证据关联用户，未知/冲突归属进入管理员专属待归属组，不默认公开。
- [ ] **P0-RESOURCE-12 代操作的所有者与操作者**：当前生成归档已经继承 `run.userId`，直接放开管理范围会让代跑结果归操作者；开放代跑前必须冻结目标所有者和实际操作者，覆盖 Worker 归档、异步重试及任务恢复。首期是否开放代跑/归属迁移待讨论，未确认前保留管理资料、资源信息与状态的核心范围。
- [ ] **P0-SECURITY-13 改密与会话撤销**：校验当前密码或有效重置凭据，新密码服务端安全哈希；改密、禁用、角色变更后按明确策略使旧会话失效，并保护最后一个有效管理员；保护失效时走独立恢复流程，不重开首次初始化。
- [ ] **P0-DATA-14 迁移及回滚验收**：为状态、邮箱验证、初始化、审计和归属补齐迁移，先在隔离数据库及对象存储验证；记录备份、双读兼容窗口与回滚条件。

### P1：完成后台与个人工作流

- [ ] **P1-PROFILE-01 个人信息页面**：查看/编辑头像、昵称、个人简介；邮箱独立显示验证状态，保存反馈准确，失败保留输入。
- [ ] **P1-SECURITY-02 账户安全页面**：提供明显的修改密码按钮、更换邮箱、重发验证和退出其他会话；显示真实状态与错误原因，不虚报成功。
- [ ] **P1-USERS-03 用户列表**：搜索邮箱/昵称，按状态筛选，稳定分页排序；清晰区分创建成功、等待验证与发送失败。
- [ ] **P1-USERS-04 创建普通用户**：管理员填写资料并发送激活邀请，收件人验证并设置密码后激活；账号创建和邮件任务幂等，禁止双击重复创建。
- [ ] **P1-USERS-05 用户详情和编辑**：集中展示资料、项目、资源、任务、会话状态；可编辑字段与操作反馈完整，敏感变更留审计。
- [ ] **P1-RESOURCES-06 管理员分用户资源库**：默认用户分组视图，组内包含上传和生成结果；用户切换有明确标题和空态，不把所有资源混成一组。
- [ ] **P1-RESOURCES-07 我的资源库**：普通用户查看自己跨项目及个人资源；支持类型、来源、项目、标签、时间、归档状态筛选与分页。
- [ ] **P1-RESOURCES-08 预览与详情**：文本、图片、音频、视频可预览，显示所属用户、来源、项目、时间、大小、版本和引用位置；下载需重新鉴权。
- [ ] **P1-RESOURCES-09 资源管理闭环**：重命名、标签、版本、归档/恢复、导出可用；删除前列出引用与运行影响，永久删除和批量覆盖另行确认。
- [ ] **P1-RUNS-10 任务中心**：支持用户范围、运行中/失败筛选、详情、结果与回画布；重试显式操作并复用已知任务身份，避免重复收费。
- [ ] **P1-AUDIT-11 操作记录**：记录用户创建、禁用、资料/邮箱变更、改密、资源编辑/归属变更与敏感配置更新，支持检索及权限过滤。
- [ ] **P1-MAIL-12 邮件投递状态**：区分排队、SMTP 接收、失败与用户验证完成；超时/重试/重发可追踪且不重复激活账号，队列成功不能冒充收件箱送达。
- [ ] **P1-MOTION-13 动画与导航**：侧栏、账户菜单、详情面板和页面均有进出场；保留焦点、滚动、过滤条件和正确卸载时机，支持减少动态效果；首页另按专项 TODO 完成首屏、滚动及鼠标周围/hover 目标联动特效。
- [ ] **P1-QA-14 全站质检报告**：执行配套 QA 清单并记录证据；失败项保持未完成，给出原因、范围、复测记录与剩余风险。

### P2：推荐增强，讨论后排期

- [ ] **P2-OVERVIEW-01 后台概览**：按用户汇总存储、待验证账户与异常任务，并能跳转对应筛选页；仅展示实测数据。
- [ ] **P2-SYSTEM-02 系统状态**：显示存储、队列、API/Worker 健康和邮件配置状态，错误信息脱敏。
- [ ] **P2-QUOTA-03 用量与配额**：先展示资源数量、实际占用和上游明确返回的用量；限额及计费另行讨论，不估算为真实账单。
- [ ] **P2-SESSIONS-04 登录活动**：提供可识别的会话时间、设备摘要与撤销入口，明确当前会话，遵循数据保留策略。
- [ ] **P2-BULK-05 批量操作预览**：按当前用户/筛选范围执行，展示影响数量和逐项结果；危险操作单独确认。
- [ ] **P2-RECOVERY-06 回收站保留策略**：讨论保留天数、恢复引用冲突、用户禁用后的数据保留和定时清理，再决定实现。

## 邮件与验证流程草案

建议默认“管理员创建普通用户 + 邮件激活”；现有公开注册是否保留待讨论。无论入口如何，验证前不能获得完整业务访问权限。

建议状态：管理员提交合法资料 -> 事务创建待激活账户和投递任务 -> 发送一次性邀请 -> 用户验证邮箱并设置密码 -> 原子消费邀请并激活 -> 正常登录。邮件服务失败时账户仍为待激活，并允许受控重发；不能退回到免验证创建。

邮箱更换保持旧邮箱有效，直到新邮箱验证成功并再次校验申请身份；不同用途的验证码/链接不能互用。令牌过期、错误次数超限、重复点击、旧链接、并发提交、邮箱已被占用都必须有明确结果。真实发信前确定测试收件地址和测试场景；本轮没有执行发信。

`email.txt` 只作为部署配置来源。容器不依赖 Windows 桌面路径；配置工具或部署阶段转换为受控 Secret，页面仅显示已配置状态和脱敏字段。模板和公开链接只使用已配置站点地址，禁止拼接不可信 Host 或把密钥/密码放入链接。

## 历史数据、兼容与回滚

实施前需记录当前管理员数量、账户状态、邮箱规范化冲突、无密码历史账户、各来源资源数量、缺失/冲突 owner 数量、运行归档情况及对象存储引用；仅输出统计与脱敏映射，不输出用户内容或凭据。不得以迁移默认值把现有未核验邮箱直接认定为已验证。

存量账户目前没有邮箱验证字段。启用强制验证前先确定补验证期限、受限验证入口、管理员恢复和过渡期权限；未制定这些方案前不能直接阻断所有既有账户。历史资源继续保留和隔离；账户是否暂可访问由明确的过渡规则决定，不能以数据归属回填代替邮箱验证。

优先采用新增字段和索引、兼容读取、受控回填、校验后收紧约束的顺序。既有项目授权与无 owner 历史资源存在兼容路径，调整时同时验证原普通用户仍能访问自己的历史资源；无法判定归属的数据进入管理员待处理区，不按“当前登录用户”随意认领。

备份应包含数据库、对象键映射及对象存储版本/快照。回滚必须恢复到仍保持权限边界的兼容版本；新增账户或资源发生写入后，不直接反向删字段或还原旧快照覆盖新数据。回填提供幂等运行、预演、数量对账、错误中止和可审计映射。涉及正式数据归属变更、批量删除或覆盖时另行确认具体执行范围。

## 阶段划分与交付物

1. **讨论稿**：本文件及配套清单、当前缺陷证据、选项与验收标准。
2. **P0 账户交互**：先修头像误退出和会话恢复，提交针对用户真实操作顺序的回归。
3. **P0 认证与后台入口**：完成初始化、邮箱验证、用户状态及服务端管理权限，再开放管理页面。
4. **P0/P1 用户资源**：先归属盘点和权限测试，再完成分用户资源库、历史回填与生成结果收录。
5. **P1 页面与体验**：完成个人中心、用户管理、任务/审计、动画及 UI/UX 清单。
6. **P1 全站验收**：隔离真实设施测试、桌面浏览器全流程、邮件真实投递验收及发布前回滚演练。真实 Provider 调用单列证据，不因模拟测试通过而宣称真实生成通过。

后续涉及公开 API、数据库、依赖或跨模块行为的实现属于大改动，按仓库要求提交、创建中文 annotated Tag 并推送。当前只有讨论文档，按小改动提交至已配置上游。

## 待讨论的产品决策

| 决策                    | 推荐草案                                                                                           | 其他可选方向                                                         |
| ----------------------- | -------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------- |
| `/admin` 的普通用户访问 | 后台只给管理员，个人页面放 `/account`、`/resources`                                                | 后台壳同时服务普通用户，但严格隐藏并拒绝管理功能                     |
| 第一位管理员的身份核验  | 已确定首次 `/admin` 直接显示创建页、完成后不再初始化；推荐一次性部署凭据 + 邮箱验证 + 原子完成标记 | 受控离线身份核验并接续明确的已有账户；不改变用户已确定的首访页面规则 |
| 用户创建与公开注册      | 优先管理员邀请普通用户，邮箱验证后激活                                                             | 保留公开注册，但同样强制邮箱验证                                     |
| 管理员编辑范围          | 可改业务资料和资源；密码通过重置，邮箱通过验证，所有敏感操作留审计                                 | 增加特定字段或更细权限，需要逐项定义                                 |
| 管理员代运行与转移      | 首期先完成用户/资源编辑；如开放代跑，资源归目标项目用户并记录管理员为操作者                        | 本期一并实现代跑或跨用户迁移，但先定义完整归属与引用契约             |
| 会话期限                | 短期访问令牌 + 可撤销续期机制，明确闲置/绝对期限                                                   | 固定长期会话；具体时长及安全边界待确认                               |
| 存量账户补验证          | 设置过渡期限及受限验证入口，保留数据和管理员恢复能力                                               | 受控维护窗口中统一完成验证迁移，再开启强制规则                       |
| 验证方式                | 邀请用一次性激活链接，邮箱更换用短效验证码或链接                                                   | 统一验证码；需确定过期和重发策略                                     |
| 资源删除                | 优先归档/恢复，永久删除独立流程                                                                    | 配置保留期后自动清理，需讨论引用及费用影响                           |
| 管理员人数              | 支持多个管理员但保护最后一位；普通用户创建入口默认不能提升角色                                     | 仅一位管理员，需额外设计丢失后的恢复                                 |

## 本轮验证结果与证据边界

- `pnpm --filter @multimodal-canvas/web exec vitest run src/auth-client.test.ts src/navigation/AppNavigation.test.tsx`：2 个文件、60 项通过。
- `pnpm --filter @multimodal-canvas/web exec vitest run src/pages/HomePage.test.tsx`：1 个文件、2 项通过，仅验证既有首页结构和导航。
- `pnpm --filter @multimodal-canvas/api exec vitest run src/auth-routes.test.ts src/auth-service.test.ts src/auth.test.ts src/app-guards.test.ts src/assets.test.ts --maxWorkers=1 --minWorkers=1`：5 个文件、48 项通过。
- PowerShell 设置 `$env:WEB_BASE_URL = 'http://localhost:8080'` 后运行 `pnpm --filter @multimodal-canvas/web exec playwright test -c playwright.config.ts --grep '主页进入|主菜单支持|traps login focus|匿名新建项目'`：4 项通过，使用合成会话和模拟 API，未写入真实账户或项目。
- 以上是现有行为基线；“点击头像保持登录”尚无通过证据。新增后台、邮件、迁移、跨用户管理及完整 UI/UX 清单均待实现和验收。
- 文档验证：`pnpm exec prettier --check TODO-ADMIN.md docs/admin-ui-ux-review.md docs/homepage-ui-motion-todo.md` 与 `git diff --cached --check` 通过；编号分别为功能 34、UI 16、UX 16、QA 32、HOME 16、HOME-QA 12，无重复，13 个本地链接有效，敏感信息扫描未发现匹配。
- 本轮没有业务源码、依赖或数据变更，采用文档格式、链接、清单编号、敏感信息及 staged diff 检查作为对应验证；未重跑全仓库 lint/typecheck/test/build。后续实现必须执行完整的相关验证，不能将本轮已有行为基线描述为功能修复结果。
