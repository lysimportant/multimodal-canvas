# 后台与资源管理待办

更新时间：2026-09-21。当前身份、分组和收费路线见[New API 接入计划](docs/newapi-account-integration-plan.md)，实现与验收见[检查点](docs/newapi-account-implementation-checkpoint.md)。旧独立账号、邮件验证、初始化管理员、广场和钱包已经退出，不再作为后台待实施功能。

## 保留的运行边界

- 管理员由 New API 不可变用户 ID 允许列表指定，不能按邮箱、昵称或首次注册提权。
- 头像只打开账号菜单，退出为独立操作；跨标签页与迟到响应隔离按会话身份处理。
- 资源和任务查询持续验证所有者；管理员访问使用专用接口，不隐式改变归属。
- 后台保留资源、运行、安全审计和实际服务状态；查看任务不触发自动生成。

## 未完成及后置项

- [ ] 共享/生产切换：重新清点旧用户、失主资源、任务和对象，冻结可审阅的删除/保留清单，备份并核对匹配消费者；本次仅完成隔离演练。
- [ ] P0-RESOURCE-11：历史归属不能由可靠证据判定时保留管理员专属待归属状态，不能归给首个新登录者。
- [ ] P0-RESOURCE-12：管理员代跑/归属迁移尚未开放；如需实施，先明确目标所有者、操作者、Worker 归档、重试和审计合同。
- [ ] P2-BULK-05：批量资源操作需要影响范围预览、逐项结果和危险操作确认。
- [ ] P2-RECOVERY-06：明确回收站保留天数、恢复引用冲突、账号禁用后数据保留及定时清理规则。
- [ ] 大规模资源目录、复杂移动端、长期运行及生产恢复另行验收；已有本地测试数不代表这些场景通过。

管理操作说明见[运行文档](docs/admin-operations.md)。恢复任务时先读本文件、接入检查点与 Git 状态；数据恢复不能覆盖切换后新用户作品，不能让旧 Worker 消费新授权队列。
