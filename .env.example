# 本地开发模式：不启用生产环境的强制配置校验，使用本地文件和内存运行。
NODE_ENV=development

# 本地进程内运行，不依赖 Redis 或 BullMQ。
RUN_SERVICE=memory

# 使用真实 API 适配器；URL 和 API Key 在网页设置页填写，不写入此文件。
WORKER_PROVIDER=newapi

# 不设置 DATABASE_URL：项目保存到仓库根目录 .data/projects.json。
