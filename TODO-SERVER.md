# Server 未完成任务

更新时间：2026-09-05

本文件只记录目标环境的入口、业务数据设施、遥测接收端和供应商回调验收。

状态说明：

- [ ] 尚未执行。
- [~] 已有可复用的实现或证据，仍需完成本文件列出的验收。

## P0：生产基础设施与恢复

### [ ] P0-PROD-STARTUP-02 实际生产入口与安全边界

- 验证域名证书、反向代理、入口路由、真实客户端地址传递、HSTS、告警接收端和配置回滚。
- 验证 Redis 全局限流、TLS、S3、API/Worker 启动和故障关闭行为。
- 故障演练前记录备份、流量切换窗口、回滚步骤和恢复责任人。

### [ ] P0-MEDIA-OPS-03 生产媒体处理与外部可观测性

- 验证业务 S3 bucket/prefix/IAM 的上传、下载、派生对象、权限边界、回滚和孤立对象处理。
- 验证 OTLP/Sentry 接收端的投递、脱敏、容量、超时、503、重定向和故障互不影响。
- 验证 FFmpeg/ffprobe 运行环境、非特权容器、临时目录和对象键双读或回滚约束。

## P1：Provider 回调、媒体与密钥

### [ ] P1-PROVIDER-WEBHOOK-04 视频回调与幂等

- 配置正式回调地址和签名后，完成真实视频任务的取消、Webhook、重放、异常恢复和幂等演练。
- 验证供应商对签名原始 body、时间戳、重复请求、重复计费、取消状态和安全重试的行为。
- 回调、查询、下载和归档必须关联同一平台任务身份；发送结果不明不得自动重发创建 POST。

### [ ] P1-PRODUCTION-MEDIA-05 音频与扩展字段归档

- 启用音频或扩展字段时，使用已确认的模型、voice 和字段契约完成真实请求，并验证 Worker、数据库和对象存储归档。
- 未确认的音频模型、voice、参考角色或扩展字段继续拒绝，不得静默透传或降级。
- 媒体归档、权限、回滚和外部观测统一受 P0-MEDIA-OPS-03 与 P1-PRODUCTION-09 的验收约束。

### [ ] P1-VENDOR-KEY-ROTATION-06 供应商侧多 Key 轮换

- 验证供应商后台实际撤销、轮换和失效行为。
- 验证同一真实 DAG 使用多把供应商 Key 时的冻结、幂等、失败恢复和历史任务行为。
- 演练轮换、兼容窗口、暂停写入、回滚和旧任务恢复。

### [ ] P1-PROVIDER-FIELD-E2E-07 角色与扩展字段结果验证

- 针对 negativePrompt、lastFrame、audioTrack、style、character、mask 和多参考图取得正式字段契约。
- 完成每个已确认字段的真实请求和结果验证，确认字段确实被供应商读取而非被忽略。
- 未确认的角色和参数必须继续在请求前显式拒绝，不能静默降级。

### [ ] P1-PRODUCTION-09 生产环境完整验收

- 完成 S3 上传和下载、Redis/BullMQ 队列接管、跨实例恢复、TLS/反向代理和全局限流验收。
- 完成 OTLP/Sentry 投递、Provider 回调、媒体归档、凭据轮换和故障演练。
- 记录迁移兼容窗口、回滚步骤、对象存储前缀、队列 namespace、数据备份和恢复结果。

## Server 完成判定

- 使用目标环境入口和真实网络边界产生可复核证据。
- 故障注入、配置回滚、暂停写入、Key 撤销和数据恢复均记录授权、备份、影响范围和回滚路径。
- 关闭任务时记录版本、域名或入口、配置版本、测试时间、证据位置和未覆盖风险；不得把 Secret 写入文档、日志或提交。

## 执行准备

- 域名、证书、反向代理、入口规则、告警接收端、回滚版本和操作权限。
- 数据库、Redis/S3 连接配置、IAM、遥测接收端、备份和故障演练授权。
- 供应商后台权限、至少两把可区分的测试 Key、正式回调签名配置和目标环境地址。
- 授权的维护窗口。
