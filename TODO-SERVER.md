# Server 未完成任务

更新时间：2026-09-05

本文件是目标服务器、测试或生产网络边界、公开域名、真实业务 S3/Redis/遥测及供应商回调环境的唯一待办清单。Local 前置能力与共用 P2 后置事项见 [TODO-LOCAL.md](TODO-LOCAL.md)；Local 通过不能关闭本文件的验收项。

状态说明：

- [~] 已有部分实现或部分证据，仍需完整验收。
- [!] 依赖外部平台、部署权限、正式契约或具体配置。

## P0：生产基础设施与恢复

### [!] P0-PROD-STARTUP-02 实际生产入口与安全边界

- 验证实际部署的域名证书、反向代理、入口路由、真实客户端地址传递、HSTS、告警接收端和配置回滚。
- 在实际生产网络边界验证 Redis 全局限流、TLS、S3、API/Worker 启动和故障关闭行为。
- 需要具备：生产域名、证书、反向代理配置、告警地址、回滚版本和部署权限。
- Local 的 HTTPS 代理、独立 NODE_ENV=production API 和隔离 Docker 只能作为部署前门槛，不能替代本项验收。
- 验收时不得使用生产数据做破坏性故障注入；应先确认备份、流量切换窗口、回滚步骤和恢复责任人。

### [~] P0-MEDIA-OPS-03 生产媒体处理与外部可观测性

- 验证真实业务 S3 bucket/prefix/IAM 的上传、下载、派生对象、权限边界、回滚和孤立对象处理。
- 验证生产 OTLP/Sentry 接收端的投递、脱敏、容量、超时、503、重定向和故障隔离。
- 验证生产 FFmpeg/ffprobe 运行环境、非特权容器、临时目录和对象键双读或回滚约束。
- 需要具备：生产 S3 bucket/prefix/IAM、FFmpeg 运行环境、OTLP/Sentry 地址、告警和回滚权限。
- Local 的 MinIO、回环 collector 和二进制测试不能替代真实业务桶或外部接收端证据。

## P1：Provider、媒体覆盖与多 Key

### [~] P1-VIDEO-CONTRACT-04 真实视频完整契约（Server 子项）

- 在目标部署配置正式回调地址和签名后，完成真实视频任务的取消、Webhook、重放、异常恢复和幂等演练。
- 验证供应商实际部署对签名原始 body、时间戳、重复请求、重复计费、取消状态和安全重试的行为；不能用稳定 idempotency key 推断供应商一定去重。
- 真实任务的回调、查询、下载和归档必须关联同一平台任务身份；发送结果不明时不得自动重发创建 POST。
- 前置条件：供应商正式契约、回调地址和签名配置、测试任务权限、目标部署和明确的故障演练授权。
- [TODO-LOCAL.md](TODO-LOCAL.md) 中的适配器测试和历史 legacy-v1 成功记录不能替代本项。

### [~] P1-MEDIA-COVERAGE-05 音频、参考输入与供应商扩展（Server 关联）

- 若目标部署启用音频或扩展字段，必须使用已确认的模型、voice 和字段契约完成真实请求，并验证 Worker、数据库和生产对象存储归档。
- 供应商未确认的音频模型、voice、参考角色或扩展字段继续拒绝，不得在部署时静默透传或降级。
- 生产媒体归档、权限、回滚和外部观测统一受 P0-MEDIA-OPS-03 与 P1-PRODUCTION-09 的验收约束。

### [~] P1-MULTIKEY-ROTATION-06 供应商侧多 Key 轮换

- 验证供应商后台实际撤销、轮换和失效行为。
- 验证同一真实 DAG 使用多把供应商 Key 时的冻结、幂等、失败恢复和历史任务行为。
- 在实际业务部署中演练轮换、兼容窗口、暂停写入、回滚和旧任务恢复。
- 需要具备：供应商后台权限、两把以上可区分的测试 Key、部署配置位置和回滚授权。
- [TODO-LOCAL.md](TODO-LOCAL.md) 中的应用侧密钥加密、CAS 和历史恢复通过，不能证明供应商侧撤销或生产部署轮换通过。

### [~] P1-PROVIDER-ROLES-07 真实角色与扩展字段映射（Server 最终验收）

- 针对 negativePrompt、lastFrame、audioTrack、style、character、mask 和多参考图取得正式字段契约。
- 用目标部署完成每个已确认字段的真实请求和结果验证，确认字段确实被供应商读取而非被忽略。
- 未确认的角色和参数必须继续在请求前显式拒绝，不能静默降级。
- 前置条件：目标部署正式 API 文档、模型适用范围、样例响应和测试授权。

### [!] P1-PRODUCTION-09 生产环境完整验收

- 生产 S3 上传和下载、Redis/BullMQ 队列接管、跨实例恢复、TLS/反向代理和全局限流。
- 生产 OTLP/Sentry 投递、Provider 回调、媒体归档、凭据轮换和故障演练。
- 迁移兼容窗口、回滚步骤、对象存储前缀、队列 namespace、数据备份和恢复记录。
- 前置条件：生产部署地址、数据库/Redis/S3 连接配置、证书、IAM、监控接收端、回滚版本和操作授权。
- Local Docker、Mock、历史测试和隔离测试不能替代生产证据。

## Server 完成判定

- 必须使用实际目标部署、实际入口和真实网络边界产生可复核证据；本地回环地址或本地 Node 代理不能作为生产入口证据。
- 任何故障注入、配置回滚、暂停写入、Key 撤销和数据恢复都必须先确认授权、备份、影响范围和回滚路径。
- Server 验收通过后，记录部署版本、域名或入口、配置版本、测试时间、证据位置和未覆盖风险；不得把 Secret 写入文档、日志或提交。

## Server 阻塞条件

- 生产域名、证书、反向代理、入口规则、告警接收端、回滚版本和部署权限。
- 生产数据库、Redis/S3 连接配置、IAM、遥测接收端、备份和故障演练授权。
- 供应商后台权限、至少两把可区分的测试 Key、正式回调签名配置和目标部署地址。
- 真实生产验收必须在授权的维护窗口执行；Local 证据不能解除这些条件。
