# P0/P1 修复执行清单：资源提及、Provider 与生产验收

更新时间：2026-09-04
状态：待按本清单逐项实现和验收；本文件不是完成证明。

## 1. 用途与范围

本文件收集此前讨论中容易混淆的缺口，作为可执行的 P0/P1 修复清单。它不替代下列原始计划：

- [TODO-RESOURCE-MENTIONS.md](TODO-RESOURCE-MENTIONS.md) 是通用资源 `@` 引用的产品和实现方案。
- [TODO-NEXT.md](TODO-NEXT.md) 是真实 Provider、生产基础设施和外部验收的后续计划。

`@` 搜索、选择确认、提及卡片、`promptDocument`、引用版本冻结和 Mock 闭环属于资源提及方案，**不在 `TODO-NEXT.md` 的既有待办中**。此前把它们报告为 `TODO-NEXT.md` 缺口是范围理解错误；本清单用来避免再次混报。

当前工作区已经出现 `promptDocument`、资源提及冻结和运行快照相关的未提交实现改动。它们只能视为“进行中”的代码信号；在对应测试、端到端流程和下列验收证据完成前，不能把任何任务标记为完成。

## 2. 问题澄清

| 编号 | 结论 | 是否阻塞 P0 | 是否阻塞真实 Provider 验收 |
| --- | --- | --- | --- |
| FIX-CLARIFY-01 | Mock 可以接收结构化输入，也可以回显文字、图片、音频、视频四类提及；它不是“无法输入”的原因。 | 否 | Mock 不能替代真实验收 |
| FIX-CLARIFY-02 | 真实问题是供应商字段和能力映射尚未逐项确认，例如参考图、首尾帧、角色、音轨、遮罩和多参考混合输入。 | 否 | 是 |
| FIX-CLARIFY-03 | Mock 能验证应用自身的搜索、保存、冻结、重试、顺序、重复引用和 Provider-neutral 请求构造；它不能证明供应商实际读取了媒体、接受了字段或正确扣费。 | 否 | 是 |
| FIX-CLARIFY-04 | `@` 搜索被误归入 `TODO-NEXT.md` 是跟踪范围错误，不是 Mock 或 Provider 的技术限制。 | 是，需纠正跟踪 | 否 |
| FIX-CLARIFY-05 | 没有有效测试 API Key 不会阻塞本地 Mock、领域层、API、Worker 或资源提及 P0；它只阻塞付费真实 Provider 的隔离 E2E。 | 否 | 是 |
| FIX-CLARIFY-06 | 价格不是 P0 的代码阻塞。它是实际付费请求前必须具备的预算、单次上限和测试额度约束；若供应商提供沙箱或免费额度，可替代付费额度。 | 否 | 视供应商而定 |
| FIX-CLARIFY-07 | 生产连接、跨实例恢复、S3/队列接管、OTLP/Sentry/CI 不是由 API Key 导致的单一问题；它们需要隔离部署环境、服务权限和外部平台配置。 | 是 | 部分相关 |

## 3. P0：本地可执行闭环与生产就绪基础

P0 的目标是在**不调用付费真实 Provider**的前提下，让资源提及从编辑到运行快照形成可重复验证的闭环，并把生产基础设施的本地/隔离验收边界补齐。

### P0-01：固定范围和验收边界

- [ ] 在本文件、`TODO-RESOURCE-MENTIONS.md` 与 `TODO-NEXT.md` 中保持边界一致：资源提及 UI/协议归前者，真实 Provider 与生产外部环境归后者。
- [ ] 每个后续提交都注明证据类型：`Mock`、本地隔离服务、真实 Provider 隔离凭据或生产部署；不得把前两者写成真实供应商或生产验证。
- [ ] 不因没有 Key 或测试预算阻塞 P0 开发；缺 Key 时将 P1 真实请求明确标为外部依赖。

验收：审阅三个文档时不再出现“`TODO-NEXT.md` 缺少 `@` 搜索”或“Mock 证明真实 Provider”的表述。

### P0-02：结构化提示词与兼容层

- [ ] 完整实现并测试版本化 `PromptDocument`、文字块和资源提及块；同一文档内拒绝重复 `mentionId`、非法媒体类型和无效字段。
- [ ] 明确并测试优先级：存在 `promptDocument` 时只以它作为执行来源；没有它时才从旧 `prompt` 生成纯文本兼容文档。
- [ ] 纯文本渲染只用于预览或旧接口兼容，不能把 `@标签` 当作资源身份；资源身份必须使用 `assetId` 和版本。
- [ ] 保存、复制、刷新、导入和导出保留块顺序、重复引用、标签与绑定；缺失或无权限资源创建不可执行占位并给出逐项诊断。

验收：领域单元测试覆盖有效文档、重复 `mentionId`、旧 `prompt` 回退、普通 `@` 文本不被自动识别为提及，以及导入缺失资源的诊断。

### P0-03：桌面端 `@` 搜索与编辑器

- [ ] 将普通 `TextPromptEditor` 替换或封装为所有可编辑提示词节点共用的资源提及编辑器。
- [ ] 输入 `@` 后按当前项目权限查询资源；支持名称、别名、媒体类型、标签和已引用资源分组。
- [ ] 支持键盘上下选择、Enter/点击确认、Esc/点击外部取消，以及提及卡片的删除、替换、详情查看和重复引用。
- [ ] 保持中文输入法组合态、粘贴、撤销/重做和光标位置；首轮只验收 PC Web 工作台，移动端复杂交互后置。

验收：浏览器 E2E 覆盖搜索、确认、取消、四种媒体卡片、同资产重复引用、删除中间提及、保存后刷新恢复。任何未确认的搜索结果都不得写入节点数据或发送到 Provider。

### P0-04：资源权限、版本冻结与 Mock 运行闭环

- [ ] 在保存和提交运行时校验项目归属、访问权限、归档状态、MIME、大小和资产版本；失败必须在任何 Provider 请求之前返回 `RESOURCE_MENTION_*` 结构化诊断。
- [ ] 将每个提及以独立记录写入运行快照，冻结 `assetId`、`assetVersion`、媒体类型、块顺序、标签和绑定；同一资产重复引用也必须分别保留。
- [ ] 重试、队列恢复和 Worker 重启只读取快照版本，不能重新解析“最新版本”。
- [ ] Mock Provider 接收并回显所有已解析提及，覆盖文字、图片、音频和视频节点，以及顺序、重复引用和删除后的结果。
- [ ] Mock 结果和诊断明确标记为模拟，不得作为供应商媒体生效或计费成功的证据。

验收：API/Worker/Mock 集成测试和至少一个浏览器 E2E 同时断言：编辑器显示的提及、API 冻结的版本、运行快照和 Mock 收到的数据完全一致；不支持、无权限、归档、MIME 不符和版本缺失均未发起 Provider POST。

### P0-05：隔离基础设施、跨实例恢复与对象存储

- [ ] 在明确隔离的 PostgreSQL、Redis/BullMQ、S3 兼容对象存储环境中验证迁移、重启、跨实例读写、队列接管和运行快照恢复。
- [ ] 验证资源提及冻结版本能由 Worker 在恢复后解析，且不会访问错误项目、更新到最新版本或泄露签名 URL、媒体内容、API Key。
- [ ] 覆盖生产启动 fail-closed：缺少数据库、队列、对象存储或必需密钥时，API/Worker 不应带着不完整配置对外服务。

验收：至少两个独立 API/Worker 进程在隔离环境完成一次排队、接管、重启和归档链路。该证据标为“隔离环境”，不得写成已验收生产环境。

### P0-06：媒体处理、可观测性和 CI 基础

- [ ] 在隔离部署验证 FFmpeg/ffprobe 的图片缩略图、视频 poster、音频波形和失败分支。
- [ ] 配置并验证 OTLP/Sentry 的脱敏错误事件；外部投递失败不影响主运行流程。
- [ ] 在 GitHub Runner 或等效隔离 CI 运行格式化、lint、类型检查、单元测试、集成测试和 Mock E2E。

验收：失败的资源提及或媒体任务可按请求 ID 追踪，但遥测、日志和 CI 输出不含原始媒体、签名 URL、Bearer token 或 API Key。

## 4. P1：真实 Provider 契约、费用边界与多 Key 恢复

P1 的目标是让每个真实 Provider 的“声明支持”变成可审计的实际请求证据。P1 不能用 Mock、静态代码或猜测的字段名代替。

### P1-01：能力矩阵与付费前预检

- [ ] 为每个模型记录 `mediaTypes`、`mentionMediaTypes`、`semanticRoles`、`maxMentions`、`supportsMixedMentions`、节点模式和版本来源。
- [ ] 在提交前按节点媒体类型、模式、模型、提及媒体、语义角色、顺序和数量输出逐项诊断。
- [ ] 能力缺失、未知或组合不支持时，真实付费 Provider 一律 fail-closed；不能静默丢弃、降级为文字，或只发送第一项资源。
- [ ] Mock 预览路径可按明确规则继续，但结果必须显示模拟性质与未验证能力。

验收：测试证明不支持的组合在付费 POST 之前失败，并包含节点、`mentionId`、资源 ID、媒体类型、语义角色、模型和请求 ID，且不包含秘密或可访问 URL。

### P1-02：确认真实 Provider 输入映射

- [ ] 对每个已接入供应商获取正式 API 文档、版本、测试项目和可用的隔离凭据。
- [ ] 分别确认 `prompt`、`negativePrompt`、`content`、`style`、`character`、`firstFrame`、`lastFrame`、`audioTrack`、`transcript`、`mask` 的真实字段、允许媒体、数量上限和组合限制。
- [ ] 确认图片参考、音频参考、视频参考、多参考图、混合媒体、角色绑定和节点模式的实际语义；没有契约的角色保持不支持，不可自行假设字段。
- [ ] 对每一项已确认映射增加 Provider 契约测试，断言结构化提及进入正确供应商字段，而非仅渲染成纯文本。

验收：每种支持组合都有一份可复现的隔离请求/响应证据与测试；每种未支持组合都有明确的不支持诊断。供应商实际是否读取媒体，应通过可观察结果或供应商提供的回显/日志验证。

### P1-03：测试 Key、价格和费用控制

- [ ] 由平台或项目管理员提供仅用于隔离项目的有效 Key，使用部署密钥注入，不写入源码、测试夹具、日志或导出文件。
- [ ] 在执行任何付费测试前确认模型价格、币种、最小计费单位、测试预算、单次成本上限和停止条件；若有沙箱/免费额度，优先使用。
- [ ] 将实际消耗、供应商请求 ID、内部运行 ID 和归档结果关联，但不得记录原始 Key。
- [ ] Key 缺失、预算未批准或价格无法确认时，保持该真实请求任务为 `[!] 外部阻塞`，继续推进其他 P0/P1 非付费项。

验收：一组最小真实请求在预先批准的费用上限内完成，价格/usage 与供应商返回或账单可核对；超预算或无 Key 时在发起请求前停止。

### P1-04：真实任务生命周期与多 Key

- [ ] 以确认过的真实契约验证创建、轮询或 Webhook、原始 body 签名/重放保护、取消、失败、重试、下载、归档、去重和 usage 字段。
- [ ] 验证两把或多把凭据轮换：新任务冻结新版本，历史运行和队列恢复只使用其冻结版本。
- [ ] 明确撤销凭据后已排队任务的策略，并在真实队列/跨进程恢复时验证 fail-closed 或批准的替代策略。
- [ ] 验证供应商侧幂等、扣费与内部 idempotency key 的对应关系，避免恢复或重试重复扣费。

验收：隔离环境中完成轮换、撤销、重启和恢复场景；导出、日志和诊断不含原始 Key，且每次恢复均能解释使用的凭据版本与费用结果。

### P1-05：生产部署外部验收

- [ ] 在用户明确批准、与真实生产数据隔离的部署环境执行生产 S3 上传/下载、Redis 队列接管、跨实例恢复、TLS/反向代理、全局限流、OTLP/Sentry 投递和 CI Runner 验收。
- [ ] 设计迁移兼容窗口、回滚步骤、对象存储前缀与队列 namespace；不得以清理真实数据作为测试手段。
- [ ] 对真实 Provider 回调、凭据轮换和媒体归档做一次端到端故障演练。

验收：部署记录明确环境、时间、使用的隔离项目、回滚路径和监控证据。未获得环境或权限时保持 `[!]`，不能借本地 Docker 或 Mock 宣称生产完成。

## 5. 执行顺序与检查点

1. 先完成 P0-01 至 P0-04，使本地资源提及闭环具备完整自动化证据。
2. 并行准备 P0-05/P0-06 所需的隔离服务、遥测项目和 CI 运行环境，但不要接触真实生产数据。
3. P0 验收通过后，再执行 P1-01 和 P1-02；没有确认的能力只保留 fail-closed 诊断。
4. 仅在获得隔离 Key 与预算批准后执行 P1-03 和 P1-04。
5. 最后由部署负责人授权 P1-05；该步骤是生产验收，不能被本地测试替代。

每完成一个任务，必须在对应条目下追加：修改文件、提交 ID、验证命令及结果、环境类型、外部依赖状态和剩余风险。完成状态只能使用 `[x]`，且须同时具备实现、测试、冒烟、文档证据、`git diff --check` 和 Git 状态检查；其余情况保留 `[ ]`、`[~]` 或 `[!]`。

## 6. 当前外部依赖登记

| 依赖 | 影响任务 | 当前处理原则 |
| --- | --- | --- |
| 供应商正式字段与签名契约 | P1-02、P1-04 | 未确认即 fail-closed，不猜测字段名 |
| 隔离测试 API Key | P1-02 至 P1-04 | 缺失时只阻塞真实请求，不阻塞 Mock/P0 |
| 测试预算、价格和额度批准 | P1-03、P1-04 | 未批准前不得发起可能收费的请求 |
| 隔离部署、S3、Redis/BullMQ 与数据库权限 | P0-05、P1-05 | 使用独立 namespace、bucket、数据库和回滚方案 |
| OTLP/Sentry 项目与 CI Runner 配置 | P0-06、P1-05 | 先验证脱敏与投递失败隔离，再接入部署环境 |

## 7. 不可接受的完成判断

- “Mock 通过，所以供应商图片/音频/角色输入已经生效。”
- “没有 Key，所以 `@` 搜索、快照、重试和本地 E2E 无法做。”
- “价格未知，所以 P0 全部暂停。”
- “`TODO-NEXT.md` 没有 `@` 搜索，因此该能力不需要实现。”
- “本地 Docker 或历史测试通过，所以生产 S3、队列接管和跨实例恢复已经验收。”
