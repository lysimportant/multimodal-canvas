# 独立页面 Agent 计划

版本：0.1
状态：方案待评审，未开始实现
优先级：P0/P1，PC Web 工作台优先

## 1. 依赖和文档边界

本文件描述脱离画布的独立 Agent 页面：多轮会话、联网开关、动作可视化、搜索记录、来源引用、上下文用量、文件引用和产物操作。

公共 Agent Runtime、工具、联网安全、Provider Adapter、事件和数据基础契约统一引用 [Agent 公共底座与联网能力计划](TODO-AGENT-COMMON.md)。画布节点的输入快照、显式连线和结果回写见 [画布节点 Agent 计划](TODO-AGENT-CANVAS-NODE.md)。

## 2. 产品目标

提供一个可在浏览器运行的独立 Agent 工作台，体验类似 DeepSeek Harness，但使用项目自己的 Agent Runtime 和资产体系：

- 用户可以创建和恢复多轮 Session。
- 用户可以手动开启或关闭联网。
- 用户可以看到 Agent 当前动作，而不是只看到最终答案。
- 用户可以查看每次搜索、打开的网页、最终引用和未引用来源。
- 用户可以上传或选择文件、资产和画布结果作为引用。
- 用户可以查看上下文使用量、Token、文件片段和成本。
- 用户可以把最终文本或媒体产物保存到资产库或发送回画布。

## 3. 非目标

首版不做：

- 自动登录第三方网站。
- 自动发邮件、发帖、提交表单、下单、付款或修改外部数据。
- 任意代码执行、系统命令和未隔离的浏览器操作。
- 默认读取整个项目或整台电脑的文件。
- 无限上下文、无限搜索和不受预算约束的深度研究。
- 移动端复杂工作台。

真实浏览器点击属于 `browser_actions` 后续阶段，需要独立沙箱和逐项审批；它不等同于本计划的“搜索与读取网页”。

## 4. 页面信息架构

```text
独立 Agent 页面
├── 顶部工具栏
│   ├── Agent 选择
│   ├── 模型选择
│   ├── 联网开关/模式
│   ├── 预算状态
│   └── 停止/暂停
├── 左侧栏
│   ├── Session 列表
│   ├── 收藏和归档
│   └── 最近运行
├── 中央对话区
│   ├── 用户消息
│   ├── 流式回答
│   ├── 内联引用
│   ├── Artifact 卡片
│   └── 运行状态
├── 右侧详情面板
│   ├── 当前动作
│   ├── 搜索记录
│   ├── 来源和引用
│   ├── 上下文用量
│   ├── 文件/资产引用
│   └── 成本与 Token
└── 底部输入区
    ├── 文本输入
    ├── @资源提及
    ├── 文件上传
    ├── 发送/停止
    └── 快捷操作
```

PC 宽屏下默认显示中央对话和右侧详情；窄屏时右侧面板改为抽屉，移动端适配不阻塞 PC 版本。

## 5. Session 和 Run 语义

### 5.1 Session

Session 保存：

- 会话标题和用户归属。
- AgentDefinition 和默认模型偏好。
- 最近消息和历史摘要。
- 用户锁定的文件、来源和上下文项。
- 联网模式偏好和隐私设置。
- 创建、更新时间和保留策略。

Session 不保存某一次 Run 的中间状态；中间状态属于 AgentRun 和 Checkpoint。

### 5.2 AgentRun

用户每次点击发送都创建独立 Run。Run 冻结：

- 当前 Session 版本。
- AgentDefinition 版本。
- 模型、Provider 和凭据版本引用。
- 当前用户消息。
- 文件/资产/`@`引用。
- NetworkPolicySnapshot。
- ToolAllowlist。
- 上下文快照、预算和超时。

用户可以继续同一 Session，也可以从某个历史 Run Fork 出新的分支；原 Run 不被改写。

## 6. 联网开关和模式设计

### 6.1 主界面

顶部工具栏显示：

```text
联网：关闭 ▾
```

菜单包含：

- 关闭联网。
- 搜索与读取网页。
- 深度研究。
- 浏览器操作（灰度/后续版本）。

开关旁显示简短说明和当前搜索预算。不能把“联网”设计成仅由前端控制的视觉开关，服务端必须根据 Run 快照阻断未授权的 `web.*` 调用。

### 6.2 开启联网时的预检

用户选择联网模式后，页面显示：

```text
联网模式：搜索与读取网页
可访问：公开 HTTP/HTTPS 页面
搜索上限：5 次
页面上限：10 个
文件外发：关闭
```

首次发送或从 `offline` 切换到联网时，显示外发提示：

```text
搜索词会发送给搜索服务。
只有你明确选择的文件片段才可能发送给模型供应商。
未选择的项目文件不会自动发送。
```

### 6.3 深度研究模式

深度研究不是简单提高搜索次数，应显示：

- 预计耗时。
- 最大搜索/打开页面数量。
- 是否使用后台运行。
- 预计上下文和费用。
- 可暂停、取消和稍后恢复。

研究任务仍使用同一个 AgentRun 状态机和事件协议。

## 7. 独立页面运行流程

```text
创建或选择 Session
      ↓
选择 Agent、模型和联网模式
      ↓
输入文字、上传文件或添加 @资源
      ↓
服务端预检权限、能力、预算和外发范围
      ↓
创建不可变 AgentRun
      ↓
LangGraph 执行计划和工具循环
      ↓
SSE 推送动作、Token、搜索和来源事件
      ↓
流式展示回答和引用
      ↓
生成文本/媒体 Artifact
      ↓
保存到资产库、导出或发送回画布
```

页面刷新、浏览器休眠或 SSE 中断后，应先读取 Run 快照和事件游标，再继续订阅，不重复发送用户消息。

## 8. 当前动作可视化

### 8.1 目标

用户需要知道 Agent 正在搜索、读取文件、生成媒体还是等待审批，但不应看到完整隐藏思维链。

### 8.2 时间线

右侧“当前动作”面板显示公共事件转换后的可读摘要：

```text
运行中 00:18

✓ 解析任务
✓ 读取 2 个文件片段
✓ 生成 2 个搜索词
↻ 搜索：最新行业数据
↻ 打开 3 个网页
○ 比较来源差异
○ 整理证据
○ 生成最终回答
```

每张动作卡片显示：

- 阶段名称和状态。
- 开始时间、耗时和重试次数。
- 工具名称和短输入摘要。
- 来源/文件数量。
- Token、费用和是否可取消。
- 错误和下一步建议。

### 8.3 长任务操作

- 停止：请求取消当前 Run。
- 暂停：保存检查点，稍后继续。
- 重试步骤：仅重试失败且幂等的步骤。
- Fork：从某一步创建新 Run，允许更换模型或联网策略。

## 9. 搜索记录和来源面板

### 9.1 搜索记录

“搜索记录”面板按时间展示：

- 搜索词。
- 搜索服务。
- 时间范围和域名过滤。
- 返回结果数、打开页面数和耗时。
- 是否进入上下文。
- 是否被最终回答引用。
- 失败或被策略阻止的原因。

用户可以：

- 复制或重新执行搜索词。
- 打开原始网页。
- 固定一个来源。
- 排除一个来源。
- 仅基于选中来源继续回答。
- 查看“检索但未引用”的来源。

### 9.2 来源面板

来源卡片显示：

- 标题、域名和 URL。
- 发布时间和访问时间。
- 网页、文件、资产或画布来源类型。
- 被使用的片段、页码、章节或时间范围。
- 最终引用次数。
- 内容 Hash 和是否已更新。

回答中的引用必须是可点击的，点击后打开来源详情；文件引用应定位到页码或片段。

### 9.3 冲突和可信度提示

当来源之间有明显冲突时，页面显示“来源存在差异”，并列出双方证据。不能把搜索排名当作事实可信度，也不能在没有来源支持时生成确定性结论。

## 10. 上下文用量面板

### 10.1 总览

显示：

```text
当前模型上下文窗口：模型能力矩阵中的值/未知
已使用：18,240 tokens（估算）
预留输出：4,000 tokens
剩余可用：12,760 tokens
预计成本：...
实际成本：运行结束后显示
```

### 10.2 分类明细

```text
系统指令       1,200
当前问题       380
最近对话       3,400
历史摘要       1,100
文件内容       5,600
网页证据       6,800
工具结果       1,240
预留输出       4,000
```

必须区分输入、输出、工具结果、缓存、搜索证据和文件片段；没有供应商实际值时明确标记为估算。

### 10.3 上下文检查器

用户可以查看：

- 哪些内容实际进入了本次请求。
- 哪些文件片段被选中。
- 哪些网页只被检索但未进入上下文。
- 哪些内容被摘要、压缩或移除。
- 哪些内容被用户锁定。

支持手动移除文件/来源、锁定重要指令和清理历史摘要。

## 11. 文件和资源引用

### 11.1 输入方式

支持：

- 拖拽文件。
- 文件选择器。
- 项目资产选择器。
- `@`搜索并确认资源。
- 从画布选择节点输出。

用户选择后显示引用卡片，不立即外发文件。

### 11.2 文件卡片

卡片显示：

- 文件名、媒体类型、版本和大小。
- 解析/OCR/转录状态。
- 已使用页码、段落或时间范围。
- 是否进入当前上下文。
- 外发策略和删除引用按钮。

删除引用只将其移出当前 Run，不删除项目文件或资产。

### 11.3 文件处理链路

```text
上传/选择
  -> MIME、大小和权限校验
  -> 恶意文件扫描
  -> 文本解析、OCR 或转录
  -> 分块和索引
  -> 按权限检索相关片段
  -> 写入 ContextItem
  -> 生成页码/段落级 FileReference
```

首版建议先支持文本、Markdown、PDF 和图片 OCR；音频转录、视频字幕和复杂媒体解析可作为后续阶段。

## 12. 对话和 Artifact 体验

### 12.1 流式回答

使用公共 SSE 事件协议显示：

- 文本增量。
- 工具状态。
- 来源标记。
- 文件引用标记。
- Artifact 创建进度。

回答完成前可以停止；停止后保留已生成内容和运行事件。

### 12.2 Artifact 操作

首版支持：

- 保存文本为 Artifact。
- 保存图片、音频、视频为 Asset/AssetVersion。
- 复制文本或导出 Markdown/JSON。
- 查看完整来源和产物溯源。
- 发送到用户明确选择的画布目标。

发送到画布时必须显示目标选择器、输入角色和覆盖风险。目标已有内容时默认拒绝覆盖，不根据坐标或最近节点猜测。

### 12.3 结果分支

建议提供：

- 从当前 Run Fork。
- 只替换模型。
- 只关闭/开启联网。
- 只移除一个文件或来源。
- 重新执行失败步骤。

原始回答和来源不能被覆盖，便于比较不同模型或不同联网策略的结果。

## 13. API 和事件交互

公共接口见 [TODO-AGENT-COMMON.md](TODO-AGENT-COMMON.md)。独立页面专用接口建议：

```text
POST   /agent/sessions
GET    /agent/sessions
GET    /agent/sessions/:sessionId
PATCH  /agent/sessions/:sessionId
DELETE /agent/sessions/:sessionId
POST   /agent/sessions/:sessionId/messages
POST   /agent/sessions/:sessionId/runs
GET    /agent/sessions/:sessionId/runs
POST   /agent/runs/:runId/save-artifact
POST   /agent/runs/:runId/send-to-canvas
POST   /agent/runs/:runId/retry-step
```

创建 Run 的请求至少包含：

```json
{
  "sessionId": "session-uuid",
  "agentDefinitionId": "...",
  "agentDefinitionVersion": 3,
  "message": "请根据最新资料整理一份报告",
  "resourceReferences": [],
  "networkPolicy": {
    "mode": "search_read",
    "allowedDomains": [],
    "maxSearchQueries": 5
  },
  "modelAlias": "...",
  "toolAllowlist": ["web.search", "web.open", "file.search"],
  "budget": {}
}
```

服务端必须重新检查 Session 所属用户、资源权限、模型能力和联网策略，不能信任浏览器提交的完整文件内容或权限字段。

## 14. 数据模型建议

独立页面至少需要：

- `AgentSession`：用户、项目、标题、偏好和归档时间。
- `AgentMessage`：用户/助手消息和版本。
- `AgentRun`：一次执行、状态和快照。
- `AgentStep`、`AgentEvent`、`AgentCheckpoint`：运行过程。
- `WebSearchQuery`、`WebSource`、`Citation`：搜索和引用。
- `FileReference`、`ContextItem`、`ContextUsage`：文件和上下文。
- `ArtifactProvenance`：回答或媒体的来源清单。

Session 和 Run 应按用户、项目、更新时间建立索引；来源和事件按 `runId + sequence` 建立唯一约束，避免 SSE 重放产生重复记录。

## 15. 实施阶段

### S0：交互和契约

- [ ] 确定页面布局、Session 列表和右侧详情面板。
- [ ] 确定联网四态、预检卡片和文件外发提示。
- [ ] 确定动作时间线、来源卡片、上下文账本和 Artifact 卡片。
- [ ] 确定 Session/Message/Run 数据契约。

验收：用静态 Mock 数据可以完整演示一次运行的各个面板。

### S1：独立 Session 和基础聊天

- [ ] Session 创建、列表、重命名、归档和恢复。
- [ ] 多轮消息和流式文本。
- [ ] 停止、取消、刷新恢复和事件回放。
- [ ] 使用公共 Agent Runtime 和 SSE。

验收：刷新页面后历史消息和正在运行的 Run 都能恢复。

### S2：只读联网和可观测性

- [ ] `offline` 和 `search_read`。
- [ ] 搜索记录、来源、最终引用和域名策略。
- [ ] 当前动作时间线和工具摘要。
- [ ] 失败、暂停、恢复和重试。

验收：用户能完整看到 Agent 搜索了什么、打开了什么、引用了什么。

### S3：文件、@引用和上下文

- [ ] 文件上传、解析、分块和权限过滤。
- [ ] 项目资源选择和 `@`提及。
- [ ] 文件页码/片段引用。
- [ ] 上下文用量、Token 估算/实际值和压缩。

验收：用户能定位回答使用的文件片段，并能手动移除或锁定上下文项。

### S4：Artifact 和画布互通

- [ ] 文本、图片、音频、视频 Artifact 保存。
- [ ] 产物溯源和成本统计。
- [ ] 用户明确选择画布目标后发送结果。
- [ ] 冲突检测和人工确认。

验收：独立页面产生的产物可以在资产库和画布中继续使用，来源不丢失。

### S5：深度研究和高级浏览器能力

- [ ] `deep_research` 后台运行。
- [ ] 更长任务的暂停、恢复和进度。
- [ ] 隔离浏览器 Worker、页面截图和 DOM 摘要。
- [ ] 点击、输入、上传、提交等高风险动作审批。

默认排在只读联网稳定后，不阻塞 MVP。

## 16. 测试矩阵

### 页面和状态

- [ ] 新建、切换、重命名、归档和恢复 Session。
- [ ] 多轮上下文正确传递，Fork 不修改原 Run。
- [ ] SSE 断线、浏览器刷新和 Worker 重启恢复。
- [ ] 停止、暂停、继续、超时和失败状态正确显示。

### 联网和来源

- [ ] 关闭联网时无外部请求。
- [ ] 搜索词、来源、引用和未引用来源准确展示。
- [ ] 域名白名单、黑名单、私有地址和恶意重定向阻断。
- [ ] 搜索结果冲突和读取失败可诊断。

### 文件和上下文

- [ ] 文件上传、解析失败、OCR、页码引用和权限隔离。
- [ ] `@`资源选择、替换、删除、版本冻结。
- [ ] 上下文用量估算、实际 usage、压缩和手动移除。
- [ ] 不会把未选择文件发送给外部 Provider。

### Artifact 和互通

- [ ] 文本/图片/音频/视频 Artifact 归档。
- [ ] 产物来源包含 Run、模型、搜索和文件引用。
- [ ] 发送回画布时目标明确、冲突不覆盖。
- [ ] 重复事件不重复创建资产或节点。

## 17. 独立页面发布前冒烟

```text
打开独立 Agent 页面
  -> 创建 Session
  -> 开启“搜索与读取网页”
  -> 上传一个 PDF
  -> 提出需要最新资料的问题
  -> 查看预检和外发提示
  -> 观察当前动作时间线
  -> 查看搜索词和打开页面
  -> 查看最终网页引用和 PDF 页码引用
  -> 查看上下文用量和成本
  -> 保存一份带来源报告
  -> 刷新页面并恢复历史
```

必须确认：

1. 联网开关由服务端强制执行。
2. 搜索记录和引用可回放。
3. 文件引用能定位到页码/片段。
4. 上下文用量能区分估算和实际。
5. 报告和 Artifact 具备来源溯源。
6. 页面刷新和 SSE 重连不重复发送或扣费。

## 18. 性能、可用性和体验优化

- 首屏先加载 Session 和最近消息，运行详情按需加载。
- 事件流按 `sequence` 合并，避免每个 Token 触发全页面重渲染。
- 搜索结果和正文按内容 Hash 缓存，避免同一 Run 重复抓取。
- 长任务使用后台队列，页面关闭不影响运行。
- 右侧面板支持折叠，默认不阻塞对话输入。
- 对话、动作、来源、上下文和文件面板提供键盘导航和无障碍标签。
- 提供“仅显示关键动作”和“显示详细事件”两种密度。
- 对搜索和工具结果提供复制、导出和重新执行入口。
- 对模型切换、联网切换和文件移除提供 Fork，而不是修改历史结果。

## 19. 安全和数据治理

- API Key、Cookie、密码、Bearer token 不进入浏览器和事件摘要。
- 文件外发前必须经过项目权限和用户选择校验。
- 网页内容、文件内容和 OCR 结果视为不可信输入。
- Session、消息、来源和文件引用按项目权限隔离。
- 支持用户删除会话消息，但保留审计所需的最小不可变 Run 元数据。
- 配置来源和事件保留时长，避免无限增长。
- 成本、Token 和供应商错误进入可观测性，但日志脱敏。

## 20. 产品指标

建议上线后观察：

- 首次响应时间和首个动作显示时间。
- 运行成功率、工具调用成功率和恢复成功率。
- 平均/P95 搜索耗时和页面读取失败率。
- 最终回答引用覆盖率。
- 文件引用命中率和上下文压缩次数。
- 单次 Token、成本、搜索次数和 Artifact 产出率。
- 用户停止、重试、Fork 和手动修正比例。

## 21. 完成定义

独立页面 Agent 只有在以下条件全部满足后才可标记完成：

1. Session、消息、Run 和事件可以持久化并恢复。
2. 用户可以明确开启/关闭联网，且模式在服务端强制执行。
3. 当前动作、搜索记录、来源引用、文件引用和上下文用量都可见。
4. 联网、文件和网页数据不会越权或静默外发。
5. 文本和媒体 Artifact 可以保存、导出并追溯来源。
6. 页面刷新、SSE 断线、Worker 重启和失败重试行为可恢复。
7. 发送结果回画布时目标明确，不能静默覆盖已有内容。

## 22. 待用户确认

- [ ] 独立页面首次默认联网是否为关闭。
- [ ] 首个搜索服务和搜索费用归属。
- [ ] 是否在 MVP 开放深度研究，还是先只做搜索与读取。
- [ ] 文件外发的项目级默认策略。
- [ ] 是否允许用户固定/排除来源。
- [ ] 上下文面板默认显示估算值还是只在有实际 usage 时显示。
- [ ] 是否需要报告导出为 Markdown、JSON 或 PDF。
- [ ] 独立页面产物发送到画布时支持哪些目标节点类型。
- [ ] Session 和来源的默认保留时长。
