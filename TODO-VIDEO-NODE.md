# 视频节点强化方案 TODO（待审核）

更新时间：2026-09-08
状态：审查修订稿，仅更新方案，未修改实现；待确认事项见第 11 节

## 1. 目标与硬性结论

视频节点必须被设计为**多输入节点**，不能用一个 `image` 字段或一个“参考图”端口代表所有视频参考。

长期产品目标（不等于 P0 全量交付；按第 10 节分阶段开放）：

- 同一个视频节点可以同时接收提示词、首帧、尾帧、角色参考、风格参考、通用图片参考、音轨和视频内容。
- 可重复输入使用 `N` 个有顺序的连接，不因第一张图片存在就拒绝后续图片。
- 首帧、尾帧、关键帧和普通参考图语义不同，不能互相静默替代。
- 生成（`generate`）与转换（`transform`）必须分别定义，不得把视频转换节点伪装成文生视频。
- Provider 不支持某个角色时必须在真实请求前明确报错，不能静默丢弃参考素材；该校验是输入契约保护，不是本地收费闸门。
- 视频成功归档后可以派生最后一帧图片预览/资产，并按节点设置追加图片源节点；该后处理失败不能改变原视频任务的成功状态。

### 1.1 当前费用边界

- 本视频强化阶段不实施本地收费、余额冻结或结算；后续由统一计费模块承接，边界见 [本地计费与模型广场执行方案](docs/billing-and-model-marketplace-plan.md)。该计费方案尚未实施，不代表当前视频流程已经具备钱包能力。
- 视频模块向统一计费模块提供稳定运行身份、Provider 请求身份、用量和结果状态；末帧派生不重新提交视频任务，也不得重复结算视频生成费用。
- 项目只记录供应商返回的状态、`usage`、请求 ID 和可选费用字段，用于展示、审计、故障诊断和幂等恢复；价格未知或费用字段缺失不阻塞视频功能实现。
- 步数、时长、输入大小、并发和超时等限制属于运行资源保护，不能被解释为本地计费限制。

## 2. 当前基线与缺口

审查基线：`main @ 747220b`（2026-09-08），工作区干净；Node `v24.12.0`、pnpm `11.19.0`。本节是该提交的核对结果，不代表后续实现状态。

| 分类         | 范围与代码证据                                                                                                                                                | 后续动作                             |
| ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------ |
| 已有且复用   | [Provider](packages/providers/src/index.ts) 的 `legacy-v1`、`newapi-unified-v1` 合同及冻结合同恢复；[合同测试](packages/providers/src/video-contract.test.ts) | 扩展而非重写，不改变历史任务合同     |
| 已有但需扩展 | [领域角色/快照](packages/domain/src/index.ts)、[默认连接](apps/web/src/connection-utils.ts)、Provider 输入映射                                                | 扩展多输入及能力组合校验，保留旧画布 |
| 全新能力     | `referenceImage`、`motionReference`、`keyframe`、可恢复末帧派生与完成动作                                                                                     | 按阶段新增，不能当作已有实现         |
| 外部待确认   | 各模型多参考、首尾帧、转换等组合的真实请求映射                                                                                                                | 逐合同取证，未知能力不开放真实调用   |

基线验证：`pnpm --filter @multimodal-canvas/providers exec vitest run src/video-contract.test.ts`，55 项通过；请求由注入式 fetch 模拟，不是新增多输入能力的真实供应商验收。

### 已有能力

- 领域层已经定义视频输入角色：`prompt`、`negativePrompt`、`content`、`style`、`character`、`firstFrame`、`lastFrame`、`audioTrack`、`transcript`、`mask`。
- 画布层允许图片连接到视频的 `style`、`character`、`firstFrame`、`lastFrame` 等端口。
- 运行快照保留输入角色和 `sortOrder`，具备保存顺序的基础。

### 当前阻塞

- `NewApiVideoProvider` 当前只保留一个 `prompt` 和一个 `firstFrame`。
- 当前请求只将首帧转换成对应合同的 `image` 字段；`style`、`character`、`lastFrame`、`audioTrack`、`mask` 等角色尚未映射，当前输入映射会明确拒绝不支持角色，不是静默丢弃。
- 视频 Provider 当前只允许 `generate` 模式，`transform` 模式会被拒绝。
- 图片拖到视频节点主体时默认映射到 `character`，用户无法表达“这是首帧/尾帧/风格图”的意图。
- 现有 E2E 主要验证画布可以连多个端口，尚未证明真实 New API 请求会携带这些参考素材。

## 3. 目标视频节点输入拓扑

### 3.1 固定语义端口

| 端口             | 上游类型       |                   基数 | 语义                                    |
| ---------------- | -------------- | ---------------------: | --------------------------------------- |
| `prompt`         | 文字           |                   0..1 | 主提示词；真实视频通常必填              |
| `negativePrompt` | 文字           |                   0..1 | 负面提示词                              |
| `firstFrame`     | 图片           |                   0..1 | 视频起始画面                            |
| `lastFrame`      | 图片           |                   0..1 | 视频结束画面                            |
| `audioTrack`     | 音频           | 0..N（由模型能力限制） | 旁白、音乐或口型驱动音轨                |
| `transcript`     | 音频/文字      |                   0..N | 转录或对白上下文                        |
| `mask`           | 图片           |                   0..N | 编辑/重绘区域；必须能绑定到内容或时间段 |
| `content`        | 视频/图片/文字 |                   0..N | 转换、视频生视频、编辑的主体内容        |

### 3.2 可重复参考端口

以下端口必须支持多个输入，并保留连接顺序：

| 端口                              | 上游类型 | 典型用途                           |
| --------------------------------- | -------- | ---------------------------------- |
| `referenceImage`（新增规范角色）  | 图片     | 通用参考图、产品图、场景图、道具图 |
| `character`                       | 图片     | 一个或多个角色/人物身份参考        |
| `style`                           | 图片     | 一个或多个风格、色板、镜头语言参考 |
| `motionReference`（新增规范角色） | 视频     | 动作、节奏或镜头运动参考           |

已有 `character`、`style` 角色要保持兼容；`referenceImage` 用于不适合归类为角色或风格的图片，避免把所有图片塞进 `character`。

### 3.3 关键帧序列

首帧和尾帧不足以表达复杂镜头，需要支持中间关键帧：

- 规范化输入：`keyframe`，内部统一使用非负整数 `timeMs`，以目标输出视频起点为 0；`frameIndex` 仅作带时间基/来源帧率的补充信息，不作为并行权威时间。供应商专用表示仅在适配层转换。
- 校验时间上限、重复时间及顺序；要求可确定的目标时长后才接受定时关键帧。首帧占用起点，尾帧是结束约束；关键帧与首尾约束冲突时明确报错，不静默覆盖。具体供应商末端时间量化规则需随合同确认。
- 至少支持 `first`、`middle...`、`last` 的有序序列。
- UI 可以先提供“添加关键帧”按钮，内部仍以有序输入集合保存。
- 若供应商只接受首帧/尾帧，必须由能力矩阵明确拒绝或提示，不得把中间关键帧静默丢掉。

## 4. 视频生成/转换模式

节点的 `mode` 保留 `generate` / `transform`。视频专用配置拆为“操作类型 + 输入约束集合”，具体字段名在 schema 评审时冻结；下表是能力场景，不是互斥的 `generationMode` 枚举。首尾帧与角色/风格参考可以组合，但必须通过模型组合能力校验。

| 视频模式                | 必要输入                             | 说明                     | 优先级 |
| ----------------------- | ------------------------------------ | ------------------------ | ------ |
| `text_to_video`         | `prompt`                             | 纯文生视频               | P0     |
| `image_to_video`        | `firstFrame` + `prompt`              | 让单张或首帧图片运动     | P0     |
| `first_last_frame`      | `firstFrame`、`lastFrame`            | 首尾画面约束或插值       | P0     |
| `reference_guided`      | `referenceImage`/`character`/`style` | 多参考图保持主体或风格   | P0     |
| `multi_keyframe`        | `keyframe[]`                         | 多关键帧控制镜头阶段     | P2     |
| `video_to_video`        | `content`（视频）                    | 基于已有视频改风格或重绘 | P1     |
| `edit_inpaint_outpaint` | `content` + `mask`                   | 局部编辑、扩展画面       | P2     |
| `extend_continue`       | `content`（视频）                    | 延长已有镜头             | P2     |
| `motion_transfer`       | `motionReference` + 角色图           | 将动作迁移到目标角色     | P2     |
| `audio_driven_lipsync`  | `audioTrack` + 视频/角色             | 音频驱动口型或表演       | P2     |
| `storyboard_multishot`  | 多镜头脚本/关键帧                    | 一次生成多镜头序列       | P2     |

规则：

- `generate` 节点可以承载 `text_to_video`、`image_to_video`、首尾帧和参考图生成。
- `transform` 节点必须要求可转换的 `content`（通常是视频），用于 `video_to_video`、编辑、延长等。
- 不允许因为 Provider 暂不支持转换，就把 `transform` 自动改成 `generate`。仅用上一视频末帧生成新片段仍属于生成，不冒充对原视频的延长操作。
- P0 场景表示规范输入与预检的覆盖目标；真实调用仅开放已取证组合，不要求一个模型支持表中全部 P0 场景。

## 5. 视频完成后的最后一帧派生

视频生成或转换任务成功、视频文件完成归档并可受保护读取后，节点可以自动提取最后一张**可解码**画面。该能力属于结果后处理，不重新提交视频任务，也不把后处理状态混入原视频任务状态。

### 5.1 触发、提取与资产

- 在视频 `asset`/`asset_version` 归档事务内持久化派生意图或 outbox；事务提交后由独立派生消费者执行。投递失败可扫描补偿，不能依赖浏览器在线或 SSE/轮询触发。
- API 仅显式发起重试/查询，SSE 和轮询负责展示；所有执行入口复用同一幂等服务。归档后投递失败不回滚原视频成功状态。
- 使用 `ffprobe` 读取视频流时长、帧率、时间基和编码信息，再由 FFmpeg 从靠近末尾的可寻址位置向前播放方向解码到结束，保留最后一张可解码帧；必要时在资源上限内扩大回溯窗口；不能把现有起始帧 poster 当作最后一帧。
- 提取产物与正式资产登记分离：仅预览时使用受保护临时产物；创建资产类动作才登记独立图片 `asset`/`asset_version`，使用 `derivativeKind=final_frame`，不覆盖源视频或已有图片版本。
- 派生提取记录关联源视频 `assetId`、`assetVersionId`，动作记录关联各自 `runId`；共享提取结果不覆盖运行归属，并保存提取时间戳或帧号、提取方法、FFmpeg/ffprobe 版本、策略版本和创建时间。
- 最后一帧图片必须可以作为后续 `firstFrame`、`lastFrame`、普通图片参考、画布图片源节点和 `@` 资源引用；导出/导入不能丢失来源关系。
- 视频损坏、无可解码帧、格式不支持或 FFmpeg 超时只将派生记录标记为失败并保留可诊断错误；原视频成功任务和视频资产保持成功，供应商返回的 `usage`/状态按原任务记录，本项目不改写计费结果。

### 5.2 完成动作

节点设置增加 `completionAction`（名称可评审）和可选的 `completionTargetNodeId`：

| 动作                         | 行为                                                 | 目标节点要求       |
| ---------------------------- | ---------------------------------------------------- | ------------------ |
| `none`                       | 不提取、不展示、不创建派生资产                       | 无                 |
| `preview_final_frame`        | 只在运行结果面板展示最后一帧临时预览，不登记用户资产 | 无                 |
| `create_asset`               | 创建最后一帧图片资产并显示在资产区                   | 无                 |
| `append_image_node`          | 创建图片资产，并追加一个绑定该版本的图片源节点       | 由系统创建新节点   |
| `fill_designated_image_node` | 创建图片资产，并填入用户明确指定的空图片节点         | 必须指定且保持为空 |

旧节点缺少设置时迁移为 `none`，不回补历史视频派生；新节点默认值待产品确认，确认前同样使用 `none`，`create_asset` 仅作为候选。自动追加图片节点必须由用户显式开启。`append_image_node` 只能通过源节点工厂创建一次，并保存与视频运行/派生资产的来源关系。不得根据画布坐标、创建时间或“最近的节点”推断下一个图片节点；指定目标已存在内容时必须拒绝填充并保留派生资产；本阶段不实现确认后替换，用户可另选空节点或显式追加。

### 5.3 状态与幂等

- 派生流程至少有 `skipped`、`pending`、`processing`、`ready`、`failed` 状态，UI 能区分“视频成功但最后一帧派生失败”。
- 最后一帧提取使用 `(projectId, sourceVideoAssetVersionId, extractionPolicyVersion)` 作为唯一幂等身份；动作使用非空稳定 `actionId`，绑定冻结的 `runId`、画布、源节点、动作与可选目标。重试复用该身份，显式新动作另建身份；不要将可空目标字段直接作为唯一去重依据。
- 消费者以原子条件更新领取任务，记录租约、尝试次数和领取代次；回写须匹配代次，超时回收后的旧 Worker 不得提交。数据库产物去重与执行领取分别验证。
- SSE 重放、轮询重复、任务重试、Worker 重启或消息重复投递不得重复生成图片资产、图片节点或重复创建 Provider 任务；资产已就绪但节点创建失败时可只重试动作应用。供应商侧费用和 `usage` 由上游负责，本项目只保持请求幂等并记录返回结果。
- 取消只影响尚未开始的派生任务；正在提取的任务应可超时回收，已生成的资产保持可追踪，不删除原视频结果。

### 5.4 画布并发与生命周期

- 追加/填充复用图片源节点工厂，并参与现有画布 revision 协议；同一事务完成 revision CAS、目标类型/空内容校验、节点写入、动作完成记录及 revision 更新。
- 冲突时保留派生资产并标记动作冲突，刷新画布后显式重试；不能绕过整图保存协议直接写节点。用户删除已创建节点后，事件重放不得重新创建。
- 源节点/目标节点被删除、项目已归档或权限失效时，阻止新的画布动作；提交前重检状态与权限，不把旧授权视为永久授权。
- 临时产物须定义 TTL、受保护读取、过期状态与清理所有权；过期后重新领取提取，不能将不存在的对象继续展示为 ready。升级正式资产时原子登记并转移或隔离清理所有权，禁止临时清理删除正式对象。
- 实施前补齐源视频、派生图、Run、项目、画布节点的归档/删除/恢复矩阵，对照 [现有 schema](prisma/schema.prisma) 的级联与空引用策略。不得默认级联删除仍被引用的派生图；来源不可用时保留可诊断来源标识，不泄露已失去权限的内容。
- 导入导出使用明确 ID 映射修复源版本、运行来源和目标节点引用；来源未包含或不可访问时保留缺失提示，不能错误绑定到同名资产。归档/删除策略与 TTL 未确定前，不开放对应派生动作。

## 6. 数据模型与连接协议 TODO

- [ ] 将视频输入从“按角色取一个值”升级为 `VideoInputSet`，区分单值、可重复值和带时间元数据的关键帧。
- [ ] 为重复输入定义稳定排序：沿用 `edge.order`/`sortOrder`，并在导入、导出、重试、恢复时保持不变。
- [ ] 为关键帧增加规范 `timeMs` 和可选来源帧信息，校验范围、重复时间和首尾顺序。
- [ ] 为 `mask` 增加与 `content`、帧或时间段的绑定关系，禁止出现无目标遮罩。
- [ ] 明确每个角色的基数、允许媒体类型和模型能力上限；超限在提交前报可诊断错误。
- [ ] 设计旧画布迁移：旧的 `firstFrame`、`style`、`character` 连线无需丢失；未知新角色必须保留并提示迁移失败。
- [ ] 保持来源节点只读、图无环和输入资产版本冻结等现有契约。
- [ ] 在视频节点快照中持久化 `completionAction`、`completionTargetNodeId` 和派生策略版本；未配置旧节点迁移为 `none`，不自动补跑历史任务。
- [ ] 扩展资产派生类型和关系模型，记录 `final_frame` 的源视频资产版本、运行 ID、提取元数据、状态和错误上下文。
- [ ] 为提取记录、资产创建和节点追加分别建立唯一约束/幂等键；导入导出、重试和恢复后仍能指向同一派生结果。
- [ ] 明确派生图片的权限、生命周期、存储配额和版本冻结规则，确保 `@` 资源索引只暴露用户有权访问的资产。

## 7. Web 画布与交互 TODO（PC 优先）

- [ ] PC 节点显示紧凑摘要、输入计数与有限缩略图；完整参考列表、分组配置和高级时间轴放侧栏，按阶段显示已开放能力。
- [ ] 节点外部尺寸仅由用户显式拖拽调整；输入增加、缩略图与结果回显使用内部滚动/折叠，不能撑大节点。
- [ ] 每个可重复角色提供“添加输入”槽位；侧栏同一角色可出现 `N` 行缩略图，并支持拖拽排序和删除。
- [ ] 首帧、尾帧和关键帧在时间轴上可见，不能只显示为同一种普通图片端口。
- [ ] 图片拖到节点主体时弹出角色选择（首帧/尾帧/角色/风格/通用参考），禁止静默默认为 `character`。
- [ ] 显式端口连接优先于自动推断；自动连接只在目标角色唯一且不会造成歧义时使用。
- [ ] 连接前显示兼容性和剩余容量；不支持的角色给出原因，不创建“看似成功”的边。
- [ ] 节点面板显示当前模式、输入计数、缺失必填项和 Provider 能力限制。
- [ ] 预览每个参考素材的缩略图、媒体类型、顺序和来源版本。
- [ ] 导入/导出后保持端口角色、排序、关键帧时间和模式不变。
- [ ] 在视频节点设置中提供完成动作选择、目标图片节点选择和当前派生状态；旧节点及默认值尚未确认的新节点使用 `none`，不得自动追加。
- [ ] 视频成功后在结果区展示最后一帧预览、来源视频/运行 ID和失败重试入口；后处理失败时明确说明视频本身仍已成功。
- [ ] `append_image_node` 使用图片源节点工厂追加节点并绑定派生图片版本；节点命名、位置和可见来源关系稳定，重复事件不重复追加。
- [ ] `fill_designated_image_node` 只允许填充用户明确指定且仍为空的图片节点；目标节点已填充、被删除或无权限时阻止写入并保留资产。
- [ ] 追加或填充完成后，资产区、画布节点和 `@` 资源索引立即刷新；派生图可继续连接到首帧、尾帧及其他参考端口。

## 8. New API / Provider 适配 TODO

- [ ] 复用已有双合同、冻结合同恢复与受保护下载；逐项确认尚未取证的创建参数、查询、远程取消、Webhook/HMAC、`usage` 和幂等语义。不猜测远程取消 endpoint；费用边界遵循第 1.1 节。
- [ ] 获取每个视频模型的能力矩阵：角色/媒体类型、单角色上限、总输入数量与字节上限、必填/互斥/依赖组合，以及模式对应时长、分辨率限制；分别支持两个角色不等于支持二者同时输入。
- [ ] 能力记录带 Provider、模型、合同版本、证据来源、确认时间和验证状态；区分支持、不支持与未知。前端提示、后端权威预检；运行快照冻结能力与合同版本，恢复不随目录刷新变更。
- [ ] 将 Provider 输入映射拆成“规范输入 -> 供应商字段”，禁止在画布层直接写供应商字段名。
- [ ] 对 `referenceImage[]`、`character[]`、`style[]`、`keyframe[]` 保持顺序、URL、MIME 类型和版本信息。
- [ ] 供应商字段未知时 fail-closed：请求前报 `UNSUPPORTED_INPUT_ROLE` 或更具体的能力错误，不发起不符合契约的 Provider POST。
- [ ] 供应商只支持一张图时，明确提示“模型上限为 1”，不能只发送第一张。
- [ ] 支持视频转换对应的真实 endpoint/参数；在契约未确认前，继续明确拒绝并记录阻塞项。
- [ ] 保留异步任务的 idempotency key、任务恢复、轮询/Webhook、取消和受保护下载。
- [ ] 参考素材无法被供应商访问时，保留脱敏 URL/资产版本诊断上下文，不泄漏 API Key、签名查询参数或短期访问令牌。
- [ ] 确认 Provider 返回的最终视频内容可下载、可解码且带可信 MIME/元数据；归档完成后再触发最后一帧派生，不从不稳定的临时 URL 提取。
- [ ] 在 API/Worker 中实现独立的 final-frame 派生队列、超时/资源限制、受保护读取和错误回写；FFmpeg/ffprobe 缺失时 fail-closed 并给出部署诊断。
- [ ] 暴露派生状态、来源关系和幂等结果供 Web 查询/SSE 刷新；不得因为派生失败重置视频任务或再次调用 Provider。

## 9. 测试与验收 TODO

各测试按第 10 节归属阶段执行；尚未开放的高级能力保持待办，不阻断不包含该能力的阶段验收。

### 9.1 协议与 Provider 单元测试

- [ ] 组合能力校验覆盖单独支持但组合互斥、未知能力、总字节/数量上限、切换模型后不兼容，以及目录更新不改变冻结任务；失败不得发起 Provider POST。
- [ ] 3 张以上图片同时连接到 `character`、`style`、`referenceImage`，验证全部进入规范输入集合且顺序稳定。
- [ ] 首帧 + 尾帧 + 至少 2 个中间关键帧，验证时间顺序、重复和越界校验。
- [ ] 文字提示词 + 首帧 + 尾帧 + 角色图 + 风格图 + 音轨：Mock 验证完整保留；真实适配只验证已取证组合的映射，未知/不支持组合验证请求前拒绝。
- [ ] 视频转换：视频 `content` + 图片参考 + 遮罩，Mock 验证转换语义；真实适配按已取证组合验证对应合同，不把全量组合作为统一发布门槛。
- [ ] Provider 不支持某角色、超过数量上限或收到错误媒体类型时，确认不会创建不符合契约的 Provider 任务。
- [ ] 失败、重试、Worker 重启和幂等恢复不会重复创建任务或改变输入顺序；供应商返回的 `usage` 仅作为可观察结果保存。
- [ ] 使用包含已知末帧的测试视频验证提取时间戳/帧号、MIME、尺寸、来源视频版本和运行 ID均正确持久化。
- [ ] 覆盖可变帧率、尾部损坏、无可解码帧、FFmpeg 缺失/超时和下载失败；确认原视频成功状态不被派生错误改变。
- [ ] 重放 SSE、轮询、重试和 Worker 重启，按动作断言：`none` 不提取，`preview_final_frame` 不登记正式资产，创建资产类动作复用同一派生资产；同一 `actionId` 至多追加一个节点或填充一次，显式新动作独立验收。

### 9.2 Web/E2E

- [ ] 在一个视频节点上可视化创建并重排至少 5 个参考输入。
- [ ] 首帧、尾帧、角色、风格和通用参考各连一张图，页面显示正确标签和缩略图。
- [ ] 直接拖到节点主体必须先选择角色；取消选择不创建边。
- [ ] 保存、刷新、导出、重新导入后，所有端口和顺序一致。
- [ ] Mock Provider 能模拟所有模式和多输入结果，真实 New API 测试只使用隔离凭据。
- [ ] 控制台无未处理异常，错误消息包含角色、数量上限、模型和任务 ID（不含密钥）。
- [ ] 分别验收五种 `completionAction`；验证追加节点显示最后一帧、目标节点冲突不覆盖、取消/重试状态可恢复。
- [ ] 保存、刷新、导出、重新导入后，派生资产来源、动作设置、目标节点和 `@` 引用仍然有效。

### 9.3 派生可靠性专项（P1）

- [ ] 归档事务提交后、队列投递前退出；恢复可补投且不再次调用视频 Provider。
- [ ] 两个消费者并发领取、租约过期后旧 Worker 回写、重复事件、共享产物的多运行动作，验证去重与隔离。
- [ ] 用户整图保存与自动追加/填充并发；目标非空、被删除或权限失效时不覆盖、不丢失用户编辑。
- [ ] 已追加节点被用户删除后重放不复活；源节点删除或项目归档后不新增画布动作。
- [ ] 临时预览过期、升级正式资产后清理、源资产/项目/Run 生命周期变化与导入 ID 重映射。
- [ ] 输入增多和结果回显不改变节点外部尺寸，侧栏排序/折叠可用，控制台无未处理错误。

### 9.4 验收样例

以下为完整规范输入的综合 Mock 用例，按 P2 能力齐备时验收，不作为 P0 或单一真实模型的发布门槛：

```text
角色图 x2 + 风格图 x2 + 通用参考图 x3
       + 首帧 + 中间关键帧 x2 + 尾帧
       + 提示词 + 音轨
       -> 一个视频生成节点
       -> 规范输入集合顺序稳定
       -> Mock 适配请求保留全部规范输入
       -> 视频任务可恢复、归档并回看
```

真实验收按 `Provider × 模型 × 合同版本 × 输入组合` 分项记录合同证据、冻结能力、脱敏请求、任务 ID、归档和恢复结果。P0 至少通过一个已确认组合；没有多参考实证时不得宣称真实多参考支持。所有已开放组合须分别验收，不要求一个模型通过全量组合。

最后一帧派生在 P1 开放前必须另有冒烟用例：

```text
视频生成成功并归档
       -> 提取最后一张可解码帧
       -> 根据动作保留临时产物或登记 final_frame 图片资产
       -> 提取关联视频资产版本，动作关联各自 runId
       -> 按 completionAction 预览、创建资产、追加图片源节点或填充指定空节点
       -> 重放完成事件不重复生成资产/节点
       -> 派生失败时视频仍保持成功并可重试
```

## 10. 分阶段执行顺序

### P0：多输入规范、能力预检与最小真实闭环

- [ ] 规范 VideoInputSet、角色基数与稳定排序；支持保存 firstFrame、lastFrame、character[]、style[]、referenceImage[]，不等于全部真实模型均支持。
- [ ] 实现操作类型与输入约束集合、组合能力矩阵、后端权威预检及运行快照冻结。
- [ ] PC 角色选择和紧凑节点摘要；导入导出与旧画布兼容，缺失完成动作迁移为 none。
- [ ] Mock 与 Provider 契约测试覆盖多输入完整性、组合限制和无静默丢弃。
- [ ] 复用双合同与恢复逻辑，至少完成一个已确认真实模型组合的创建、恢复、归档和回看；其余组合保留明确阻塞，不伪装真实支持。

### P1：末帧派生与按合同扩展

- [ ] 先冻结派生生命周期矩阵、TTL、资源限制及动作身份；实现归档事务内意图/outbox、补偿扫描、租约与过期领取隔离。
- [ ] 实现最后可解码帧提取、临时预览与资产登记分离、派生状态回写和独立重试。
- [ ] 实现 completionAction、显式追加和指定空节点填充；接入画布 revision 与原子条件检查，不覆盖用户内容、不复活已删除节点。
- [ ] 完成派生并发、故障窗口、权限变化、生命周期与导入导出验收。
- [ ] 按供应商已确认合同逐个扩展首尾帧/多参考组合；V2V 为候选增量，独立验收，不与末帧交付捆绑。未知合同不实现猜测映射。
- [ ] 对已开放能力验证恢复、取消边界、Webhook（支持时）和 usage/状态；不实施本地扣款，复用统一运行身份。

### P2：高级时间轴、编辑和镜头能力

- [ ] 多关键帧及规范 timeMs、帧时间换算与冲突校验。
- [ ] 局部编辑、画面扩展、原视频延长，以及 mask 与内容/时间段绑定。
- [ ] audioTrack、transcript 的高级组合、音频驱动口型和表演。
- [ ] 动作参考/动作迁移、多镜头 storyboard、镜头连续性和批量生成。
- [ ] 增强可视化兼容矩阵和替代建议；基础预检与不支持原因展示必须在 P0 提供。

### 10.1 实施追踪与检查点

下表是实施入口，不是完成记录。实施时每项补充负责模块、最后成功命令、代码提交、测试结果、外部证据与剩余阻塞；不能只依赖口头交接。

| 项目             | 前置条件                    | 主要代码落点                                                                      | 验收证据                                     | 当前状态                      |
| ---------------- | --------------------------- | --------------------------------------------------------------------------------- | -------------------------------------------- | ----------------------------- |
| P0 输入与兼容    | 角色/组合结构冻结           | packages/domain/src/index.ts、apps/web/src/connection-utils.ts、API 快照/导入导出 | 规范输入、旧画布往返、PC 冒烟                | 待实现                        |
| P0 Provider 预检 | 模型组合合同取证            | packages/providers/src/index.ts、API 能力目录与运行提交                           | 无 POST 失败测试、合同测试、至少一个真实组合 | 基础合同已有；扩展待实现/取证 |
| P1 派生处理      | 生命周期、TTL、资源策略确认 | prisma/schema.prisma、apps/worker/src/result-archiver.ts、独立派生消费者          | 崩溃窗口、并发领取、提取/登记/清理测试       | 待设计冻结与实现              |
| P1 画布动作      | 派生产物与动作身份确定      | apps/api/src/projects.ts、Web 结果面板/节点工厂                                   | revision 竞争、用户删除、权限变化、导入导出  | 待实现                        |
| P2 高级能力      | 对应组合真实合同确认        | 领域时间轴、Provider 适配、Web 侧栏                                               | 各已开放组合独立取证                         | 后置                          |

本次文档修订检查点（2026-09-08）：只修订本文件，未改变运行时、schema 或依赖；后续从 P0 开始，不能将上述方案规则视作已实现。

## 11. 风险与待审核决策

- [ ] **供应商契约**：New API 是否支持 `reference_images`、首尾帧、关键帧、音频和遮罩，必须以正式文档或隔离实测为准，不能猜字段名。
- [ ] **关键帧合同换算（P2）**：内部统一 `timeMs` 已确定；供应商时间基、末端量化和 VFR 换算需逐合同取证。
- [ ] **重复端口上限**：产品默认上限、模型上限和供应商上限取最小值；需要产品确认默认值。
- [ ] **结构冻结（P0）**：采用 `referenceImage`；实施前冻结输入条目 ID、操作类型/约束集合字段、schema 版本与迁移测试，不将通用参考混入 `content`。
- [ ] **转换合同（P1/P2）**：P1 仅将已确认 V2V 作为独立候选；编辑和原视频延长后置 P2。候选未支持不阻断末帧派生验收。
- [ ] **未知能力提示（P0）**：真实请求一律 fail-closed；设计显式切换 Mock 的入口和标识，不自动降级，不把 Mock 当作真实验收。
- [ ] **运行资源策略**：多参考图、关键帧和多镜头是否需要单独的输入数量、并发、时长或超时限制；不设置本地金额预算。
- [ ] **新节点默认动作（P1）**：产品确认是否采用 `create_asset`，确认前使用 `none`；旧节点固定迁移为 `none`，自动追加必须显式开启。
- [ ] **目标节点展示（P1）**：确认追加布局与来源关系展示；指定目标非空时拒绝填充，本阶段不提供覆盖流程。
- [ ] **末帧策略（P1）**：以最后可解码显示帧为准，不承诺损坏尾部的严格时间轴末端；以 VFR、B 帧、旋转信息和损坏样例确定回溯上限与诊断。
- [ ] **媒体处理依赖**：FFmpeg/ffprobe 的版本、容器镜像安装、CPU/内存/超时配额和无依赖部署时的降级方案。
- [ ] **派生资产治理**：最后一帧是否计入配额、保留多久、能否单独删除，以及删除后 `@` 引用和画布节点如何提示。

## 12. 完成定义

每阶段只验收第 10 节声明的范围，记录未开放能力和阻塞；P0 完成不代表 P1/P2 完成。整体方案只有同时满足以下条件才可标记全部完成：

1. 画布能表达并保存多输入、端口角色、顺序和关键帧时间。
2. 生成与转换模式的输入契约清晰且不会相互冒充。
3. New API 或其他真实 Provider 对每个已开放角色都有已确认映射；未支持角色会在真实请求前失败，避免静默丢弃输入。
4. 多输入工作流经过单元测试、E2E、隔离真实任务、恢复/重试和归档验证。
5. 视频成功后可按设置稳定地产生最后一帧预览/资产/图片节点；重复事件不重复创建，目标节点不会被静默覆盖。
6. 派生失败不会污染原视频成功状态，且 FFmpeg/ffprobe、权限、存储和重试错误均有可诊断路径。
7. 导入导出、旧画布迁移、错误诊断和模型能力限制均有文档证据。

各阶段还须完成范围匹配的 lint/typecheck/test/build（纯文档变更可用格式、链接与差异检查替代）、关键流程冒烟、必要文档同步和 Git 状态检查。真实 Provider 验收须单独授权隔离凭据与次数，保留已知任务身份，结果未知不得自动重发。

本文件是审核修订方案，不代表上述能力已经实现。
