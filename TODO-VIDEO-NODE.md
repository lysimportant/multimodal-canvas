# 视频节点强化方案 TODO

更新时间：2026-09-14
状态：P0 四模式画布已落地（文生视频 / 首帧 / 首尾帧 / 全能参考）。视频编辑、视频延长后置。
基线：`main @ c0b7092`；Node `v24.12.0`、pnpm `11.19.0`

## 1. 目标与硬性结论

视频生成节点是**一个节点、一次运行一种模式**。跨 Grok / MiniMax H3 / Wan3 的产品模型相同：画布先选模式，再露出该模式的端口，最后由 Provider 按已取证合同映射。不要为每个供应商做一套节点，也不要把角色、风格、通用参考当成三种模式。

本阶段落地 4 个模式，另外 2 个只保留枚举和文档：

| 模式     | 画布输入                                      | 本阶段                                                 |
| -------- | --------------------------------------------- | ------------------------------------------------------ |
| 文生视频 | 提示词（+ 可选负面提示词口）                  | 已落地                                                 |
| 首帧     | 提示词 + 一张首帧                             | 已落地                                                 |
| 首尾帧   | 提示词 + 首帧、尾帧两个可见槽位               | 已落地                                                 |
| 全能参考 | 提示词 + 可重复的参考图 / 参考视频 / 参考音频 | 已落地（真实 POST 仅 grok-imagine-video-1.5 的参考图） |
| 视频编辑 | 提示词 + 主视频                               | 后置                                                   |
| 视频延长 | 提示词 + 主视频                               | 后置                                                   |

硬性规则：

- 节点 `mode` 仍是 `source` / `generate`。生成方式写在 `data.videoMode`。
- 一次运行只使用一种 `videoMode`。默认互斥：首帧/首尾帧与全能参考不能混用。Grok 1.5 官方允许叠首帧 + `reference_images`，那是可选的家族例外，不是全模型默认。
- 角色 / 风格不是模式。旧连线在全能参考里仍收成参考图；新交互不再弹出这两个入口。
- 编辑 vs 延长都吃主视频，差别在意图，不能靠用户有没有写「延长」来猜。本阶段直接不开放。
- 画布按能力矩阵展示 4 个已落地模式；未取证字段 fail-closed，禁止把 DashScope / MiniMax 字段名猜进 New API。
- 产品已取消独立「转换节点」。历史 `transform` 读成 `generate`。用上一视频末帧再生成新片段仍是生成，不冒充原视频延长。
- 本阶段不实施本地收费。末帧派生属于 P1，失败不得改写原视频成功状态。

### 1.1 当前产品约束

- 画布只创建 `source` / `generate`。Prisma 仍保留 `TRANSFORM` 枚举以免旧行无法加载。
- 新视频生成节点默认 `videoMode=text_to_video`。旧画布缺省该字段时保持全量端口，运行时按连线推断。
- 显式模式的可见锚点：文生视频左侧不接图；首帧左侧接首帧；首尾帧左侧首帧、下侧尾帧；全能参考左侧是参考磁吸点。
- 视频合同有三种：`newapi-video-v1`（API/Worker/Docker 默认，`POST /v1/videos`）、`newapi-unified-v1`、`legacy-v1`。构造器默认仍是 `legacy-v1`；以运行配置和冻结 payload 为准。
- 已确认可发真实 POST：所有模型的 `prompt` + 可选一张 `firstFrame`；`grok-imagine-video-1.5*` 另支持尾帧 `last_frame` 与参考图 `reference_images`（最多 7 张，最高 720p）。H3 / Wan3 的首尾帧与全能参考可在画布选择，真实字段未取证前不发 POST。
- 已有真实验收是 `legacy-v1` + `grok-imagine-video-1.5（按次）` 的文生视频恢复闭环，见 [TODO-LOCAL.md](TODO-LOCAL.md)。这不证明 H3 / Wan3 全能参考已开通。

### 1.2 费用边界

- 不实施本地钱包、冻结或结算；后续见 [本地计费与模型广场执行方案](docs/billing-and-model-marketplace-plan.md)。
- 只记录供应商状态、`usage`、请求 ID 和可选费用字段。价格未知不阻塞功能。
- 步数、时长、输入数量、并发和超时是资源保护，不是本地计费。

### 1.3 能力矩阵（跨模型兼容，不是写死 Grok 字段）

```
video generate node
  └─ modelAlias（Grok 1.5 / MiniMax H3 / Wan3 / …）
       └─ explicit videoMode（1–6，由矩阵决定是否可选 / 能否 POST）
            └─ 该模式的端口
                 └─ Provider 映射（未取证 fail-closed）
```

- 画布兼容全模型：四个已落地模式对所有视频模型可选。
- `livePost` 才允许真实请求。当前：文生视频与首帧对未知模型开放；首尾帧与全能参考仅 grok-imagine-video-1.5 已接通。
- Grok 全能参考只收图片。H3 / Wan3 概念上可收图/视频/音频，但 New API 字段未取证，预检明确失败。
- 不要自动插入 `<IMAGE_1>` / `图1` / `<Picture i>` 提示词标签。

## 2. 当前基线与缺口

| 分类         | 现状                                                                                              | 后续动作                                               |
| ------------ | ------------------------------------------------------------------------------------------------- | ------------------------------------------------------ |
| 已有且复用   | 三套视频合同、冻结合同恢复、受保护下载、[合同测试](packages/providers/src/video-contract.test.ts) | 扩展输入预检，不改历史任务合同                         |
| 已有但需扩展 | 领域角色/快照、`sortOrder`、画布多端口、图片默认可见首帧锚点                                      | 增加 `referenceImage`、`VideoInputSet`、主体拖放选角色 |
| 产品已变更   | 转换节点入口删除；历史 `transform` → `generate`                                                   | 方案不再把双 mode 当 P0 交付；V2V 仍作为未取证能力     |
| 全新能力     | `referenceImage`、全能参考融合预检、可恢复末帧派生                                                | 按阶段新增                                             |
| 外部待确认   | 各模型多参考、首尾帧、音视频融合的真实字段                                                        | 逐合同取证，未知不开放真实调用                         |

### 已有能力

- 领域角色：`prompt`、`negativePrompt`、`content`、`style`、`character`、`firstFrame`、`lastFrame`、`audioTrack`、`transcript`、`mask`。
- 画布允许把图、视频、音频连到视频节点的对应端口；运行快照保留角色和顺序。
- `NewApiVideoProvider` 对未映射角色 fail-closed，不是静默丢弃。
- 图生视频已把首帧映射为各合同的 `image` 字段（字符串或 `{ url }`，按合同）。

### 当前阻塞

- 规范输入仍按「每角色一个值」在 Provider 侧消费，没有 `VideoInputSet`。
- 缺少 `referenceImage`；通用参考容易被塞进 `character` 或 `content`。
- 图片落到节点主体曾静默映射 `firstFrame`，用户无法表达尾帧/角色/风格/通用参考。
- `lastFrame`、`style`、`character`、音轨、视频内容和多参考均未取证，真实请求必须拒绝。
- 末帧派生、完成动作和 FFmpeg 流水线尚未设计冻结。

## 3. 目标输入拓扑

画布层允许融合全部资源；供应商层只发送已取证字段。

### 3.1 固定语义端口

| 端口             | 上游类型       | 基数 | 语义                                                 |
| ---------------- | -------------- | ---- | ---------------------------------------------------- |
| `prompt`         | 文字           | 0..1 | 主提示词；真实视频通常必填                           |
| `negativePrompt` | 文字           | 0..1 | 负面提示词                                           |
| `firstFrame`     | 图片           | 0..1 | 起始画面                                             |
| `lastFrame`      | 图片           | 0..1 | 结束画面                                             |
| `audioTrack`     | 音频           | 0..N | 旁白、音乐、口型或氛围音                             |
| `transcript`     | 音频/文字      | 0..N | 对白或转写                                           |
| `mask`           | 图片           | 0..N | 编辑区域，须绑定内容或时间段                         |
| `content`        | 视频/图片/文字 | 0..N | 融合用的主体视频/素材；图片 content 仅兼容映射为首帧 |

### 3.2 可重复参考端口

必须保序：

| 端口                     | 上游 | 用途                                             |
| ------------------------ | ---- | ------------------------------------------------ |
| `referenceImage`（新增） | 图片 | 产品、场景、道具等不宜归入角色/风格的参考        |
| `character`              | 图片 | 角色/人物身份                                    |
| `style`                  | 图片 | 风格、色板、镜头语言静帧                         |
| `motionReference`（P2）  | 视频 | 动作、运镜、节奏；P0 先用 `content` 保存视频参考 |

即梦全能参考把「图 + 视频 + 音频」一次投喂，再用提示词说明用途。本项目用端口角色表达用途，比把所有文件丢进一个列表更可校验。P0 不实现视频提示词内 `@素材` 绑定；现有 `@` 提及对视频仍 fail-closed。

### 3.3 关键帧序列（P2）

内部统一 `timeMs`。供应商只收首尾帧时，能力矩阵拒绝中间关键帧，不得静默丢弃。

## 4. 生成模式（一次运行一种）

节点 `mode` 仍是 `generate`。真正的生成方式是 `data.videoMode`，互斥：

| videoMode                     | 必要输入                              | 说明         | 真实合同                                              |
| ----------------------------- | ------------------------------------- | ------------ | ----------------------------------------------------- |
| `text_to_video`               | `prompt`                              | 纯文生视频   | 已确认                                                |
| `first_frame`                 | `firstFrame` + `prompt`               | 单张首帧运动 | 字段已映射；隔离真实验收另做                          |
| `first_last_frame`            | `firstFrame` + `lastFrame` + `prompt` | 两个可见槽位 | grok-imagine-video-1.5 已映射；其他模型 fail-closed   |
| `omni_reference`              | 至少一项参考图/视频/音频 + `prompt`   | 不固定首尾帧 | grok-imagine-video-1.5 仅参考图；其他模型 fail-closed |
| `video_edit` / `video_extend` | 主视频 + `prompt`                     | 后置         | 未开放                                                |

规则：

- 真实调用只开放 `livePost` 组合。画布可以先连线，请求前必须说明为什么不能发。
- 旧画布未写 `videoMode` 时：图片 `content` 仍兼容为首帧。显式全能参考下图片 `content` 收成参考图。
- 不允许因为未支持 V2V 就把视频内容改写成首帧图。
- 旧的 `reference_guided` 摘要只用于未写模式的推断，不再作为画布入口。

## 5. 最后一帧派生（P1）

视频归档成功后由独立消费者提取最后一张可解码帧。动作：`none` / `preview_final_frame` / `create_asset` / `append_image_node` / `fill_designated_image_node`。旧节点与未确认默认值均为 `none`。派生失败不改变原视频成功状态。细节保持上一版第 5 节约束，实施前再冻结生命周期矩阵。

## 6. 数据模型与连接协议 TODO

- [x] 将视频输入升级为 `VideoInputSet`，区分单值、可重复值。
- [x] 新增并冻结 `referenceImage`；旧 `firstFrame` / `style` / `character` 连线不丢失。
- [x] 重复输入沿用 `edge.order` / `sortOrder`，导入导出和重试保持顺序。
- [x] 明确基数、媒体类型和模型上限；超限或未取证角色在提交前报错。
- [x] 保持来源只读、图无环、输入资产版本冻结。
- [x] 完成动作写入节点快照；末帧幂等身份按运行/源节点/动作构造。
- [ ] （P1 余项）独立 TTL 临时对象与导入导出源关系映射。

## 7. Web 画布 TODO（PC 优先）

- [x] 生成节点提供显式生成模式选择器；编辑/延长禁用并标明后置。
- [x] 显式模式按端口互斥：文生视频不接图；首帧自动落首帧；首尾帧弹出首/尾；全能参考自动落参考图/视频/音频。
- [x] 旧画布未写 `videoMode` 时主体投放只问首帧/尾帧/参考图，不再把角色/风格当成模式。
- [x] 显式端口连线优先；首尾帧下侧可见尾帧槽。
- [x] 节点显示紧凑输入计数，不因输入增多撑大外部尺寸。
- [x] 不支持的角色在运行失败诊断中给出角色和原因，不含密钥。
- [x] 导入导出后端口角色与顺序不变。
- [x] 节点设置可选完成后动作；运行结果展示末帧状态与预览。
- [x] 追加图片节点与填充指定空节点在 Worker 归档后尽力写入；冲突保留资产不覆盖。

## 8. Provider 适配 TODO

- [x] 复用三套合同与冻结合同恢复；禁止猜测未取证字段。
- [x] 规范输入到供应商字段的映射集中在 Provider，画布不写供应商字段名。
- [x] 已确认：`prompt` + 可选一张首帧 `image`。
- [x] grok-imagine-video-1.5 系列：尾帧 `last_frame`；全能参考把参考图（含旧角色/风格连线）并入 `reference_images`（最多 7 张，最高 720p）。显式全能参考不再发送首帧。
- [x] 全能参考吸收提示词里的图片 `@` 提及为参考图，不再误走聊天资源提及能力预检。
- [x] 提示词上方资源条：缩略图/icon、右下角删除、Dialog 命名；提示词中资源名高亮，输入同名即绑定。
- [x] 显式 `videoMode` 写入运行快照；H3/Wan3 全能参考与首尾帧在未取证前 fail-closed。
- [x] 未确认角色 fail-closed：`UNSUPPORTED_INPUT_ROLE` 或更具体的组合错误，不发起 POST。
- [x] 供应商只支持一张图时，提示上限为 1，不能只发第一张。
- [x] 保留幂等键、恢复、轮询、取消和受保护下载。
- [x] 视频归档成功后提取最后可解码帧；FFmpeg 缺失或提取失败不改变视频成功。

## 9. 测试与验收 TODO

- [x] `VideoInputSet`：多张 `character` / `style` / `referenceImage` 顺序稳定。
- [x] 组合预检：未取证角色、数量上限、错误媒体类型均不发 POST。
- [x] 显式模式预检：缺首帧、缺尾帧、文生视频带参考、全能参考叠首帧均在 POST 前失败。
- [x] 旧画布未写模式时图片 content 到首帧兼容仍可用；全能参考下图片 content 收成参考图。
- [x] 不把 Mock 或合同单测当成真实多参考验收。
- [ ] 真实验收按 Provider、模型、合同、组合分项授权；禁止自动重发未知 POST。

## 10. 分阶段执行顺序

### P0：融合输入规范、角色选择与 fail-closed

- [x] `VideoInputSet` + `referenceImage` + 稳定排序。
- [x] 显式 `videoMode` + 能力矩阵；旧画布仍可按连线推断。
- [x] 后端权威预检；运行快照冻结已连接角色和模式。
- [x] PC 模式选择、按模式端口和紧凑摘要。
- [x] 合同测试覆盖多输入完整保存与无静默丢弃。
- [x] 真实闭环沿用已确认文生视频组合；不宣称全能参考已开通。

### P1：末帧派生与按合同扩展

- [x] 归档后提取最后可解码帧，支持 none/预览/创建资产/追加节点/填充空节点。
- [ ] 独立 outbox 消费者、租约领取与临时对象 TTL 仍待加强。
- [x] grok-imagine-video-1.5 已按官方 REST 扩展首尾帧与多参考图。
- [ ] 其他模型及 V2V/音视频融合仍待取证。

### P2：视频编辑、视频延长与高级参考

- [ ] 视频编辑、视频延长：需独立确认 New API / 经典 Grok 编辑延长端点，不在 1.5 生成合同上猜字段。
- [ ] `motionReference`、`keyframe`、mask 绑定、多镜头。
- [ ] H3 / Wan3 全能参考与首尾帧的 New API 字段取证后再打开 `livePost`。

### 10.1 实施追踪

| 项目             | 前置条件     | 主要落点                           | 验收证据                        | 当前状态                       |
| ---------------- | ------------ | ---------------------------------- | ------------------------------- | ------------------------------ |
| P0 输入与兼容    | 结构冻结     | domain、connection-utils、API 快照 | 规范输入、旧画布往返、PC 选角色 | 实施中                         |
| P0 Provider 预检 | 合同取证边界 | providers、运行提交                | 无 POST 失败测试                | 实施中                         |
| P1 派生处理      | 生命周期确认 | worker 归档后提取、API final_frame | 提取单测与归档回归              | 已落地主路径；无独立 outbox 表 |
| P2 高级能力      | 对应合同确认 | 时间轴与适配                       | 分项取证                        | 后置                           |

## 11. 风险与待审核决策

- [ ] **供应商契约**：New API / 即梦等是否支持多参考、首尾帧、音视频融合，以正式文档或隔离实测为准。
- [ ] **重复端口上限**：产品默认、模型上限、供应商上限取最小值；全能参考样例常见为图不超过 9、视频不超过 3、音频不超过 3、总计不超过 12，不能当成本项目已确认配额。
- [ ] **结构冻结**：采用 `referenceImage`；不把通用参考混入 `content`。
- [ ] **未知能力**：真实请求 fail-closed；Mock 必须显式，不当真实验收。
- [ ] **新节点默认末帧动作**：确认前 `none`。
- [ ] **媒体依赖**：FFmpeg/ffprobe 部署与降级。
- [ ] **派生资产治理**：配额、TTL、删除后的引用与画布提示。

## 12. 完成定义

P0 完成不等于 P1/P2 完成，也不等于全能参考真实开通。整体完成仍需：多输入可保存、未取证角色请求前失败、已开放组合有合同证据、末帧派生幂等且不污染视频成功、导入导出与诊断完备。真实验收必须单独授权凭据和次数，已知任务不得自动重发。
